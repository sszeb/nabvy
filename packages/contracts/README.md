# @nabvy/contracts

Zod schemas and inferred types for everything that crosses a boundary: events, fact templates,
the pack format, oRPC input and output, and model output (`docs/contracts.md`). Filled in task 0.2.
