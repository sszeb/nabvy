// generated by scripts/new-module.mjs; edit freely
// Drizzle Kit configuration for the asking-price-index module's migrations. Paths are relative to
// packages/db, where `pnpm db:generate asking-price-index` runs. See packages/db/README.md.
import { defineConfig } from 'drizzle-kit'

export default defineConfig({
  dialect: 'postgresql',
  schema: './src/schema/asking-price-index.ts',
  out: './migrations/asking-price-index',
  schemaFilter: ['asking_price_index'],
  migrations: { prefix: 'supabase' },
})
