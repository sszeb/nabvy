# Open questions

Agents append here when a spec is unclear or a secret is missing. Format: date, task, question, option taken and why. A human reviews at each check-in and moves answers into `docs/decisions.md`.

- **2026-09-24, Phase 1 (1.1 onwards): which runtime runs the pipeline, and where is Apify called from?** The build pack runs every provider call from Trigger.dev tasks with `APIFY_TOKEN` in the Trigger.dev vault, and names Supabase Queues + Edge Functions only as the fallback. The actor brief says the Apify token is a Supabase Edge Function secret, and a later note in the session said "use Supabase for this". Option taken: none yet. Task 0.1 does not depend on it, and `@nabvy/config` keeps both the `trigger` and `apify` groups. The brief's `APP_INTEGRATION_GUIDE.md` ("how to call the Actor") is expected to settle it.
- **2026-09-24, 0.2 onwards: rewriting the build pack to match the brief.** The owner decided the brief wins where the two conflict (`docs/decisions.md`, "Precedence"). Option taken: record the conflicts in that table now, and rewrite the affected documents (`CLAUDE.md`, `contracts.md`, `architecture.md`, `providers.md`, `valuation.md`, `scan-mode.md`, `compliance.md`, `backlog.md`) once `APP_INTEGRATION_GUIDE.md` lands, before task 0.2. Conservative because the guide will define the contracts and the Supabase schema, and rewriting twice risks inventing policy.
- **2026-09-24, all tasks: branch names.** The build pack wants one `task/<id>-<slug>` branch per task; this session was assigned `claude/hopeful-wright-r5ygps`. Option taken: work on the assigned branch, one pull request per task into `main`. Conservative because the session may only push to that branch.
- **2026-09-24, 1.0: Apify access from the build environment.** The cloud session's network policy blocks `api.apify.com`, so an agent here cannot read the actor's metadata or start the task 1.0 run. Option taken: none; the owner either adds `api.apify.com` to the environment's allowed domains or the calls go through the Supabase route above. No actor run is started without the owner's explicit approval of that run and its cost.
