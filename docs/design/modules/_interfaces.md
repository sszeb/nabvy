<!-- generated by scripts/interface-digest.mjs; edit freely -->

# Module interfaces

One short block per `services/<module>/README.md`: its exported functions, views and events,
taken from the README's Inputs, Outputs and Tables sections (or their equivalents). Run
`pnpm interfaces` to refresh after a README changes; nothing here is hand-maintained.

## account
The account's life: profile, Telegram links, push subscriptions, deletion requests, API keys and
**Inputs:**
- Web forms: profile updates, Telegram link-code requests, push subscribe/unsubscribe, export and deletion…
- The Telegram bot's link callback: `confirmTelegramLink()`, with no user session, runs inside `withPipeline`.
- `setStanding()` calls from `account-integrity` (soft dependency) or an audited admin override.
- Reads: `@nabvy/switches`' `isOn(q, 'account')` and `isOn(q, 'account-integrity')`; `@nabvy/auth`'s…
**Outputs:**
- **Events**: `account.deleted` v1 `{ userId }`; `account.standing-changed` v1 `{ userId }`;…
- **Internal views** (`nabvy_pipeline` only, no per-module roles yet):
- `account.v_profiles` (user_id, display_name, analytics_consent, design_partner, created_at).
- `account.v_channels` (user_id, kind, device_id, session_id, bound_at, revoked_at) — Telegram and push…
- `account.v_standing` (user_id, status, until, limits) — for `want-manager` and `alert-router`. Exempt from…
- **Functions** from `@nabvy/account`: `getProfile`, `updateProfile`, `createTelegramLinkCode`,…
**Tables:**
- All in the `account` Postgres schema.
- `user_profiles`: `user_id` (primary key), `display_name`, `analytics_consent`, `design_partner`,…
- `telegram_links`: `user_id` (primary key — one chat per user), `chat_id` (one active link per chat: a…
- `telegram_link_codes`: `code_hash` (primary key, `sha256` of the 8-char code, 64 hex chars — the plain code…
- `push_subscriptions`: `id` (primary key), `user_id` + `device_id` (unique), `session_id`, `endpoint`, `keys`…
- `deletion_requests`: `user_id` (primary key), `requested_at`, `purge_by`, `purged_at`.
- `api_keys`: `id` (primary key), `user_id`, `hashed_key`, `label`, `created_at`, `last_used_at`,…
- `standing`: `user_id` (primary key), `status` (`active | suspended | banned`), `until`, `limits` (jsonb),…

## apify-gateway
The only module that talks to Apify: it checks a run, reserves its cost, starts it, collects
**Inputs:**
- `submitRun(q, ApifyGatewaySubmitRunInput)` from `check-scheduler` and `details-queue`: the run shape, the…
- `watch(q, { publisher, usdGbpRate })`, one tick of the `apify-gateway-watch` task, every minute.
- Switches `apify-gateway`, `apify`, `pipeline` and `cost-meter`, through `@nabvy/switches`.
- No events consumed.
**Outputs:**
- **Events** (keyed by job ID, so each job is announced once):
- `apify-gateway.run-collected` v1 `{ jobId, apifyRunId, kind: search | details }`, key…
- `apify-gateway.run-settled` v1 `{ jobId }`, key `apify-gateway.run-settled:<jobId>`, once a paid run's final…
- **Internal views** (`nabvy_pipeline`; empty while the module is off; security_invoker):
- `apify_gateway.v_jobs`: id, kind, run_kind, status, tags, input, memory_mb, timeout_secs, reserve_usd,…
- `apify_gateway.v_run_summaries`: job_id, apify_run_id, run_kind, summary (the whole `RUN_SUMMARY`),…
- `apify_gateway.v_rows`: job_id, seq, record_type, listing_id, item (the row with every `seller` and…
- `apify_gateway.v_seller_presence`: job_id, seq, present (whether the row has a seller object; nothing about…
- **Restricted view** `apify_gateway.restricted_rows` (job_id, seq, item: whole rows, seller fields included),…
- **User-facing views:** none.
- **Functions** (`@nabvy/apify-gateway`): `submitRun`, `watch`, `readJobs(q, jobIds)`, `readSwitches`, and the…
**Tables:**
- Schema `apify_gateway`, created by `supabase/migrations` (the bootstrap) and extended by…
- `jobs`: `id` (identity), `kind` (`env_check | actor_info | run | collect`), `status` (`pending | running |…
- `items`: `(job_id, seq)` primary key, `item` (the row, whole).
- …

## audit-log
Records every human and admin action, one append-only row each: who, what, on what, before and
**Inputs:**
- `record(q, input)` calls from modules and admin procedures, with the transaction that performs the action…
- `recordRestrictedRead(q, { actorUserId, view, reason })` for developers' read sessions of a restricted view…
- No events consumed, no other module's views read. Depends on no module.
**Outputs:**
- Exported functions: `record`, `recordRestrictedRead`, `listEntries` (admin screen; newest first by `(at,…
- Internal view `audit_log.v_entries` (admin only): `id, actor_user_id, action, target, before, after, reason,…
- No restricted or user-facing views. No events.
- Contracts (`@nabvy/contracts/modules/audit-log`): `AuditLogRecordInput`, `AuditLogEntry`,…
**Tables:**
- `audit_log.entries`: `id` (uuid v7, made in code so writers need no select), `actor_user_id`, `action`,…
- | Role | Grant | Policy |
- | `nabvy_app` | insert | only rows whose `actor_user_id` is the `withUser` user |
- | `nabvy_pipeline` | insert | any row: the pipeline is trusted to name the real actor |
- | `nabvy_auth` | insert | only rows whose `actor_user_id` is an existing account (task 4.0b,…
- | anyone | no select, update, delete or truncate | triggers refuse update, delete and truncate even for the…

## auth
One atomic module (`docs/decisions.md`, "Atomic modules"), backlog tasks 4.0 and 4.0b (audited admin
**Inputs:**
- HTTP requests to `/api/auth/*`, through the route handlers apps/web mounts.
- Request headers passed to the session helpers by oRPC procedures.
- Admin actions (role changes, restrictions on `better_auth.user`: `banned`, `ban_expires`,…
- Configuration (`docs/secrets.md`): `BETTER_AUTH_SECRET`, `BETTER_AUTH_URL`, `ADMIN_EMAILS`,…
**Outputs:**
- | Export | For |
- | `createAuthRouteHandlers()` | apps/web: `{ GET, POST }` for `app/api/auth/[...all]/route.ts` |
- | `getSession(headers)` | the signed-in session and user, or `null` |
- | `requireActiveUser(headers)` | every procedure: signed in, verified, neither suspended nor banned |
- | `setRole({ actorUserId, userId, role, reason? }, auth?)` | admin procedures: change a role; one…
- | `restrictAccount({ actorUserId, userId, policy, until?, reason }, auth?)`, `liftRestriction({ actorUserId,…
- | `revokeSessions({ actorUserId, userId, reason? }, auth?)` | admin procedures: sign an account out…
- | `requireAdmin(headers)` | admin procedures: `requireActiveUser` plus the `admin` role |
- | `requireUser(headers)` | only what a restricted account may still do: see the notice, sign out, delete the…
- | `isAccountActive(db, userId)`, `assertAccountActive(db, userId)` | jobs and module functions acting for a…
- | `createAuth(options)`, `getAuth()`, `createAuthFromEnv()` | the Better Auth instance (explicit options, or…
- | `createRecordingMagicLinkSender()` | test double: records magic links instead of emailing them |
- | `AuthFailure`, `UnauthenticatedError` (401), `ForbiddenError` (403), `AccountRestrictedError` (403) |…
- | `UnknownAccountError` | an admin action named no account; nothing changed or recorded |
- | `accountRestrictedNotice(step, policy)`, `ACCOUNT_REVIEW_OFFER`, `POLICY_NAMES` | the notice and review…
- …

## cost-meter
Records the cost of every paid call in one ledger: each Apify run (reserved, then settled), each
**Inputs:**
- No events. Paying modules call `record()`, `recordModelCall()` and `settle()`, passing a `CostMeterContext`:…
**Outputs:**
- **Internal view** `cost_meter.v_costs` (row type `CostMeterCall`), granted to `nabvy_pipeline`, filtered to…
- **No restricted or user-facing views. No events.**
- **Functions** (`@nabvy/cost-meter`), each returning `Result<{ call, changed }, CostMeterError>`:
- `record(db, CostMeterRecordInput, ctx)`: an Apify run with its reservation, or a free eBay or CeX call…
- `recordModelCall(db, CostMeterModelCallInput, ctx)`: a returned model call, priced from the table and…
- `settle(db, CostMeterSettleInput, ctx)`: the provider's final cost replaces the reservation once. Same…
- `readCosts(db, { since, module? }, ctx)`: the ledger as `v_costs` shows it.
- `contextFor(db, usdGbpRate)`: a `CostMeterContext` with `state` read live from `state(db, 'cost-meter')`…
- Helpers: `unitsToMicros` (provider dollars to micros, rounded up), `toGbpMicros`, `modelCostMicros`.
**Tables:**
- `cost_meter.provider_calls`: one row per call. `id`; `module, provider, kind, ref_id` (the provider's ID:…

## detail-evidence
Keeps every detail version of a listing, keyed by its evidence hash, with how complete it is
**Inputs:**
- Event `apify-gateway.run-collected` v1 `{ jobId, apifyRunId, kind }` (`apify-gateway`), handled by…
- Views of `apify-gateway`: `v_jobs` (the job is collected), `v_run_summaries` (`RUN_SUMMARY.collectedAt`, the…
- View of `listing-ingest`: `v_listings` (the listing ID of each source listing ID).
- Switches `detail-evidence` and `pipeline`, through `@nabvy/switches` (fail closed).
**Outputs:**
- **Events**, payload `{ listingIds }` (1–500 listing UUIDs), built by `record` and published by the handler…
- `detail-evidence.changed` v1: listings whose current version this job changed (a first version, or a fresh…
- `detail-evidence.unresolved` v1: known listings whose fetch in this job could not identify the item. Key…
- **Internal views** (`nabvy_pipeline`; security_invoker; empty while off):
- `detail_evidence.v_current`: listing_id, source, source_listing_id, evidence_hash, first_seen_at,…
- `detail_evidence.v_text`: listing_id, evidence_hash, description_status, description, title, for every…
- `detail_evidence.v_outcomes`: listing_id (null for an ID listing-ingest never stored), source,…
- `detail_evidence.v_fingerprints`: listing_id, evidence_hash, fingerprint (SHA-256 of the current…
- **Restricted and user-facing views:** none (rule 5).
- **Functions** (`@nabvy/detail-evidence`): `record(q, { jobId })`, `erase(q, listingIds)`,…
**Tables:**
- Schema `detail_evidence`:
- `evidence`: one row per version. `id` (UUID v7), unique `(source, source_listing_id, evidence_hash)`;…
- `fetches`: one row per listing per job. `id`, unique `(source, source_listing_id, job_id)`; `listing_id`…

## incidents
One atomic module (`docs/decisions.md`, "Atomic modules"). A module session edits only this
**Inputs:**
- `record(db, input)`: called directly by the task wrapper once an event has exhausted its retries. Not an…
- `retry(db, incidentId)`: called by an admin action (through an oRPC procedure).
**Outputs:**
- Event `incidents.dead-lettered` (`{ incidentIds }`), built and returned by `record()`. This module has no…
- Internal view `incidents.v_open`: open (unresolved) incidents. No user-facing view.
**Owned tables:**
- Postgres schema `incidents`.
- `incidents`: one row per dead-lettered event, keyed by `(event_type, event_key)` (unique). The key is…
**Views:**
- `incidents.v_open` (internal; readers: `nabvy_app`, `nabvy_pipeline`): `id, event_type, event_key, payload,…
**Events:**
- `incidents.dead-lettered` v1: `{ incidentIds: Uuid[] }` (1–500). Key: the recorded incident's own…

## listing-ingest
Turns every collected card row into one listing identity and one observation per run, and spots
**Inputs:**
- Event `apify-gateway.run-collected` v1 `{ jobId, apifyRunId, kind }` (`apify-gateway`), handled by…
- Views of `apify-gateway`: `v_jobs` (the job is collected), `v_run_summaries` (`RUN_SUMMARY.collectedAt`, the…
- Switches `listing-ingest` and `pipeline`, through `@nabvy/switches` (fail closed).
- Later: `ebay-adapter.collected` and `gumtree-adapter.collected` (soft; neither module exists yet, and…
**Outputs:**
- **Events**, payload `{ listingIds }` (1–500 listing UUIDs), built by `ingest` and published by the handler…
- `listing-ingest.first-seen` v1: listings whose earliest observation belongs to the job. Key…
- `listing-ingest.card-changed` v1: listings whose observation in the job has a different card hash from the…
- **Internal views** (`nabvy_pipeline`; security_invoker; empty while off):
- `listing_ingest.v_listings`: id, source, source_listing_id, card_hash, price_minor, currency, money_kind,…
- `listing_ingest.v_sightings`: id, listing_id, job_id, seq, kind (`search | detail`), terms, centre_ids,…
- `listing_ingest.v_price_changes`: listing_id, sighting_id, kind, previous_minor, price_minor, currency,…
- `listing_ingest.v_city_pages_seen`: city_page_id, town_label, listings, first_seen_at, last_seen_at.
- `listing_ingest.v_fingerprints`: listing_id, fingerprint (SHA-256 of lower-cased, whitespace-collapsed…
- **Restricted and user-facing views:** none (rule 5).
- **Functions** (`@nabvy/listing-ingest`): `ingest(q, { jobId, kind })`, `erase(q, listingIds)`,…
**Tables:**
- Schema `listing_ingest`:
- `listings`: `id` (UUID v7), unique `(source, source_listing_id)`; the card values listed under `v_listings`,…
- `sightings`: `id`, `listing_id` (→ `listings`), unique `(listing_id, job_id, kind)`; the columns listed…

## marketing-consent
Keeps marketing consent, preferences and email suppressions, and gates every marketing send
**Inputs:**
- Forms: the preference centre (`setPreference`, `pauseAll`, `resumeAll`) and newsletter sign-up…
- Resend and PostHog bounce and complaint webhooks, normalised to `MarketingConsentSuppressionEvent` by a…
- Consumes `account.deleted` (module: `account`): purges this module's `marketing_consents` rows for the…
- Reads: `@nabvy/switches`' `isOn(q, 'marketing-consent')`; `@nabvy/account`'s `isActive(q, userId)`.
**Outputs:**
- **`canMarket(q, { userId, email, category })`**: whether a marketing message in `category` may go to…
- **Internal view** `marketing_consent.v_consents` (user_id, category, granted, until, source, at): every…
- **Functions** from `@nabvy/marketing-consent`: `canMarket`, `getPreferences`, `setPreference`, `pauseAll`,…
- Publishes no events of its own (module card, "Outputs": only `canMarket()`).
**Owned tables:**
- Postgres schema `marketing_consent`.
- `marketing_consents`: `user_id` + `category` (primary key). `category` is one of the four preference-centre…
- `email_suppressions`: `email_hash` (primary key, sha256 hex of the trimmed, lower-cased address — never the…
- `newsletter_subscribers`: `email_hash` (primary key, same hashing), `consent_source`, `at`,…
**Views:**
- `marketing_consent.v_consents` (user_id, category, granted, until, source, at): internal, granted to…
**Events:**
- None published (module card, "Outputs"). Consumes `account.deleted` (module: `account`); `marketing-consent`…

## product-catalogue
Holds the canonical parts and products (catalogue IDs) with their aliases, negative contexts and
**Inputs:**
- Pack data in `@nabvy/packs` (`gpu-pc`'s dictionary and its copy of `part-patterns.json`), read once by the…
- Admin edits: `addItem`, `addAlias`, `addNegativeContext`, `addCode`, each audited through…
- Consumes no events. Depends on `switches` (its own switch) and `audit-log` (every edit's audit row);…
**Outputs:**
- **Event** `product-catalogue.updated` v1, `{ catalogueIds }`, keyed `product-catalogue.updated:<ids>@<write…
- **Internal views** (`product_catalogue` schema, granted to `nabvy_pipeline` only; names reach users only…
- `v_items` — `catalogue_id, kind, family, variant, is_mobile, pack_id, name`
- `v_aliases` — `id, catalogue_id, alias, source`
- `v_negative_contexts` — `id, pattern, blocked_catalogue_id, source`
- **Functions** from `@nabvy/product-catalogue`: `resolve(q, text)`, `lookupByCode(q, kind, code)`, `addItem`,…
**Tables:**
- Postgres schema `product_catalogue`.
- `items` — one canonical part or product per row: `catalogue_id` (primary key, e.g.…
- `aliases` — text that resolves to a catalogue ID: `id`, `catalogue_id` (→ `items`), `alias`, `source`.…
- `negative_contexts` — a pattern that must never resolve to one catalogue ID: `id`, `pattern`,…
- `codes` — an EAN or CeX box ID for one item: `id`, `catalogue_id` (→ `items`), `kind` (`ean | cex_box`),…

## product-events
Records first-party product events through one `track()`, and forwards them to PostHog only when
**Inputs:**
- `track(db, userId, input, ctx)` calls from procedures (inside `withUser(userId)`) and pipeline tasks (inside…
- `@nabvy/account`'s `getProfile()` for consent (module card, "Inputs": `v_profiles`). A user with no profile…
- `@nabvy/switches`' `isOn`/`state('product-events')`, read by the caller and passed in as `ctx.state` (this…
**Outputs:**
- **Owned table:** `product_events.events`, monthly range-partitioned on `at` (`docs/contracts.md:185`).…
- **Internal view:** `product_events.v_events` (id, userId, event, properties, sessionId, at). Returns no rows…
- **Events:** none; this module publishes no domain events (module card, "Outputs": "none beyond its view").
- **Functions** from `@nabvy/product-events`: `track()`; `NoopProductEventsForwarder` and…
**Tables:**
- Postgres schema `product_events`.
- `events`: `id` (uuid, default `nabvy_core.uuidv7()`), `user_id`, `event`, `properties` (jsonb), `session_id`…

## quote-redaction
Masks identifying text (phone numbers, emails, social handles, links and the inward half of full
**Inputs:**
- Calls only. No events consumed, no views read. The SQL entry point `quote_redaction.quote` reads…
**Outputs:**
- No events, no views, no tables. Functions:
- | Function | Where | Returns |
- | `redact(text)` | `@nabvy/quote-redaction` | `QuoteRedactionResult`: `{ text, masked: { email, link,…
- | `quoteFor(q, text)` | `@nabvy/quote-redaction` | `redact(text)` while `isOn(q, 'quote-redaction')`…
- | `quote_redaction.redact_result(text)` | SQL | the same result as `jsonb` |
- | `quote_redaction.redact(text)` | SQL | the masked text only, for `app.` views (for example on listing…
- | `quote_redaction.quote(text)` | SQL | the masked text while the switch is on, otherwise `null` |
- | `quote_redaction.switch_on()` | SQL | whether the switch reads on (`switches.is_on('quote-redaction')`) |…
**Tables:**
- None. The Postgres schema `quote_redaction` holds functions only (the SQL test checks it owns no relations).

## route-health
Chooses each Facebook acquisition region's detail route, `graphql` or `page`, from recent detail
**Inputs:**
- `apify-gateway.run-collected` (module: `apify-gateway`), detail runs (`kind: 'details'`) only — search runs…
- `apify_gateway.v_jobs` (module: `apify-gateway`): the region an apify-gateway job was tagged with, in its…
- `apify_gateway.v_run_summaries` (module: `apify-gateway`): the collected job's `RUN_SUMMARY.detailRoute`.
- Switch `route-health`, through `@nabvy/switches`.
**Outputs:**
- **Event** `route-health.route-switched` v1 `{ regionId }`, key…
- **Internal view** `route_health.v_decisions` (region_id, route, reason, success_rate, attempts,…
- **Functions** (`@nabvy/route-health`): `recommendRoute(q, regionId)` — a region's current decision, for a…
**Owned tables:**
- Postgres schema `route_health`.
- `route_state`: one row per region (`region_id` primary key). `state` is the whole `RouteHealthState` the…
- `route_runs`: one row per detail run, keyed by `apify_run_id` (unique), so a retried scheduler tick cannot…

## scan-recognition
Identifies an item the user scanned: barcode first, then a CeX box lookup, otherwise one vision
**Inputs:**
- The scan form (`ScanRecognitionScanInput`): a client-generated `scanId`, the user, an optional EAN/UPC and…
- `confirm()` from the scan procedure inside `withUser`.
- Functions read: `@nabvy/switches` `state`, `isOn`; `@nabvy/account` `isActive`; `@nabvy/product-catalogue`…
- Soft: `cex-adapter`'s box lookup, passed in as `cexBoxLookup`; absent in the MVP.
- Event consumed: `account.deleted` v1 (`onAccountDeleted`).
- Caller-supplied config: `SCAN_SPEND_CAP_MINOR` (`spendCaps`) and `USD_GBP_RATE` (`exchangeRate`).
**Outputs:**
- **Event** `scan-recognition.identified` v1 `{ scanId }`, keyed…
- **Internal view** `scan_recognition.v_scans` (`nabvy_pipeline`; rows while not off): `id, user_id, method,…
- **User-facing view** `scan_recognition.v_user_scans` (`nabvy_app`, own rows by RLS, rows only while on):…
- **Functions**: `scan(q, input, deps, ctx)`, `confirm(q, input)`, `expirePhotos(q, now, limit)`,…
**Tables:**
- `scan_recognition.scan_events`: primary key `id` (the client's scan ID); `user_id`, `barcode`, `photo_ref`,…

## source-adapters
Provider adapters behind the `ProviderAdapter` contract (`docs/contracts.md`, `docs/providers.md`).

## spend-governor
Keeps every paid call inside the owner's budgets: it sums settled and reserved costs per budget,
**Inputs:**
- `cost_meter.v_costs` (cost-meter): every metered call, amounts in integer micros.
- `apify_gateway.v_jobs` (apify-gateway): the provisional cost of unsettled runs, runs queued or started but…
- `switches.state()` / `@nabvy/switches` `state()`: `spend-governor` and `cost-meter`.
- Event `apify-gateway.run-settled` (handler `onRunSettled`, one recompute per batch). A scheduled recompute…
**Outputs:**
- **Event** `spend-governor.budget-alerted` v1 `{ budget, level, periodStart }`, key…
- **Internal views** (`nabvy_pipeline`, security_invoker):
- `spend_governor.v_throttle` (row `SpendGovernorThrottle`): budget, level, reason, since, computed_at. One…
- `spend_governor.v_budgets` (row `SpendGovernorBudget`): name, provider, unit, period, limit_micros,…
- **User-facing views:** none.
- **Functions** (`@nabvy/spend-governor`): `recompute(q, { now, usdGbpRate })`, `readThrottle(q)` → `{ level,…
**Tables:**
- `spend_governor.budgets`: `name` (primary key), `provider` (null: all), `unit` (`USD | GBP | GB`), `period`…
- `spend_governor.throttle`: `budget` (primary key, references `budgets`), `level`, `since`, `period_start`,…

## switches
Holds the state of every module, provider, gate, feature flag and the global pipeline pause, so any one of…
**Inputs:**
- Admin actions only: an admin procedure checks the session and calls `set()` inside `withPipeline` (the admin…
**Outputs:**
- **Event** `switches.changed` v1, `{ names }`, keyed `switches.changed:<name>@<changed_at>`.
- **SQL functions** for views, SECURITY DEFINER, `EXECUTE` for `nabvy_app` and `nabvy_pipeline` only:
- `switches.state(name) → text`: `off | shadow | on`; an unknown name is `off`.
- `switches.is_on(name) → boolean`: true only for `on`.
- `switches.gate_allows(gate, user_id) → boolean`: the gate is `on`, and its allow-list is null (open to all)…
- **Internal view** `switches.v_state` (name, kind, state, allow_list, changed_at, changed_by):…
- **Functions** from `@nabvy/switches`: `state(q, name)`, `isOn(q, name)`, `gateAllows(q, gate, userId)`,…
**Tables:**
- `switches.switches`: `name` (primary key, kebab case, unique across kinds), `kind` (`module | provider |…

## usage-ledger
Keeps each user's credit balance and charges metered actions against it: a ledger of grants,
**Inputs:**
- `grant()` calls from `subscriptions` (top-up Checkout webhook, the monthly allowance, the taste) and…
- `chargeUsage()` and `reverseCharge()` calls from the module that runs a metered action, inside that action's…
- Event `account.deleted` (from `account`): `accountDeletedHandler` purges the user.
- Reads: `@nabvy/switches`' `state(q, 'usage-ledger')`; `@nabvy/account`'s `isActive(q, userId)` before a…
**Outputs:**
- **Functions** (`@nabvy/usage-ledger`), each on the caller's transaction:
- `grant(q, { userId, kind, credits, refId, cashMinor?, expiresAt? })` → `{ entry, changed }`. Kinds:…
- `grantAllowance(q, { userId, plan, refId, cashMinor, expiresAt }, policy)` and `grantTopup(q, { userId,…
- `chargeUsage(q, { userId, action, credits, refId, costGbpMicros? })` → `{ entry, changed, balance, events…
- `reverseCharge(q, { userId, refId })` → `{ entry, changed }`: a failed action's credits go back to exactly…
- `getBalance(q, userId)` → `UsageLedgerBalance` (credits in total and by rank, next expiry): the user's…
- `expireBuckets(q)`: the expiry sweep, one batch of up to 500 buckets (pipeline job).
- `purge(q, userIds)` and `accountDeletedHandler(deps)`.
- **Event** `usage-ledger.balance-low` v1 `{ userId, entryId }`, keyed…
- **Internal view** `usage_ledger.v_balances` (nabvy_pipeline only until per-module roles exist; rows only…
- **No user-facing view**: no module has created the `app` schema yet (the same call `account` made); the…
- Error codes `usage-ledger.off | insufficient | account_inactive | mismatch | not_found | reversed |…
**Tables:**
- All in the `usage_ledger` Postgres schema. Integers only: credits `integer`, cash integer pence, cost…
- `entries` (the card's `usage_ledger`): `id`, `user_id`, `kind` (`allowance | taste | referral | topup |…
- …

## waitlist
Takes waitlist sign-ups (email, postcode, wanted products, UTM) before launch and for cells not
**Inputs:**
- The waitlist form only (module card). No events consumed. Reads `@nabvy/switches`'s `state()` indirectly:…
**Outputs:**
- **No events** (module card, "Outputs": none beyond its view). `packages/contracts/src/modules/waitlist.ts`…
- **Internal view** `waitlist.v_waitlist` (id, email, postcode, wanted_products, utm_source, utm_medium,…
- **Functions** from `@nabvy/waitlist`: `submit(db, input, ctx)` → `Result<{ joined: true }, WaitlistError>`,…
- **SQL function** `waitlist.join(email, postcode, wanted_products, utm_source, utm_medium, utm_campaign,…
- **`WaitlistSender`**: an interface (`send(entry): Promise<void>`) and its only implementation,…
**Tables:**
- `waitlist.entries`: `id` (uuid pk), `email` (unique, lower-case, the dedupe key), `postcode` (nullable,…
