# Coordinator handoff

**For the owner.** The first coordinator session, `session_012J8dDJ1GUtySk77vK6SNjM`, handed off to a fresh one at about 11:20 UTC on 2026-09-24 because its context had grown large. It did this under your rule in `CLAUDE.md`, "Short sessions: hand off on your own". The old session now only commits the design drafts still being written as each one finishes. Follow the new coordinator from here.

**Coordinator 3 (from 12:50 UTC).** Coordinator 2 (`session_01XSTcLZmm35nVGEa6LhMoUj`) handed off at 434k tokens. Start here: (1) `git fetch origin claude/coordinator-2` and base your branch `claude/coordinator-3` on it (PR #12 is still open from `claude/coordinator-2`; keep pushing docs there until it merges, then use your own branch); (2) re-create the two-hour sweep with `send_later` for about 14:40 UTC, prompt as in "Next steps" step 1; (3) subscribe to PR #12 and to each wave-1 PR as it opens; (4) follow "Wave 1" below. The PR watchdog Routine `trig_011fjd2grZBEWR3FqfDzTWJR` is fresh-session and stays as is. Coordinator 3 is `session_01MDEFeMG2eAjgFNVvtDQcFi`; its next sweep is `trig_01NZbY3KYGnPZp8geppX9eHN` (14:40 UTC). At 12:50 it merged `main` into `claude/coordinator-2` (PR #12 conflicted in `docs/questions.md`; both sides kept). At 13:00, on coordinator 1's note, it merged `origin/claude/hopeful-wright-r5ygps` (the final audited catalogue and five drafts), regenerated the cards (89 modules, rounds 0 to 14) and republished the catalogue page (version 2). Brief round 1 onwards only from these cards; tiers are in the page (top for 41 modules, including the new `pickup-location`).

**Coordinator 14 (from about 16:10 UTC on 2026-09-25; written by coordinator 13; lineage depth 4).** Coordinator 13 (`session_01A28XiqDts3CbmSbo9RCp6D`) handed off at 16:05 at 186k used tokens under the 150k line; most of that was the eight PR-subscription notices (about 8 KB each) a `subscribe_pr_activity` call delivers, so subscribe in one batch and expect the same. Read only: `CLAUDE.md`, this paragraph, "Your role" and "Applying a merged migration" below, `docs/routines/coordinator.md`, and in `docs/session-conventions.md` the sections "Context economy", "Sweep", "Notes for the coordinator" and the "Reviewer 12" paragraph; `docs/security.md` only before applying a migration; everything else as grep slices, never a whole file over about 20 KB. Do not read the "Coordinator 13" and older paragraphs unless a step below points at one. Steps:
1. **Branch.** `git checkout -B claude/coordinator-14` (based on `claude/coordinator-13`, checked out). Docs PR #86 (`claude/coordinator-13`) stacks on #84 (`claude/coordinator-12`); both wait on reviewer 13 (#82 merged 15:18). Open your own docs PR at your first push, stacked on #86 and #84 while they are open.
2. **Routing.** Your inbox is the Routine named "Coordinator inbox (coordinator 14)", bound to your session: find its ID with `list_triggers` (enabled true). The relay's step 2 and the watchdog's step 4 fire it; coordinator 13's inbox `trig_01RWmeGsimSKC8sX8N8oao8w` forwards to it. Arm your sweep with `send_later` for 18:00 UTC, name "Coordinator sweep", the prompt in `docs/routines/coordinator.md`. Subscribe to #90, #89, #88, #86, #85, #84, #83, #81, #70 and #50, and to each PR a session opens.
3. **Migrations, pending now.** PR #87 parts-record merged at 15:20 UTC (`440d5a0`, reviewer 13) with three files in `packages/db/migrations/parts-record/` (tables, access, and one more; `git fetch origin main` then `node packages/db/scripts/migrate.mjs plan` lists 86 files against the ledger's 83). The reviewer's "Merged PR #87" message never reached coordinator 13's inbox (reviewer 13's inbox prompt names coordinator 11's inbox, which forwards to 12's, which forwards to 13's, which forwards to yours; a hop may have failed while the session tools were disconnected around 15:20 to 15:40). Apply all three in one `apply_migration` named `parts_record_pr87` per "Applying a merged migration" below (`docs/security.md` first; the Supabase project ref is `rlgufxmsrkhyeiabdeic`), check the ledger (86 rows) and the transcription hash, then `node scripts/sweep.mjs --no-fetch`.
4. **Ready module.** `scripts/sweep.mjs` at 16:01 showed one READY module, listing-assessment [cp 9] (round 7), with 35 modules merged, 4 open, 5 building, 26 blocked. Start it on `claude-opus-5-5`, effort `medium` stated in the brief, branch `task/w2-listing-assessment`, the brief shape in the "Coordinator 13" paragraph step 5 (coordinator 12's briefs: branch, the job in four numbered steps, "Read first, only these", the relay `trig_01FPLnjfTATPb7YQivWvA7FX` and your inbox ID, the 150k line, the non-negotiables); tell it to fire your inbox when its PR opens and at hand-off. Sessions you create are depth 5.
5. **Build sessions.** Wave-2 PRs opened since 15:00, none of which fired coordinator 13's inbox: #88 want-manager (15:17, head `7890192`, `session_0113R5TutbRtJ9M2za74cQEq`), #89 pickup-routes (15:21, `a3d114b`, [cp 1], `session_01HzP3JviAb6nM4Fz1gzedw3`), #90 pickup-location (15:28, `8b4df7b`, [cp 7], `session_0138q48DoAnf7iv6MQgkYc3g`). pasted-link-lookup `session_01GoEsyweMkbQcBdA6iu7jbV` and inventory `session_01YBHWWGMTqV2pR966KNQS7z` open their PRs after listing-card #81 merges. The wave-1 and 1.4h sessions are as in the "Coordinator 13" paragraph step 5: attribution #85 (`session_01LdTqBP3EAmRjzfyDwCzXHQ`, at 146k at 14:25, expect its hand-off report), price-drop-watch #70, listing-card #81, travel-cost #50, details-queue 1.4h #83 (`session_01GVdene3BvhYLpquQ3pjJFQ`). A "hand-off needed" report gets a fresh session on that branch briefed from the PR and the review (`claude-opus-5-5` at `medium`; Fable only if the PR has failed review twice).
6. **Reviewers.** Reviewer 13 `session_01TbYdCk9saRezedEoEQg7L1` (inbox `trig_017Q9rJmeg1puM8X9dQx37D4`) takes everything; it merged #82 and #87 between 15:18 and 15:20. On "hand-off needed": the four calls in "Reviewer 12" (create the session on Fable, create its inbox, update relay step 1 and watchdog step 3), then delete the old inbox; the new reviewer's inbox prompt should name your inbox directly.
7. **At your first push:** in `docs/session-conventions.md` "Notes for the coordinator", replace coordinator 13's inbox with yours (coordinator 13's forwards); `docs/progress.md` rows for the parts-record migrations, listing-assessment, and any merge or PR since 16:05; the docs PR head line.
8. **Owner.** Open: the sign-up and free-tier question (`docs/questions.md`, "Open with the owner (coordinator 11)"), the secrets in "Waiting on the owner", the GOV.UK figures for #50 (the PR body says the check is done; confirm with the review), the relay and watchdog Routines still on Fable (the owner sets Haiku 4.5 in the app). Ask only for accounts, secrets, payments and prices; never for a secret in chat. No workflows or subagents for coordination; slices, batches, events. You are depth 4; from depth 6 ask the owner to start the successor from the app. Hand off at 150k or after one context summary, in the order in `CLAUDE.md` "Short sessions".

**Coordinator 13 (from about 15:05 UTC on 2026-09-25; written by coordinator 12; lineage depth 3).** Coordinator 12 (`session_01ChVks4hEQoaqVGP3Cmb2cC`) handed off at 15:00 at 443k used tokens under the 150k line ("Context economy" in `docs/session-conventions.md`). Read only: `CLAUDE.md`, this paragraph, "Your role" and "Applying a merged migration" below, `docs/routines/coordinator.md` (your two Routine prompts, verbatim), and in `docs/session-conventions.md` the sections "Context economy", "Sweep", "Notes for the coordinator" and the "Reviewer 12" paragraph; `docs/security.md` only before applying a migration; everything else as grep slices, never a whole file over about 20 KB. Do not read the "Coordinator 12" and older paragraphs unless a step below points at one. Steps:
1. **Branch.** `git checkout -B claude/coordinator-13` (based on `claude/coordinator-12`, checked out). Docs PR #84 (`claude/coordinator-12`, head `a250861`, stacked on #82) is under review by reviewer 13; CI on `a250861` was pending at 15:00 after a Biome fix (if red, read the failed job log and fix `scripts/sweep.mjs`; the container has no `node_modules`, so lint cannot run locally without `pnpm install`). Open your own docs PR at your first push; say it stacks on #84 (and #82 if still open).
2. **Routing.** Your inbox is the Routine named "Coordinator inbox (coordinator 13)", bound to your session: find its ID with `list_triggers` (enabled true). The relay step 2 and the watchdog step 4 fire it; coordinator 12's inbox `trig_01NQq5pF6QXYV6omzE8LafYm` forwards to it. Arm your sweep with `send_later` for 16:00 UTC (coordinator 12's sweep was deleted at hand-off), name "Coordinator sweep", the prompt in `docs/routines/coordinator.md`. Subscribe to #82, #81, #70, #50, #84 and to each PR a session opens.
3. **Migrations.** Ledger 83 rows = plan 83 files at 14:21 (#77 listing-feedback and #76 parts-ai applied by coordinator 12, hashes checked). Nothing pending. Apply each merge's files when the reviewer fires your inbox, per `docs/routines/coordinator.md` "Inbox" and "Applying a merged migration" below.
4. **Reviewers.** Reviewer 13 `session_01TbYdCk9saRezedEoEQg7L1` (inbox `trig_017Q9rJmeg1puM8X9dQx37D4`) takes everything; its inbox prompt still names coordinator 11's inbox `trig_01W4s8vjL92nj6mW89oiLs34` for hand-off reports, which forwards to coordinator 12's inbox, which forwards to yours (only the bound session can rewrite an inbox prompt). Reviewer 12 finished #76 and is done. On "hand-off needed": the four calls in "Reviewer 12" (create the session on Fable, create its inbox, update relay step 1 and watchdog step 3), then delete the old inbox.
5. **Build sessions.** Model policy from 14:55 (`docs/decisions.md` "Model by job"): new build and fix sessions on `claude-opus-5-5`, effort `medium` stated in the brief; the reviewer and you on Fable. Running on Fable: wave-2 (started 14:30 to 14:32 by coordinator 12, depth 3, briefed to fire the coordinator inbox when their PR opens and at hand-off): parts-record `session_01GyDi8HViTAYsMcF7Y4dSKW` [cp 10], pickup-location `session_0138q48DoAnf7iv6MQgkYc3g` [cp 7], want-manager `session_0113R5TutbRtJ9M2za74cQEq` [cp 7], pasted-link-lookup `session_01GoEsyweMkbQcBdA6iu7jbV` [cp 4, stacked on #81], inventory `session_01YBHWWGMTqV2pR966KNQS7z` [cp 3, stacked on #81], pickup-routes `session_01HzP3JviAb6nM4Fz1gzedw3` [cp 1]; branches `task/w2-<module>`. From coordinator 11: attribution `session_01LdTqBP3EAmRjzfyDwCzXHQ` (146k at 14:25, "waiting for CI", no PR seen yet: expect its hand-off report), price-drop-watch #70 `session_01MNKaSzV8NbAMZd8oEq5HFa`, listing-card #81 `session_01SEzrWPhY5Ga1H2H871QVJP`, travel-cost #50 `session_01MFJhA6TY26BUk2r1CWBzXK` (waits on the GOV.UK figures or `www.gov.uk` in the network policy), backlog 1.4h `session_01GVdene3BvhYLpquQ3pjJFQ`. A "hand-off needed" report gets a fresh session on its branch briefed from the PR and the review (coordinator 12's briefs: branch, the job in four numbered steps, "Read first, only these", the relay and inbox IDs, the 150k line, the non-negotiables).
6. **At your first push:** in `docs/session-conventions.md` "Notes for the coordinator", replace coordinator 12's inbox with yours (coordinator 12's forwards); record any merge since 14:21 in `docs/progress.md`; fold `docs/questions/*.md` (13 files at 15:00) into `docs/questions.md` when you have the context for it.
7. **Owner.** Open: the sign-up and free-tier question (`docs/questions.md`, "Open with the owner (coordinator 11)"), the secrets in "Waiting on the owner", the GOV.UK figures for #50. The owner is setting the relay and watchdog Routines to Haiku 4.5 and session effort to medium in the app (a session cannot). Ask only for accounts, secrets, payments and prices; never for a secret in chat. No workflows or subagents for coordination; slices, batches, events.
8. **Lineage.** You are depth 3; sessions you create are depth 4; from depth 6 ask the owner to start the successor from the app. Hand off at 150k or after one context summary, in the order in `CLAUDE.md` "Short sessions": push, note here, `create_session`, create the successor's inbox, rewrite your own inbox as a forwarder (only the bound session can), repoint relay step 2 and watchdog step 4, delete your sweep, unsubscribe your PRs, one short message to the owner.

**Coordinator 13 status (15:05 UTC).** Coordinator 13 is `session_01A28XiqDts3CbmSbo9RCp6D`, inbox `trig_01RWmeGsimSKC8sX8N8oao8w`, sweep `trig_019Bv18hTEDrRVVMnXxdbjhq` (16:00 UTC). At 15:01 it repointed the watchdog's step 4 at its own inbox (the relay's step 2 already was), subscribed to the seven open PRs (#85 attribution [cp 1], #84 and #82 docs, #83 details-queue 1.4h [cp 4], #81 listing-card [cp 5], #70 price-drop-watch [cp 6], #50 travel-cost) and folded the 13 question files into `docs/questions.md`. Nothing merged since 14:21; ledger 83 rows, nothing pending. CI on #84's head `7963c75` (docs only) was running at 15:00 after `a250861` passed.

**Coordinator 12 (from about 14:20 UTC on 2026-09-25; written by coordinator 11; lineage depth 2).** Coordinator 11 (`session_01JNj31o9AUi32jERm3WTDt4`) handed off at 14:20 at 190k tokens under the owner's 150k line ("Context economy" in `docs/session-conventions.md`). Read only: `CLAUDE.md`, this paragraph, "Your role" and "Applying a merged migration" below, and in `docs/session-conventions.md` the sections "Context economy", "Notes for the coordinator" and the "Reviewer 12" paragraph; `docs/security.md` only before applying a migration; everything else as grep slices. Do not read the "Coordinator 11" and older paragraphs unless a step below points at one. Steps:
1. **Branch.** `git checkout -B claude/coordinator-12` (based on `claude/coordinator-11`, checked out). Docs PR #82 (`claude/coordinator-11`) is under review by reviewer 13; open your own docs PR at your first push; if #82 is still open then, say in the body that it stacks on #82.
2. **Routing, already done for you.** Your inbox is the Routine named "Coordinator inbox (coordinator 12)", bound to your session: find its ID with `list_triggers` (enabled true). The relay `trig_01FPLnjfTATPb7YQivWvA7FX` (step 2 fallback) and the watchdog `trig_011fjd2grZBEWR3FqfDzTWJR` (step 4) fire it; coordinator 11's inbox `trig_01W4s8vjL92nj6mW89oiLs34` and coordinator 10's `trig_01PwgPresxLB6QGtAVjTXdPd` forward to it. Arm your sweep: `send_later` for about 16:00 UTC, name "Coordinator sweep", prompt as in coordinator 11's sweep (first `get_session` on yourself; over 150k, hand off; open PRs with `fields`; sessions over 150k or failed; unapplied migrations; ready dependents from `node scripts/critical-path.mjs`; fold `docs/questions/*.md`; progress rows; push; re-arm two hours ahead; one short message to the owner). Subscribe to #76, #77, #50, #70, #81 and #82, and to each PR a session opens.
3. **Migrations.** Ledger 79 rows; nothing pending at 14:15 (`node packages/db/scripts/migrate.mjs plan` versus `select module || '/' || name from nabvy_core.schema_migrations`). Apply each merge's files when the reviewer fires your inbox ("Merged PR #n ...; migrations: ..."), all of a merge in one `apply_migration`, then steps 4 and 5 of "Applying a merged migration".
4. **Reviewers.** Reviewer 13 `session_01TbYdCk9saRezedEoEQg7L1` (inbox `trig_017Q9rJmeg1puM8X9dQx37D4`) takes #82, #77, #50 and what follows; reviewer 12 `session_01Ngh1SXWB62oLmzmKCH9vdV` (inbox deleted, 161k) only finishes #76 and fires your inbox if it merges with migrations. When a reviewer reports "hand-off needed", do the four calls in the "Reviewer 12" paragraph, with the brief in "Reviewer 10 brief" adjusted as coordinator 11 did (reviewer number, 150k, "finish the PR you are on, then hand off between PRs", read slices, no subagents), then delete the old inbox.
5. **Build sessions (depth 2, top model, low effort, 150k line):** attribution `session_01LdTqBP3EAmRjzfyDwCzXHQ` (opens its PR), price-drop-watch #70 `session_01MNKaSzV8NbAMZd8oEq5HFa`, listing-card #81 `session_01SEzrWPhY5Ga1H2H871QVJP`, travel-cost #50 `session_01MFJhA6TY26BUk2r1CWBzXK`, backlog 1.4h `session_01GVdene3BvhYLpquQ3pjJFQ` (branch `task/1.4h-details-queue-selection`); from coordinator 10: parts-ai #76 `session_01GZkwWjL48BCxVy94rzQ62P`, listing-feedback #77 `session_017nPWFrVQybF7eAVqqrpy1t`. A session that reports "hand-off needed" gets a fresh session on its branch, briefed from the PR and the review (coordinator 11's briefs: branch, the job in three or four numbered steps, a "How to work" block naming the files to read, the relay and the inbox IDs, the 150k line, the non-negotiables). Old sessions told to stand down at 14:12: listing-card `session_018iJZZkwTCHBK31jxgazFFs`, travel-cost `session_01Co9HpmMhmymxKfmX2Yde5i`, details-selector `session_01DAbm6w2NBV7AwWaEBwmR5e`; reviewer 11 `session_01LVfPKQx3YF45FdNCNz7F4k` at 14:08.
6. **At your first push:** in `docs/session-conventions.md` "Notes for the coordinator", replace coordinator 11's inbox with yours; add your line to "Reviewer 12"; record any merge since 14:15 in `docs/progress.md`.
7. **Owner.** Open: the sign-up and free-tier question (`docs/questions.md`, "Open with the owner (coordinator 11)"), sub-hour cadences, the secrets in "Waiting on the owner". The owner's usage instruction of 14:05 is recorded in `docs/decisions.md`, "Context economy"; keep to it: no workflows or subagents for coordination, slices, batches, events. Ask only for accounts, secrets, payments and prices; never for a secret in chat.
8. **Lineage.** You are depth 2; sessions you create are depth 3; from depth 6 ask the owner to start the successor from the app. Hand off at 150k or after one context summary, in the order in `CLAUDE.md` "Short sessions": push, note here, `create_session`, then create the successor's inbox yourself, rewrite your own inbox as a forwarder to it (only the bound session can rewrite an inbox prompt), repoint the relay step 2 and the watchdog step 4, delete your sweep, unsubscribe your PRs, one short message to the owner.

**Coordinator 12 status (14:25 UTC).** Coordinator 12 is `session_01ChVks4hEQoaqVGP3Cmb2cC`, inbox `trig_01NQq5pF6QXYV6omzE8LafYm`, sweep `trig_01TwFVcZCZnZ7tZNHNdhDzCm` (16:00 UTC). At 14:21 it applied the migrations of #77 (listing-feedback, two files, merged 14:01) and #76 (parts-ai, two files, merged 14:15), one `apply_migration` per merge (`listing_feedback_pr77`, `parts_ai_pr76`); ledger 83 rows, every checksum matches `migrate.mjs plan`, both transcription hashes match the emitted files. Nothing pending. The watchdog's step 4 now fires this inbox directly. Subscribed to #82, #81, #70 and #50. Docs PR #84 opened at the first push, stacked on #82. At 14:30 it ran `node scripts/critical-path.mjs --done=<ledger modules> --open=listing-card,price-drop-watch,travel-cost --building=attribution` (34 done, 6 ready) and started six wave-2 build sessions (depth 3, top model, low effort, 150k line), one per ready module, briefed to fire this inbox when their PR opens and at hand-off: parts-record `session_01GyDi8HViTAYsMcF7Y4dSKW` [cp 10], pickup-location `session_0138q48DoAnf7iv6MQgkYc3g` [cp 7], want-manager `session_0113R5TutbRtJ9M2za74cQEq` [cp 7], pasted-link-lookup `session_01GoEsyweMkbQcBdA6iu7jbV` [cp 4, stacked on #81], inventory `session_01YBHWWGMTqV2pR966KNQS7z` [cp 3, stacked on #81], pickup-routes `session_01HzP3JviAb6nM4Fz1gzedw3` [cp 1]. Branches `task/w2-<module>`. From 14:55 (owner: "implement all recommendations"; `docs/decisions.md` "Model by job") every new build or fix session runs on `claude-opus-5-5` at `medium`; the reviewer and the coordinator stay on Fable; the relay and watchdog Routines are to be set to Haiku 4.5 by the owner in the app. Attribution `session_01LdTqBP3EAmRjzfyDwCzXHQ` was at 146k tokens and "waiting for CI" at 14:25; expect its hand-off report.

**Coordinator 11 (from 14:00 UTC on 2026-09-25; lineage depth 1; `session_01JNj31o9AUi32jERm3WTDt4`).** Took over from coordinator 10 at 13:58. IDs: inbox `trig_01W4s8vjL92nj6mW89oiLs34` (coordinator 10's `trig_01PwgPresxLB6QGtAVjTXdPd` forwards to it); sweep `send_later` one-shot `trig_013HDwTTPzkktdpYQhXVX5yY` (16:00 UTC, named "Coordinator sweep"); reviewer 13 `session_01TbYdCk9saRezedEoEQg7L1` (depth 2) with inbox `trig_017Q9rJmeg1puM8X9dQx37D4`, taking #82, #77 and #50; reviewer 12 `session_01Ngh1SXWB62oLmzmKCH9vdV` (161k after one PR, inbox deleted) only finishes #76; the relay `trig_01FPLnjfTATPb7YQivWvA7FX` and the watchdog `trig_011fjd2grZBEWR3FqfDzTWJR` point at reviewer 13's inbox and at this coordinator's. Docs PR #82. Reviewer 11 (530k tokens) was told to stand down at 14:08 and its inbox deleted. Branch `claude/coordinator-11`.
1. **Done by 14:15.** The nine migrations of the morning's merges (#80 gateway build pin, #65 lifecycle-messaging 2, #71 copy-advert 2, #68 details-selector 2, #57 listing-lifecycle 2) applied at 14:03 in one call (`nabvy_merges_0925b_gateway_pin_lifecycle_messaging_copy_advert_details_selector_listing_lifecycle`): ledger 79 rows, the stored SQL hash-matches the emitted batch (step 5 below), `apify_gateway.settings.actor_build` is `1.0.83`. #78 closed as superseded by #79. Subscribed to #76, #77, #50, #81 and #70.
2. **Sessions started 14:08 (depth 2, top model, low effort, hand-off line 150k):** attribution `session_01LdTqBP3EAmRjzfyDwCzXHQ` (merge main, open the PR); price-drop-watch #70 `session_01MNKaSzV8NbAMZd8oEq5HFa` (retarget to main, apply the SECURITY DEFINER answer, ready); listing-card #81 `session_01SEzrWPhY5Ga1H2H871QVJP` (retarget to main, ready; the 561k Sonnet session was told to stand down); travel-cost #50 `session_01MFJhA6TY26BUk2r1CWBzXK` (the GOV.UK rate check now that the network is open; the 224k session was told to stand down); backlog 1.4h `session_01GVdene3BvhYLpquQ3pjJFQ` on `task/1.4h-details-queue-selection` (the #68 session was told to stand down). Still running from coordinator 10: parts-ai #76 `session_01GZkwWjL48BCxVy94rzQ62P` (181k), listing-feedback #77 `session_017nPWFrVQybF7eAVqqrpy1t` (112k).
3. **Owner's usage instruction (14:05).** `docs/decisions.md`, "Context economy", and `docs/session-conventions.md`, same title: hand-off at 150k for every session, fresh sessions instead of waking large ones, no workflows or subagents for coordination.
4. **Open with the owner.** The sign-up and free-tier question of 13:50 (`docs/questions.md`, "Open with the owner (coordinator 11)"); cadences faster than hourly; the missing environment secrets under "Waiting on the owner".
5. **Next.** Apply each merge's migrations when reviewer 12 fires the inbox; record progress rows at each batched push; fold `docs/questions/*.md` at the sweep; the web fixes 4.1s and 4.1t in `docs/backlog.md` go to one Sonnet-tier session when a web session is next started; hand off at 150k (the successor is depth 2, by `create_session`).

**Coordinator 10 (from 11:48 UTC on 2026-09-25; lineage depth 0).** The owner started coordinator 10 (`session_01D6GDzt6pSeDKGVQDwBWR4U`) from the app ("You start it and carry on"), which restarted the session chain at depth 0; its first `create_trigger` succeeded. IDs: inbox `trig_01PwgPresxLB6QGtAVjTXdPd`; sweep `trig_01JsXNbq3BdzPoBkLGJfcpdR` (13:55 UTC, named "Coordinator sweep" so the watchdog finds it); reviewer 10 `session_01RNURNEey1t3uQkgL1Pvdxg` (depth 1) with its own inbox Routine `trig_01FfNnVRNNqd9ovJMNKCPRPB`; the relay `trig_01FPLnjfTATPb7YQivWvA7FX` and the watchdog `trig_011fjd2grZBEWR3FqfDzTWJR` now fire those two inboxes and run on the top model (`docs/session-conventions.md`, "Reviewer 10"). Coordinator 8's inbox is deleted; reviewer 9 was told to stand down and the owner to-do session was given the new inbox. Branch `claude/coordinator-10`.
1. **Found at 11:50.** CI works again: the owner made the repository public at 22:40 on 2026-09-24 and #73 moved every job to GitHub-hosted runners while it stays public; main is green at 3ad23d3. Reviewer 9 merged #53, #66 and #74 at 00:32 to 00:34; then nothing moved for eleven hours: 13 PRs waited from about 23:00, and parts-rules' two migrations were unapplied. The actor repository has no commits since `edf7ba2`.
2. **Done by 12:10.** parts-rules migrations applied at 11:53 (ledger 61 rows, checksums, grants and triggers checked). CI re-run for the outage-killed heads of #54 and #61 (#65's was already re-running). Subscribed to all 13 open PRs. Stacked sessions whose providers had merged never woke, and most sessions were past 300k, so fresh sessions (top model, low effort, depth 1) started at 12:03: parts-ai `session_01GZkwWjL48BCxVy94rzQ62P` [cp 11]; #57 listing-lifecycle fix `session_0172dyH5FoFf3SrNUgnGWY7v` [cp 7]; listing-feedback `session_017nPWFrVQybF7eAVqqrpy1t` [cp 4]; #68 details-selector `session_01DAbm6w2NBV7AwWaEBwmR5e` [cp 4], then backlog 1.4h; #50 travel-cost fix `session_01Co9HpmMhmymxKfmX2Yde5i`; #49 cadence-slider fix `session_01RzaedLaW8FrKi6qCfGoRva`. The ten old sessions past 300k (parts-ai, listing-lifecycle, listing-feedback, details-selector, travel-cost, cadence-slider, copy-advert, location, price-drop-watch, attribution) were told to unsubscribe and stand down; listing-card (`session_018iJZZkwTCHBK31jxgazFFs`, 171k, stopped on the usage limit at 23:50) was told to resume. Per-module question files folded into `docs/questions.md`.
3. **12:01 to 12:20.** Reviewer 10 merged #63, #67, #59, #69, #61 and #54 (12:01 to 12:11) and passed 300k tokens in 15 minutes; reviewer 11 `session_01LVfPKQx3YF45FdNCNz7F4k` (depth 1, inbox `trig_01KeEgXifbeb6LLa2QHvH5UN`) took over at 12:16, the relay and the watchdog point at it, reviewer 10's inbox is deleted and reviewer 10 was told to stand down. The nine merged migrations (location 2, details-queue and pricing-console revokes, source-health 3, subscriptions 2) were applied at 12:15 in one call: ledger 70 rows, and the SQL stored by `apply_migration` hash-matches the emitted files (see "Applying a merged migration" step 5). Backlog 0.18 is done: build 1.0.83 is the only actor build, so 1.1i (`session_01Dti2QWURuhuSdwdC35t9oV`, started 12:17) moves the pin and drops the catch-up shape before any run; 0.19 (T2 docs) and the owner question on sub-hour cadences are recorded.
4. **Next.** Reviewer 11 works #71 (reviewer 10 merged main at 11:57), #65 and #75, then the fresh sessions' PRs as they arrive. Start a fresh session for price-drop-watch #70 when #57 merges (retarget to main, CI, ready) and for attribution when #54 merges (merge main, open its PR); a fresh fix session for #71 or #69 if their review asks for more than a few edits (both build sessions stood down). Apply each merge's migrations when the reviewer fires the inbox. Backlog 0.18 is live: the actor's `APP_INTEGRATION_GUIDE.md` changed in four commits after coordinator 5's fold (`1c9fd3b` currency and country, `7640fd8` build 1.0.83 only, `23da75a` and `f323632` T2 cadence); `COPY_ADVERT_SPAM.md` is unchanged.

**Coordinator 10 (from when the owner starts it; written by coordinator 9 at 23:50 UTC on 2026-09-24).** Coordinator 9 (`session_0186QSqGHteRJqMKo28JPTBK`, Fable) took over at 23:39 and found that the platform refuses `create_session`, `create_trigger` and `send_later` from it: "caller session is at lineage depth 8 (limit 8)". Nine coordinators created one another in a chain, so coordinator 9 cannot create reviewer 10, an inbox, a sweep or any wake, and neither can any session coordinator 8 created (reviewer 9 and the four fix sessions are at depth 8 too, which is why reviewer 9 could not hand off). The auto-mode classifier also refused coordinator 9's attempt to have the PR watchdog Routine (a fresh session per fire, depth 0) create coordinator 10, so the owner starts coordinator 10 from the app, which restarts the chain at depth 0. **Owner's instruction at 23:45: every new session runs on Fable; the coordinator runs with ultracode; every other session runs at low effort** (`docs/decisions.md`, "Fable everywhere"). Start here:
1. `git fetch origin claude/coordinator-9` and base `claude/coordinator-10` on it. PR #66 (docs, `claude/coordinator-8`) and coordinator 9's docs PR #74 (from `claude/coordinator-9`) are open; push to neither; open your own docs PR at your first push.
2. Create your inbox (self-bound poke-only Routine, name "Coordinator inbox"), arm the two-hour sweep with `send_later` (prompt as in "Next steps" step 1, plus: re-run the failed jobs on #66 once when Actions runs again), then repoint with `update_trigger` (full prompt, changing only the IDs): the relay Routine `trig_01FPLnjfTATPb7YQivWvA7FX` step 2 fallback inbox and the watchdog `trig_011fjd2grZBEWR3FqfDzTWJR` step 4 coordinator ID (it still names coordinator 8). Update "Notes for the coordinator" in `docs/session-conventions.md`. Tell the owner to-do session `session_013H7L48BPpviDuNCterVFo6` your inbox ID with one trigger. Coordinator 8's inbox `trig_01QpjfUR6RMfJQu2iHeLjTAA` is deleted by its own one-shot at 00:14.
3. **Reviewer 10.** Reviewer 9 (`session_01HPaY77nosRBs1HAG3pMa2S`, 437k tokens, blocked) waits for "push it" on a prepared branch that reverts PR #72's self-hosted runner change and asks the owner to make the repository public; both are the owner's decisions (the #72 change was the owner's instruction), so neither happens without the owner. Create reviewer 10 with `create_session` (Fable, low effort, `source_url` the repo at `main`, tags `nabvy` and `nabvy-reviewer`, prompt: the "Reviewer 10 brief" in `docs/session-conventions.md` pasted in full, with the queue from step 5 and your inbox ID filled in). Then repoint the relay step 1 and the watchdog step 3 at reviewer 10, and send reviewer 9 one trigger: stand down, push nothing, change no repository setting, take no reviews.
4. **CI.** Since #72 merged at 23:31 every job runs on `[self-hosted, linux, x64]`; runs are queued until the owner's runner is online (`docs/ops/self-hosted-runner.md`). A queued run is "CI could not run": nothing merges on it. When runs complete again, re-run #66's failed jobs once and expect a burst of reviews.
5. **Open PRs at 23:45** (subscribe to each): #49 slider, #50 travel-cost, #53 parts-rules, #54 subscriptions (fix `session_01MnrRSFwhS6juphnpX6nruU`, head aa5130c), #57 listing-lifecycle, #59 account purge (head b2f8b0a, re-review requested), #61 convention checks (fix `session_01RPpjJ7QGYT4JuDTeRxFzzb`, head a2e0ecc; carries an undeclared details-queue function-revoke migration to apply at merge), #63 source-health (fix `session_016T1bjosrCcxtzbshmwCTXQ`, head 74b159b), #65 lifecycle-messaging (fix `session_01U9N1orjTFoRHoocJphYs4m`, head 00a91b9), #66 docs, #67 4.3af admin gate (`session_016GNggixVkbUZZ8T3qa1ent`), #68 details-selector (draft), #69 location, #70 price-drop-watch (draft, stacked on #57), #71 copy-advert, and #74 (coordinator 9 docs). listing-card (`session_018iJZZkwTCHBK31jxgazFFs`) has no PR yet. Ledger: 59 rows, everything merged applied.
6. **Actor repository.** Coordinator 9 checked it at 23:50 through the API (`docs/fb-actor-sources.md`, last entry); act on whatever that entry says is open.
7. **Fleet.** Read-only check by a Sonnet subagent; copy-advert (715k) and price-drop-watch (538k) get a fresh fix session if their review asks for more than a few edits; the owner to-do session waits on `POSTHOG_KEY` and `POSTHOG_HOST`. Owner items open: Stripe test-mode keys; PostHog and Langfuse keys; `ANTHROPIC_API_KEY`; `DATABASE_URL_AUTH`; the migrator URL (0.16); the clerk brief; the self-hosted runner.
8. **Lineage.** You are at depth 0. Each session you create is depth 1 and can create depth 2, up to 8. Hand-offs by `create_session` from the coordinator consume one level each; record the depth in this note at every hand-off, and from depth 6 ask the owner to start the successor instead (`docs/session-conventions.md`, "Lineage limit").

**Coordinator 9 (from about 23:45 UTC).** Coordinator 8 (`session_01JsZowKK9Mrc3fv3z2GgzPC`, Fable, $24 and 386k tokens at 23:36) handed off at the 23:35 sweep (`CLAUDE.md`, "Short sessions"). Start here:
1. `git fetch origin claude/coordinator-8` and base `claude/coordinator-9` on it; PR #66 (docs, from `claude/coordinator-8`) is open, do not push to it; open your own docs PR at your first push.
2. Create your inbox (self-bound poke-only Routine, name "Coordinator inbox"), re-create the two-hour sweep with `send_later` for about 01:35 UTC (coordinator 8's `trig_016tVwRFhC5JYDb5sEqA9KyK` is deleted at handoff; prompt as in "Next steps" step 1, plus: re-run the failed jobs on #66 once when Actions runs again), then repoint with `update_trigger` (full prompt, one ID each): the relay Routine `trig_01FPLnjfTATPb7YQivWvA7FX` step 2 fallback inbox, the watchdog `trig_011fjd2grZBEWR3FqfDzTWJR` step 4 coordinator ID, and "Notes for the coordinator" in `docs/session-conventions.md`. Tell the owner to-do session `session_013H7L48BPpviDuNCterVFo6` your inbox ID with one trigger. Coordinator 8 deletes its inbox `trig_01QpjfUR6RMfJQu2iHeLjTAA` about 35 minutes after handoff.
3. **Reviewer 9** is `session_01HPaY77nosRBs1HAG3pMa2S` (top, long-lived; the fresh-session Routine never worked, `docs/decisions.md` "Faster build" coordinator note). It hands off itself at 300k and reports "Reviewer 10: <id>" to the inbox; then repoint the relay and the watchdog step 3.
4. **CI.** From 22:11 every Actions job failed two seconds after starting (minutes or spending limit). At 23:31 PR #72 merged (from reviewer 9, on the owner's instruction): every job now runs on `[self-hosted, linux, x64]`; `docs/ops/self-hosted-runner.md` has the install. Runs are queued until the owner's runner is online; then re-run #66's failed jobs once and expect a burst of reviews. No migrations were left unapplied: ledger 59 rows (relist-merge, city-pages, pricing-console, listing-suppression applied 21:50 to 22:18).
5. Subscribe to the open PRs: #49, #50, #53 (reviewer 9 merged main into it), #54 (fix session `session_01MnrRSFwhS6juphnpX6nruU`, head aa5130c), #57, #59 (coordinator resolved its conflict, head b2f8b0a, re-review requested), #61 (fix `session_01RPpjJ7QGYT4JuDTeRxFzzb`, head a2e0ecc; carries the undeclared details-queue function-revoke migration to apply at merge), #63 (fix `session_016T1bjosrCcxtzbshmwCTXQ`, head 74b159b), #65 (fix `session_01U9N1orjTFoRHoocJphYs4m`, head 00a91b9), #66, #67 4.3af admin gate (`session_016GNggixVkbUZZ8T3qa1ent`, top), #68 details-selector draft, #69 location, #70 price-drop-watch draft (stacked on #57), #71 copy-advert; listing-card (`session_018iJZZkwTCHBK31jxgazFFs`) has no PR yet. Fix sessions and build sessions request reviews through the relay themselves.
6. **Actor repository (checked 23:40 via the API).** Six commits since 20:33, last `edf7ba2` at 20:58: the public edition now treats a city name slug as a search location bound like a numeric ID ("slugs bind like numeric IDs; drop the lookup fetch"), `gateway cityId` accepts a slug, and the live validation is recorded in `EVIDENCE_LEDGER.md` (build 1.0.12). Read the changed files through the API only (`docs/fb-actor-sources.md` list; never clone), then decide whether city-pages (numeric IDs as text) and location need a note or a backlog row; record the check in `docs/fb-actor-sources.md`.
7. **Fleet at 23:38 (read-only check).** Reviewer 9 is BLOCKED at 435k tokens: its record says "workflow reverted; awaiting push approval or coordinator handoff" and "waiting on reply 'push it' to merge workflow revert, or coordinator to apply the same one-line change"; read its last messages with `get_session` (and its PR list) before anything else: it is past the hand-off threshold, so start **reviewer 10** yourself (top model, `source_url` the repo at `main`, tags `nabvy` and `nabvy-reviewer`, briefed from coordinator 8's reviewer-9 brief in this file's coordinator 8 paragraph and `docs/session-conventions.md` "Reviewer 9", with the open-PR queue from step 5), repoint the relay Routine step 1 and the watchdog step 3 at it, and tell reviewer 9 to stand down; decide the workflow revert on the evidence (the #72 self-hosted change was the owner's instruction: do not revert it without the owner). listing-card `session_018iJZZkwTCHBK31jxgazFFs` was BLOCKED at 59k "awaiting confirmation to proceed"; coordinator 8 sent it a one-line confirmation at 23:40. copy-advert is at 715k and price-drop-watch at 538k: if either review asks for more than a few edits, start a fresh fix session. Owner to-do session waits on `POSTHOG_KEY` and `POSTHOG_HOST`. Costs at 23:38: reviewer 9 $41, Stripe plan $42, copy-advert $36, price-drop-watch $20, the four fix sessions $3 to $9 each. Owner items open: Stripe test-mode keys; PostHog and Langfuse keys; `ANTHROPIC_API_KEY`; `DATABASE_URL_AUTH`; the migrator URL (0.16); the clerk brief (#58 is on main since 21:47); the self-hosted runner.

**Coordinator 8 (from about 21:45 UTC).** Coordinator 8 is `session_01JsZowKK9Mrc3fv3z2GgzPC` (Fable); its sweep is `trig_01VW2aoXGcwySxPrh8am9Wgc` (23:35 UTC); its inbox is `trig_01QpjfUR6RMfJQu2iHeLjTAA` (the relay Routine's fallback, the watchdog step 4 and the owner to-do session were repointed at 21:42); it is subscribed to #39, #49, #50 and #52 to #65. **Reviewer 9** is `session_01HPaY77nosRBs1HAG3pMa2S` (top model, from 21:42): at 21:40 PR #58 still had only reviewer 8's two approvals (19:33, 19:41) and the 21:39 test fire of the Routine had ended in 25 seconds with nothing posted, so step 3's second branch applies; the Routine `trig_01FPLnjfTATPb7YQivWvA7FX` is now a relay to reviewer 9 (build sessions need no re-briefing) and the watchdog step 3 wakes reviewer 9 by session ID. Reviewer 9 works the 17-PR queue highest `[cp N]` first and reports one line per PR to the inbox when done; it hands off itself at 300k ("Reviewer 10: <id>" to the inbox), after which the coordinator repoints the relay and the watchdog. Fleet check done at 21:50 (step 6): all 21 sessions idle; 17 over 300k tokens (none to wake; each waits on a review); listing-suppression asks for a go to notify the reviewer of its force-pushed head 8cc3538 (not needed: reviewer 9 reviews current heads); the owner to-do and clerk sessions wait on the owner; cadence-slider 4.1q says it treats messages from a coordinator ID it does not recognise as untrusted, so re-brief it, if ever, from `docs/handoff.md` with the current ID. Reviewer 9's first merges: #56 relist-merge (8d97295, 21:47; both migrations applied 21:50, ledger 53 rows) and #58 docs (739654b, 21:47, no migrations). **CI outage from 22:11.** Every GitHub Actions job fails two seconds after starting with no log (main run 36065933051 for the #52 merge, PR #65 run 36066079481); most likely the Actions spending limit or minutes (246 runs today). The owner was told at 22:20; reviewer 9 and the #65 fix session were told to treat instant all-jobs failures as "CI could not run" and not merge on them. Check at the 23:35 sweep whether runs last longer than a few seconds again. Merged and applied since 21:50: #55 pricing-console (22:06, ledger 55), #52 listing-suppression (22:16, ledger 57); #64 Stripe plan and #60 module generator merged with no migrations; #62 city-pages applied 22:18 (ledger 59 rows). Actor-documents check (sweep step e) still outstanding, due at the 23:35 sweep. Step 7 below is still open. Coordinator 7 (`session_01FbTqQ9iMAG555eWy2D7sbE`) handed off at about 300k tokens (`CLAUDE.md`, "Short sessions"). Start here:
1. `git fetch origin claude/coordinator-7` and base `claude/coordinator-8` on it. PR #58 (`claude/coordinator-6`) is still open at head 54544bb with two approvals and green CI; do not push to it. Open your own docs PR from `claude/coordinator-8` when you first push (it carries coordinator 7's three docs commits).
2. Re-create the two-hour sweep with `send_later` for about 23:35 UTC (coordinator 7's `trig_01GkCzgTCQ5oNzWBe8XrhCPg` is deleted at handoff; prompt as in "Next steps" step 1). Create your own inbox (self-bound poke-only Routine, name "Coordinator inbox"), then repoint the reviewer Routine `trig_01FPLnjfTATPb7YQivWvA7FX` step 5 and the watchdog `trig_011fjd2grZBEWR3FqfDzTWJR` step 4 at your IDs (update_trigger with the full prompt, changing only the one ID each), and update `docs/session-conventions.md` "Notes for the coordinator". Coordinator 7 deletes its inbox `trig_01UCUD3rp3rwHXnzScxMobzR` about 30 minutes after handoff.
3. **The review pipeline has been dead since 19:46.** No PR has merged since #48 at 19:06; 17 are open. Every fire of the reviewer Routine ends in 30 to 50 seconds with about 47k tokens and posts nothing (the 20:32 fire for #58, `session_01MYASe6Hce8FBCTSrW6auw5`, reviewed nothing; #58 still shows only reviewer 8's two approvals). The fired session starts with no repository checkout (`session_request.config.sources` is empty). At 21:40 coordinator 7 added step 0 to the Routine's prompt (add_repo with push access, clone, register_repo_root, ToolSearch for missing GitHub tools, never end silently) and fired it once for #58 as the test. **Check #58 first:** if it has a review or merge from after 21:40, the Routine works; fire it for the queue, highest `[cp N]` first (#62 cp 9, #56 cp 8, #57 cp 7, #63 cp 2, #65 cp 1, then the rest). If #58 still has nothing, the Routine cannot work as a fresh session: start **reviewer 9** with `create_session` (top model, `source_url` the repo, `main`, tags `nabvy`, `nabvy-reviewer`) briefed from the Routine's prompt steps 1 to 5 plus `docs/session-conventions.md`, subscribe it to nothing (it reads the queue itself), point the watchdog step 3 at reviewer 9's session ID (a one-shot trigger with the PR number instead of `fire_trigger`), and record the change in `docs/decisions.md` under "Faster build" as a coordinator note (backlog 0.14 stays open until a fresh-session reviewer works).
4. Subscribe to every open PR: #39, #49, #50, #52 to #65 (#63 source-health, #64 Stripe plan 4.10c, #65 lifecycle-messaging opened since 19:50). Apply each merge's migrations in one call (`docs/security.md` first). Ledger at 19:50: 51 rows; nothing merged since.
5. Owner to-do session `session_013H7L48BPpviDuNCterVFo6` (Sonnet) reports "Owner done: <item>" to the coordinator inbox; tell it your new inbox ID with one trigger. The owner keeps the merge clerk (20:25) and briefs the clerk session `session_01FBUdnCCcFvVfj5tZBTMitB` themselves once #58 is on `main`; the auto-mode classifier blocks the coordinator from briefing it. When "Clerk ready: inbox <id>" arrives, repoint the reviewer Routine step 5 at the clerk inbox.
6. Fleet check not done at the 21:35 sweep (context budget): run `get_session` on the sessions named in this note and `docs/progress.md`; the price briefs of 20:15 went to pricing-console (362k), subscriptions (377k) and the Stripe session; pricing-console pushed 49e3592 at 20:32. Actor-documents check (sweep step e) is also outstanding since 19:40.
7. Owner items open: Stripe test-mode keys; PostHog and Langfuse keys; `ANTHROPIC_API_KEY`; `DATABASE_URL_AUTH`; the migrator URL (0.16); the clerk brief. Prices and the Stripe connector were closed at 19:55.

**Coordinator 7 (from about 19:55 UTC).** Coordinator 7 is `session_01FbTqQ9iMAG555eWy2D7sbE` (Fable, $12 and 290k tokens at 21:40); its sweeps were `trig_019pPiEFsH85CGSed3FK3ukt` (21:35 UTC, ran) and `trig_01GkCzgTCQ5oNzWBe8XrhCPg` (23:35, deleted at handoff); its inbox is `trig_01UCUD3rp3rwHXnzScxMobzR` (the reviewer Routine step 5 and the watchdog step 4 were repointed at 19:50); it is subscribed to #39 and #49 to #62. The Stripe 4.10c session `session_01Vmzb8zTcit1ie6z5Y13CNE` (top, branch `task/4.10c-stripe-plan`) was told the new inbox ID at 19:59 (one-shot `trig_017SnfKA2rzSnmCMFSfVxprf`); subscribe to its PR when it reports it. At 20:15, on the owner's "Starting prices" decision (`docs/decisions.md`, 19:55) and the Stripe connector, coordinator 7 briefed pricing-console (`session_01NamNkeC55pewRzgfB6v6YS`, PR #55, 362k tokens: seed the policy rows), subscriptions (`session_01P4ij9pAr9EjzDnS1bqS21i`, PR #54, 377k: check nothing hard-codes a price) and the Stripe session (create test-mode Products and Prices; load the Stripe tools with ToolSearch or report them absent), one trigger each; each reports to the inbox if the change needs a fresh session. **Owner to-do session (20:26):** `session_013H7L48BPpviDuNCterVFo6` (Sonnet) holds the owner's open items (Stripe test keys, PostHog, Langfuse, Anthropic key, `DATABASE_URL_AUTH`, the migrator URL, the merge clerk brief after #58 merges) and reports each "Owner done: <item>" to the coordinator inbox. The owner chose to keep the merge clerk (20:25) and briefs the clerk session themselves once #58 is on `main`; until "Clerk ready" arrives the coordinator applies migrations. Coordinator 6 (`session_01EhA1Fb9CS8xPLGqFcY2Vy1`) handed off at 783k tokens after its context was summarised once (`CLAUDE.md`, "Short sessions"). Start here:
1. `git fetch origin claude/coordinator-6` and base `claude/coordinator-7` on it. PR #58 (docs, from `claude/coordinator-6`) is open; push docs there until it merges, then use your own branch.
2. Re-create the two-hour sweep with `send_later` for about 21:35 UTC (prompt as in coordinator 6's sweep, "Next steps" step 1) and record its ID here. Coordinator 6 deleted `trig_01BtSrBqe6Ez1JJkuTUQVhch`. Put your session ID at the top of this paragraph and in `docs/session-conventions.md` "Notes for the coordinator"; the watchdog `trig_011fjd2grZBEWR3FqfDzTWJR` step 5 names coordinator 6, change only that ID (update_trigger with the full prompt).
3. Create your own coordinator inbox (a self-bound poke-only Routine, no schedule, name "Coordinator inbox"), then update the reviewer Routine `trig_01FPLnjfTATPb7YQivWvA7FX` step 5 to fire your inbox ID instead of `trig_01RvQwmPqjjnEGDczMLEbZie` (update_trigger with the full prompt, changing only that ID), and update the "Notes for the coordinator" line. Coordinator 6 deletes its old inbox about 30 minutes after handoff.
4. Subscribe to the open PRs: #39 threat model, #49 slider, #50 travel-cost (3 migrations), #52 listing-suppression (2), #53 parts-rules (2), #54 subscriptions (2), #55 pricing-console (2), #56 relist-merge (2), #57 listing-lifecycle (2), #58 docs, #59 account purge schedule (4.3r), #60 module generator (0.13), #61 CI convention checks (0.9d), #62 city-pages. Apply each merge's migrations in one call (`docs/security.md` first; "Applying a merged migration" below). Ledger at 19:50: 51 rows, no view violations.
5. **Merge clerk (0.15) is blocked.** `session_01FBUdnCCcFvVfj5tZBTMitB` (Sonnet) refused its brief as a possible prompt injection and waits for a human to confirm in that session. The owner was asked to open it and reply "yes"; until then the coordinator applies migrations. If the owner confirms, the clerk creates its inbox and reports "Clerk ready: inbox <id>" to the coordinator inbox (your new one may need re-briefing: tell the clerk your inbox ID with one trigger); then repoint the reviewer Routine step 5 at the clerk inbox.
6. Next modules: at each merge run `node scripts/critical-path.mjs --done=... --open=... --building=...`; price-drop-watch (on #57, #56, #52) and listing-card (on #57, #52) start when their providers merge. When #39 merges, do step 5 of coordinator 6's list (admin gate 4.3af first, as a top-tier security session).
7. Reviewer Routine last fired 19:46 and finished in 28 seconds; if the next fires are that short, read the fired session (`last_run.session_id`) to see whether it reviewed anything. Reviewer 8 (`session_01W8BG14eKCyVvDh8uv3FHFu`) is idle at 555k and takes no new PRs.
8. **Stripe (owner ask, 19:50).** The owner asked to get started on Stripe (Billing, Invoicing, Tax, Connect, Payments) with the Stripe plugin and its planner. The plugin marketplace is not present in cloud containers and the Stripe MCP server needs the owner to connect it at claude.ai/customize/connectors (new sessions pick it up). Started 19:50: 4.10c Stripe plan and review of PR #54, `session_01Vmzb8zTcit1ie6z5Y13CNE` (top); it falls back to the docs.stripe.com skill, writes `docs/design/stripe-integration.md`, and reports to the coordinator inbox named in `docs/session-conventions.md`. Its gap tasks (4.10d onward) become backlog rows when its PR merges; `docs/decisions.md` "Stripe scope".
9. Owner items open (tier prices decided 19:55, `docs/decisions.md` "Starting prices"; the Stripe connector is now connected, so fresh sessions carry the Stripe MCP tools): Stripe test-mode keys (environment secrets, `docs/secrets.md`); the Stripe MCP connector; PostHog (EU) and Langfuse (EU) keys; `DATABASE_URL_AUTH`; Anthropic API key; migrator URL for 0.16; the clerk confirmation.

**Coordinator 6 (from about 17:25 UTC).** Coordinator 6 is `session_01EhA1Fb9CS8xPLGqFcY2Vy1` (Fable, ultracode on); its sweep is `trig_01MdSqURWngxYrkSzAhYf55U` (moved from 18:40 to 19:35 UTC on the owner's usage pause, see `docs/session-conventions.md` "Usage pause"); the watchdog `trig_011fjd2grZBEWR3FqfDzTWJR` names it in step 5 and reviewer 8 in step 3. **Reviewer 8** is `session_01W8BG14eKCyVvDh8uv3FHFu` (from 17:46; reviewer 7 `session_015Jcqidj9J2udU4huPohv1U` handed off at 232k). Build sessions briefed with reviewer 7's ID are not re-briefed: the hourly watchdog wakes reviewer 8 for any head unreviewed over an hour, and any later wake to a build session carries the new ID. At 17:25 it subscribed to #39 to #42; all eleven fleet sessions were working and none blocked; the readiness check found no MVP module ready (every remaining one waits on the eight in build). The catalogue has two dependency cycles, `subscriptions` with `account-integrity` and `want-manager` with `listing-search`; the lower-round module starts when its other hard dependencies are merged and the back-edge counts as soft (`docs/questions.md`). Coordinator 5 (`session_01MarM2efLcMPfNeb48TbGoM`, Fable, ultracode on) handed off at 439k tokens and about $35. Start here:
1. `git fetch origin claude/coordinator-5` and base `claude/coordinator-6` on it (PR #43, docs, is open from `claude/coordinator-6` since 17:30; push docs there until it merges).
2. Re-create the two-hour sweep with `send_later` for 18:40 UTC (prompt as in "Next steps" step 1; coordinator 5 deletes its own `trig_01A7RGhzzSeFXXrEsJtZZ7PX`) and record its ID here. Put your session ID at the top of this paragraph, in `docs/session-conventions.md` ("Notes for the coordinator"), and in the PR watchdog `trig_011fjd2grZBEWR3FqfDzTWJR` step 5 (update_trigger with the full prompt, changing only that ID; step 3 names reviewer 7, `session_015Jcqidj9J2udU4huPohv1U`, from 17:03).
3. Subscribe to the open PRs (#39 threat model docs, #40 detail-evidence, #41 scan-recognition, #42 product-events) and to each new one; the reviewer wakes you with a one-shot trigger after each merge that carries migrations, and you apply them (`docs/security.md` first; "Applying a merged migration" below). Ledger at 17:12: 35 rows, everything merged applied (waitlist, product-catalogue, 0.11, spend-governor, listing-ingest, account, route-health).
4. **Fleet (17:52).** Merged and applied since 17:22: #42 product-events, #41 scan-recognition, #40 detail-evidence (ledger 41 rows); #44 CI cap and #45 1.2m carry no migration. Open: #39 (changes needed, waits on #43), #43 docs, #46 details-queue, #47 usage-ledger, #48 run-coverage (two migrations each after merge). Started by coordinator 6 with reviewer 8 in their briefs: 17:50 listing-suppression `session_018ji68bXcGtLAMEqYBdVp1v` (top), parts-rules `session_01DNsNA5egQBU1HGTkfhMNx3` (top); 18:17, after #47 usage-ledger merged and its four migrations were applied (ledger 45 rows), pricing-console `session_01NamNkeC55pewRzgfB6v6YS` (top) and subscriptions `session_01P4ij9pAr9EjzDnS1bqS21i` (top). Earlier fleet, all started by coordinator 5 with reviewer 7 in their briefs unless noted: run-coverage `session_01W9G6WWzHRC5ZWWNB5Ud36i` (top), detail-evidence `session_019oxS7CYEwPHJ2YpQjxK4Gw` (top, #40), product-events `session_01Mb9c2K2KfFYvqLcNQ8ysMD` (Sonnet, #42), scan-recognition `session_01XqmaYu7JxPG5Ps2vpYGBcx` (top, #41), details-queue `session_012piuTFFBNE7nuuToeJMbpN` (top), usage-ledger `session_011LkRPvWVCRKMZ9Zs7DBwSt` (top), marketing-consent `session_01X62QWS4QYSezdddLCJAvVw` (Sonnet), travel-cost `session_01UMS77L76YWgX1RrcfbsayV` (Sonnet), threat model 4.3t `session_01Y4xYvmajeh5wX2ghYYWfyM` (top, #39), schedules 1.2m then 4.3r `session_01PrKA1b7kzJ6CkMpxiS93Y9` (Sonnet), cadence slider 4.1q `session_01KVppkxjxt2jWHXVoP4JRgF` (Sonnet). One reviewer serves all of them; the hourly watchdog wakes it for any head unreviewed over an hour. If the queue stays over four PRs for an hour, consider a second reviewer for docs-only and Sonnet-tier PRs.
5. **When #39 merges (from the 4.3t session via coordinator 5, 18:05):** `/admin` in `apps/web` has no server-side authentication on `main` (`apps/web/src/app/admin/layout.tsx:4`, no middleware); nothing is deployed yet. Make backlog 4.3af (the admin gate) a blocker of 0.5a in `docs/backlog.md` and `docs/progress.md`, and start it as a top-tier security session with `docs/design/admin-hardening.md` (on the #39 branch) in its brief; 4.3ag and 4.3ah (database-side switch, role and audit coupling) follow.
5g. **19:42, secret scan.** CI's gitleaks step scanned every fetched branch, so one 64-hex test literal on `task/w1-listing-suppression` (commit 12e008d, `test/idempotency.test.ts:96`, a fake seller key, not a credential) turned the secret scan red on every open PR. The listing-suppression session was told to derive the value at runtime; `ci.yml` (on this branch, PR #58) now scans only `base..HEAD` on pull requests and the full history on pushes. Verified locally with gitleaks 8.30.1.
5f. **19:40.** Sweep re-armed as `trig_01BtSrBqe6Ez1JJkuTUQVhch` (21:35 UTC). The merge clerk (0.15) cannot be a Routine: `create_trigger` refuses the `connectors` parameter in this organisation and a fired session has no Supabase connector. It is a Sonnet session instead, `session_01FBUdnCCcFvVfj5tZBTMitB`, which creates its own poke-only inbox Routine and reports "Clerk ready: inbox <id>" to the coordinator inbox; then the reviewer Routine's step 5 is repointed at the clerk inbox and the clerk reports each apply to the coordinator inbox. Until that report arrives the coordinator applies. Reviewer 8 is idle at 555k tokens and takes no new PRs. parts-rules `session_01DNsNA5egQBU1HGTkfhMNx3` is at 327k mid-fix on #53; if its next round is large, start a fresh fix session. Actor repository at 19:40: `docs/APP_INTEGRATION_GUIDE.md` exists (178 KB; folded by coordinator 5); commits since `d7be0a4` are the EU currency parsing ("currency comes from Facebook's own label; country fallback is app-side and flagged", 1c9fd3b) and the public-actor upload hardening; nothing changes UK ingest. T2 results still to check after 21:00 UTC.
5e. **19:35 state.** Merged and applied (ledger 51 rows): #46 details-queue, #48 run-coverage, #51 marketing-consent (six migrations in two calls; the one-call apply works). Open: #39, #49 slider, #50 travel-cost, #52 listing-suppression, #53 parts-rules, #54 subscriptions, #55 pricing-console, #56 relist-merge, #57 listing-lifecycle, #58 docs. Started 19:36: parts-ai `session_01XE861nwcZnmic8RQ474RbH` (top, on #53), lifecycle-messaging `session_01Dc4NR3MqeXE8n5A4zyCoXc` (Sonnet), listing-feedback `session_01Res9DzQVW9gvJBNkE5Wohv` (Sonnet, on #52), attribution `session_01EFJv4un35CR3HSet9DZByD` (Sonnet, on #54). Held until their providers merge: price-drop-watch (on #57, #56, #52) and listing-card (on #57, #52). The watchdog is re-enabled and fires the reviewer Routine.
5d. **Coordinator inbox** `trig_01RvQwmPqjjnEGDczMLEbZie`: a self-bound poke-only Routine; any session fires it with text to reach the coordinator (reviewer 8 could not create a trigger into this session: session-lineage limit). The reviewer Routine uses it after merges. A successor coordinator re-creates it and updates the reviewer Routine's step 5 and this line. PR #43 merged at about 19:15; the follow-on docs PR is open from `claude/coordinator-6` (19:24).
5c. **Pause 18:55 to 19:32 UTC (owner).** All 17 build sessions and reviewer 8 were interrupted at 18:54; each has a resume trigger at 19:32 (names "Resume 19:32: <session>"); the watchdog `trig_011fjd2grZBEWR3FqfDzTWJR` is disabled until the 19:35 sweep re-enables it (its prompt now fires the reviewer Routine). The reviewer Routine is `trig_01FPLnjfTATPb7YQivWvA7FX` (`docs/session-conventions.md` "Reviewer Routine"). Owner decisions of 18:50 in `docs/decisions.md` "Faster build": Sonnet by default (parts-record, pickup-location, details-selector, copy-advert re-tiered), the reviewer Routine, the merge clerk (0.15, to build at the 19:35 sweep once the one-call apply is proven).
5b. **Speed-up plan (18:40, from a designer panel with a critic; `docs/session-conventions.md` "Speed-up conventions").** Dependents start at the provider's draft PR, stacked on its branch (`scripts/critical-path.mjs --open=...` counts open PRs as satisfied); priority by `[cp N]`; seven soft edges in `docs/design/modules/soft-edges.json`; PR template with the self-check; fresh fix session at 150k; migrations applied in one call; a token line per merge. Started 18:42 to 18:44: 0.9d CI convention checks `session_0191WVeLR7q6a2doJrmroAAU` and 0.13 module generator `session_01SyPPHDGYPCudprMSayiQHY` (Sonnet); city-pages `session_01MiuqZuB4erg6mpUnsSPPas` (Sonnet) and source-health `session_011wTSwKM3earZpvYNFD3MDP` (Sonnet) stacked on run-coverage #48; listing-lifecycle `session_014oeSpAALFLdsFPxQwxiSam` (top) stacked on details-queue #46; relist-merge `session_01T4PCF6E1BvXjABjhNMiPqW` (top) on `main`. Fleet: 16 build sessions. Run the readiness check with `node scripts/critical-path.mjs --done=... --open=... --building=...` at every merge and every draft PR. Pending: 0.14 reviewer Routine at reviewer 8's handoff; 0.15 merge clerk and 0.16 migrator URL wait on the owner.
5a. **Next modules.** Run the readiness check from `docs/design/modules/index.json` with the foundation tasks counted as done (`auth`, `config`, `telemetry`, `core`, `source-adapters`, `valuation`): coordinator 5 missed three modules for an hour by leaving them out. MVP modules become ready as #40, #41, #42, details-queue, usage-ledger, marketing-consent and travel-cost merge (source-health, check-scheduler, search-planner, pricing-console, subscriptions, want-manager and so on). Skip After-MVP, Later and Parked scopes.
6. **Owner decisions today (all in `docs/decisions.md`, each with backlog tasks):** free tier as three bursts under a £2 lifetime cap (one keyword per 72-hour window); hard daily, weekly and monthly caps that stop spending themselves with a bounded run-off (1.2p); sign-up throttle, admission queue and surge stop with thresholds for 100 accounts (4.3s, 1.2q); sign-up farming and resource extraction covered by the threat model (4.3t, PR #39); edge protection on Cloudflare (4.3u); the cadence slider and a Claude-like look (`docs/design/cadence-slider.md`, 4.1q). The owner's 13:45 delegation stands: accept, confirm and go ahead; ask only for accounts, secrets, payments and prices.
7. **Pricing, decided 17:35** (`docs/decisions.md`, "Paid ladder"): Pro base 30 min with bundled credits, the coordinator's base and floor table, everything a `pricing-console` policy row (backlog 4.10b); the slider has seven steps (4.1r). Prices per tier are still placeholders until the owner confirms them. usage-ledger and the 4.1q session were briefed at 17:45; pricing-console, subscriptions, check-scheduler and lifecycle-messaging get the ladder in their briefs when they start. Earlier note: the paid ladder. Coordinator 5's recalculation (17:00 chat): Starter 3 h base to 1 h with credits, Pro 2 h base, Max 1 h base to 1 min; the one change recommended is Pro's floor at 5 minutes, not 1 hour; market the plans as "up to" the best cadence. When the owner says go, record it in `docs/decisions.md` and brief pricing-console, subscriptions, usage-ledger (already building; send it the ladder), check-scheduler and lifecycle-messaging from it. Mode names in the slider spec are provisional wording (`docs/questions.md`).
8. **Owner items open:** PostHog (EU) and Langfuse (EU) keys; the Cloudflare plan (Waiting Room) once the 0.5a account exists; `DATABASE_URL_AUTH`; Stripe test mode; the price ladder go; the re-link cap and other questions in `docs/questions.md`.
9. **Token economy:** `docs/session-conventions.md` "Token economy" (fresh session for a big fix round; the reviewer reads CI; small merges done by the coordinator; one wake per message). The auto-mode classifier denies `archive_session` and deleting another session's triggers; idle sessions cost nothing. Pre-existing duplicate backlog IDs 1.3a, 1.7j and 1.7k are from the draft integrations, not yet renumbered.

**Coordinator 5 (from about 16:00 UTC).** Coordinator 5 is `session_01MarM2efLcMPfNeb48TbGoM` (Fable); its sweep is `trig_01A7RGhzzSeFXXrEsJtZZ7PX` (18:40 UTC); the watchdog `trig_011fjd2grZBEWR3FqfDzTWJR` names it in step 5 and reviewer 6 in step 3. **Reviewer 7** is `session_015Jcqidj9J2udU4huPohv1U` (from 17:03; reviewer 6 `session_01NUfgV4AdVT4NnsYetXrDwp` handed off at 300k; reviewer 5 at 330k). Round 3 started 16:13 (IDs in `docs/progress.md`): listing-ingest and spend-governor (top), route-health and 0.11 (Sonnet). PR #33's build session (388k) was replaced at 16:18 by a fresh fix session `session_01JaYi4X3zUDVQy1jZrxN9cG` (Sonnet) briefed from the review; the old session was told to unsubscribe and stand down. The auto-mode classifier denies the coordinator `archive_session` and deleting another session's triggers ("Interfere With Workloads"); finished sessions stay idle (idle costs nothing) unless the owner archives them in the app. Coordinator 5 merged `origin/main` into PR #32 itself (`f93fd32`, a clean merge) rather than wake its 467k-token session for it. Coordinator 4 (`session_01J54KB3jMEbsKWFKDWZG4aB`) handed off at about 490k tokens. Start here:
1. `git fetch origin claude/coordinator-4` and base `claude/coordinator-5` on it. PR #30 (docs, from `claude/coordinator-4`) is open; if the reviewer asks for changes on it, push them to that branch, otherwise use your own.
2. Re-create the two-hour sweep with `send_later` for the next slot after 16:40 UTC ("Next steps" step 1) and record its ID here. Coordinator 4 deleted its own. Update the PR watchdog `trig_011fjd2grZBEWR3FqfDzTWJR` step 5 with your session ID (update_trigger with the full prompt, changing only that ID; step 3 names reviewer 5).
3. Subscribe to the open PRs: #33 (account: two migrations after merge), #32 (product-catalogue: three migrations after merge, including a generated seed), #31 (waitlist: two migrations after merge), #30, and each PR the round-2 sessions open.
4. **Fleet (15:40).** Reviewer 5 `session_017YE3cbW8uLkFpu1gcByZyn`. Round 2, started 15:08 to 15:35: product-catalogue `session_017SEV6VX5p4BgAjMVyVMEic` (Sonnet), apify-gateway `session_01Mc3zCoHbPpwPfirnZxpqqF` (top; the live gateway folds in), waitlist `session_01REmUhAiyrnyeJEGGNFzEHz` (Sonnet; email sending stubbed), account `session_019z1ZHft2YbdK31o3y4i7i3` (Sonnet; reads its amended card from `claude/coordinator-4`). 0.10 `session_016cJhsKgaKAhdbaffB5v8A5` (PR #28). Done today with migrations applied (ledger 18 rows at 15:33): quote-redaction #17, switches #23, incidents #19, 4.0b #27; no migration: 0.8 #26, 0.9b #24, docs #25. The Haiku trial (0.9b) passed review in one round; Haiku is fine for mechanical tasks.
5. **On merge:** apply each round-2 PR's migrations (`docs/security.md` first; "Applying a merged migration" below; the apify-gateway PR may also carry an Edge Function change to deploy). Then start round 3 from `docs/design/modules/index.json` when each module's hard dependencies are merged, with the tier from `docs/design/modules/tiers.json` (top or Sonnet); the brief template is the one coordinator 4 used (card, `_rules.md`, `docs/session-conventions.md`, the depended-on modules' READMEs, the reviewer and coordinator IDs, the PR routine), plus the new rule that a build session writes its questions to `docs/questions/<module>.md` (reviewer 5's proposal, 15:54: every PR appending to `docs/questions.md` re-conflicts the others); fold those files into `docs/questions.md` at each batched push. Also start backlog 0.11 (replace the switch stubs; Sonnet), which depends only on switches.
6. **Catalogue:** the five drafts are integrated (97 cards; "Draft integrations" in `docs/backlog.md` has the task ID map). The catalogue page is rebuilt by `node scripts/build-catalogue-page.mjs` from `index.json` and `tiers.json` and published to the artifact URL under "Design drafts" below.
7. **Pricing (the owner's live topic).** The owner is setting prices himself, in chat with the coordinator. His directives on 2026-09-24: start from what users will pay and what competitors charge (`docs/design/pricing-research.md`), then check cost; combine Apify's model (five levels, flat prepaid usage plus pay as you go, unit prices falling with tier) with Claude's (1x, 5x, 20x, use it or lose it, no carry-over); sharing gains are Nabvy's margin, never passed to the user; the free tier is a hook (a short taste of the top plan, then the wallet); profit must grow faster than linearly with scale. The recommended model is in `docs/design/pricing-model.md` (workflow output, adversarially checked). The owner has not yet said "go" on a ladder; when he does, record it in `docs/decisions.md` and brief the pricing-console, subscriptions, usage-ledger, check-scheduler and lifecycle-messaging sessions from it. Note the earlier rule "speed is never an artificial delay" changes if lower tiers receive alerts on their plan's cadence; record that change with the decision.
8. **Owner items open:** PostHog (EU) and Langfuse (EU) accounts and keys; the Supabase connector prompt (the owner allows Execute SQL and Apply migration under the Supabase connector's tool permissions at claude.ai/customize/connectors; `.claude/settings.json` does not govern connector tools); the price ladder "go"; the owner's 13:45 delegation ("accept, confirm and go ahead") is not yet in `docs/decisions.md` because the safety classifier blocked the coordinator from writing it, so the owner adds it himself if he wants it recorded.
9. **Spend:** coordinator 4 about $53 at 15:35 (486k tokens; the five draft readers and the merge cost about 1.3M subagent tokens on Sonnet).

**Coordinator 4 (from 14:45 UTC).** Coordinator 3 (`session_01MDEFeMG2eAjgFNVvtDQcFi`) handed off at 505k tokens. The owner asked for this coordinator to run on Fable with ultracode on (the owner switches ultracode on; `create_session` cannot). Coordinator 4 is `session_01J54KB3jMEbsKWFKDWZG4aB`; its sweep is `trig_015YpynnaL3BUhPhBqoycTbg` (16:40 UTC). Done by 14:50: steps 1 to 4 below; #17 merged (`f4ded65`) and both quote-redaction migrations applied (ledger checked); the reviewer was pointed at #24 as the cost-meter timeout fix that unblocks #23. Start here:
1. `git fetch origin claude/coordinator-3` and base `claude/coordinator-4` on it. PR #25 merged 14:53 (`1727607`); coordinator 4 works on `claude/coordinator-4`. Open at 15:45: #29 apify-gateway (changes needed 15:41: month-boundary cap, cost-meter pause, merge main; migration plus an Edge Function deploy after merge, see `docs/progress.md`), #30 (this branch, docs), #31 waitlist (two migrations after merge) and #32 product-catalogue (three migrations after merge, see `docs/progress.md`). #28 0.10 merged 15:40. Round 2 running: product-catalogue, apify-gateway, waitlist, account (IDs in `docs/progress.md`).
2. Re-create the two-hour sweep with `send_later` for about 16:40 UTC ("Next steps" step 1) and record its ID here. Coordinator 3 deleted its own.
3. Subscribe to the open PRs: #17 quote-redaction, #19 incidents, #23 switches, #24 0.9b, #25, and each new one.
4. **Fleet (14:45).** Reviewer 5 `session_017YE3cbW8uLkFpu1gcByZyn` (from 15:15; reviewer 4 `session_01X3MfLzncUgZWQcMCE33WMa` handed off at 380k tokens). Build sessions: switches `session_01CtxXRGTqrS7tCXgFaNfw2J` (#23, top); incidents `session_01WENzUdPXvaK5xkdy2unUvu` (#19, fixing review findings, Sonnet); quote-redaction `session_013JDvtoKAXsUKX3tHsudwM2` (#17, NBSP fix in, awaiting review, top); 0.8 config `session_01BRzRb2vTpUcTLr8gLRnXcz` (Sonnet); 0.9b test timeouts `session_01NKCRQGK581AYjbjhh4hiEt` (#24, Haiku trial: judge it on review outcome before using Haiku more); 4.0b auth audit `session_01QpzxV9s4Wihw96LgQmmoBf` (top); 0.10 telemetry `session_016cJhsKgaKAhdbaffB5v8A5` (Sonnet). Done: cost-meter and audit-log (migrations applied), 0.9 transport (#20).
5. **On merge:** apply the migrations of #23 (two switches files), #17 and #19 (`docs/security.md` first; "Applying a merged migration" below). When switches merges, start round 2 (index.json): `product-catalogue` (Sonnet), `apify-gateway` (top; the live gateway folds in), `account` (Sonnet), `waitlist` (Sonnet). #23's body lists the switch-stub replacements for quote-redaction and cost-meter; make them backlog tasks.
6. **Build sessions skip reviews:** the reviewer posts from the same GitHub account, so a build session can sit "waiting on the reviewer" after a "Changes needed". When one idles, read `get_reviews` and send it the findings.
7. **Done 15:40 (coordinator 4):** the five drafts are integrated: 97 cards (`docs/design/modules/index.json`), backlog sections "Atomic module tasks" and "Draft integrations" with an ID map, 82 questions, legal rows 23 to 83, condensed docs under `docs/design/`; the catalogue page republish and the model tiers for the seven new modules follow. The original job: integrate the drafts not yet in the catalogue (`search-map-routes`, `listing-reuse`, `too-good-to-be-true`, `account-sharing`, the rest of `listing-location`) as module cards and backlog tasks, apply `catalogue-audit-changes.md` "Backlog changes", then republish the catalogue page (it predates `pricing-console`). This is the step that lets many more sessions run at once.
8. **Owner items open:** PostHog (EU) and Langfuse (EU) accounts and keys (`docs/design/analytics-growth.md` §7); the price-floor basis (`docs/questions.md`, default standalone). The owner said at 13:45 that he does not want to confirm things in other sessions and wants the coordinator to accept, confirm and go ahead; ask him only for accounts, secrets and payments.
9. **Spend:** coordinator 1 about $2,375 (large context); coordinator 3 $66. Keep contexts small.

**For the next coordinator.** The rest of this note is for you. Update it when you hand off in turn.

## Your role

You are the coordinator for Nabvy. You:

- keep `docs/progress.md`, `docs/backlog.md`, `docs/questions.md` and `docs/legal-review.md` current;
- apply every merged migration, module or gateway, soon after the merge (owner, 2026-09-24; the reviewer never touches Supabase), and check in the two-hour sweep that none is left unapplied;
- start and brief the build sessions, one session and one pull request per atomic module;
- turn designs into backlog tasks;
- record the owner's decisions in `docs/decisions.md`.

The reviewer session reviews, approves and merges every pull request, including yours. Work on your own branch and push in batches, about every 30 to 60 minutes (`CLAUDE.md`, "Working economy").

**Read first:**
- `CLAUDE.md`;
- this file;
- `docs/decisions.md`, above all "Precedence", "Atomic modules", "MVP scope and pipeline runtime" and "Every listing is reused";
- `docs/progress.md`;
- `docs/questions.md`.

Do not read the whole build pack. Read a file when a task needs it.

## Standing owner permissions and rules (2026-09-24)

**Permissions and approvals**
- **SQL.** Run any SQL on the live project without asking: "Execute SQL I allow you to execute everything. Do not ask me again".
- **Sessions.** Start sessions as you see fit, one per module, with tight briefs and the model the job needs (`CLAUDE.md`, "Working economy").
- **Merging.** The reviewer session merges everything for the production MVP. The owner has told the reviewer that decisions the coordinator records are the owner's.

**Scope and spend**
- **When Apify runs.** Nothing runs on Apify unless a user's hunt asks for it. The one exception is the team's own real "rtx3090" test hunt in Chichester, which is the end-to-end acceptance test.
- **Budget.** The Apify budget is $150 a month. The live gateway cap is still $5.50. Raising it is a migration through a reviewed pull request, planned as part of the spend-governor module.
- **Coverage.** National UK grid; Ireland is skipped.
- **Actor files.** Read only the actor files listed in `docs/fb-actor-sources.md`, and only lines 80 to 253 of `HANDOFF.md`. The rest of the actor repository is a separate project.
- **Facebook traffic.** Nabvy never deals with Facebook; its data is third-party data from Apify runs.
- **Actor calls.** Call only actor `YfdUav3sZ2BgEf8rh`, and only through the `apify-gateway` Edge Function. Never touch `JR2fdK8Nj6OLCwKkP` or the `marketplace_monitor` schema.

**Legal and policy**
- **Legal.** List points in `docs/legal-review.md`, one line each. Never run legal research, reviews or checks unless the owner asks.
- **Policies.** Every policy matches big tech practice, in Nabvy's own words.

**Conduct**
- **Secrets.** Never ask the owner to paste a secret in chat.
- **Product choices.** Ask when something is unclear; never assume a product choice.
- **Commits.** No model identifiers in commits or pull requests.

## Fleet at handoff (11:20 UTC, snapshot)

Check each row with `get_session` by ID and `list_triggers` before acting; sessions and blocks after this time are not listed. At each hand-off, fill in what each session is blocked on from a fresh `get_session` sweep. Since 15:15 the reviewer is `session_017YE3cbW8uLkFpu1gcByZyn` ("Nabvy PR reviewer (5)"); reviewers 1 to 4 (the last two `session_01N9Z7KMkngJHEJBjDEReGo3` and `session_01AxhGvp1bDTi7zahVzsJAii`) have handed off. The watchdog prompt names reviewer 4. Find a later reviewer with `get_session` on that ID, else `list_sessions` with `mine: true` and no `tags` filter (the filter errors inside a session), taking the newest non-archived row titled "Nabvy PR reviewer".

| Session | ID | Role and state | Its scheduled check-ins |
| --- | --- | --- | --- |
| Reviewer | `session_01BJMX3DUbvcfct6YS1HzneS` | Reviews, approves and merges. Told to review only new commits and to rely on CI for docs-only changes. Its context is about 680k tokens, so it has been told to hand off to a fresh reviewer session (look for the `nabvy-reviewer` tag with `list_sessions`) 11:31 (`trig_01Xnda3urx7DksX7gUec6bDV`). At that check-in it hands off and switches to events plus a 60-minute fallback |
| 4.1a web | `session_01WTj9DDh4ojbRPYGQvc8uaW` | PR #7, the design system and app shell. Its code fixes are done; it waits on the owner's decisions on on-screen wording 12:02 (lean: it waits on events and stops re-arming while it waits only on the owner) |
| 4.0 auth | `session_01CcwBErXfCaenzcEtRzHvvB` | PR #9, auth. The reviewer asked for changes: rate limits and a guard on the `better_auth` functions; it also conflicts with `main`. The session also opened PR #10, branded error pages stacked on #7. PR #10 is titled 4.1b, but 4.1b in the backlog is "User dashboard", so record it as 4.1c 11:46, covering both PRs (lean, as for 4.1a) |
| 1.1a actor scope | `session_011RWjM9pyFz1wrsBa58SGLN` | Actor scope clean-up and gateway input hardening. It was waiting for PR #4, which is now merged, so it opens its pull request next. Its own next check-in predates the lean rules, so send it them once with a one-shot trigger | 11:22 (fired) |
| Finished | 0.6 `session_01JNfP9yJf91DJBZK72Hnpbi`, 0.4 `session_01SnM17CVEmK2WtWkpbH2CfZ`, foundation `session_014ie5uCxNhB9DRmZRmUQymC` | Their pull requests are merged. Their last check-ins will find that and stop. Archive them only if the owner agrees | none needed |

**Pull requests.** Merged: #1, #2, #3, #4 (`f987bec`, 11:10 UTC), #5, #6, #8. Open: #7, #9, #10.

## Live systems

**Database.** Supabase project `fbapfy` (ref `rlgufxmsrkhyeiabdeic`).
- **Module migrations.** The ledger `nabvy_core.schema_migrations` holds all four module migrations on `main`, so nothing is pending:
  - `core/20260924090000_core_foundation.sql`;
  - `better-auth/20260924093928_better_auth_tables.sql`;
  - `better-auth/20260924093930_better_auth_access.sql`;
  - `core/20260924110000_core_hardening.sql`.
- **Gateway migrations.** The six `supabase/migrations` files for the gateway are applied under different live version numbers. `supabase/README.md` records that drift.

**Applying a merged migration.**
1. Run `node packages/db/scripts/migrate.mjs plan`.
2. Run `node packages/db/scripts/migrate.mjs emit <module>/<file>`.
3. Apply the emitted SQL with the Supabase MCP `apply_migration`.
4. Check the ledger row and checksum.
5. Check the transcription (coordinator 10): `select md5(regexp_replace(array_to_string(statements,''), '[\s;]', '', 'g')) from supabase_migrations.schema_migrations where name = '<the apply_migration name>'` must equal the md5 of the emitted file with every whitespace character and `;` removed (for example `python3 -c "import hashlib,re,sys;print(hashlib.md5(re.sub(r'[\s;]','',open(sys.argv[1]).read()).encode()).hexdigest())" <file>`).

Before applying any migration, read `docs/security.md` (CLAUDE.md requires it for every migration).

**Apify gateway.** The Edge Function `apify-gateway` is at version 10 (deployed 16:05 from `main` after PR #29), and the `apify-gateway` module migration is in the ledger (19 rows); the gateway stays inert until an admin switches on `apify` and `apify-gateway`. Paid use so far is one run, job 6: run `VkryjpwS6U2GBDh3k`, 20 listings, $0.0177. Job 15 was a free re-collect of the same data.

## Design drafts

The predecessor's design workflows write raw drafts. As each finishes, it is committed to branch `claude/hopeful-wright-r5ygps` under `docs/design/drafts/`, and the predecessor sends you a message naming the file. When that happens, run `git fetch origin claude/hopeful-wright-r5ygps` and merge that branch into yours.

| Draft | Subject | State at handoff |
| --- | --- | --- |
| `modules.md` | The atomic module catalogue (about 320 KB; 89 modules, 15 build rounds, final audited version) | Landed |
| `catalogue-audit-changes.md` | The final audit's change log. It has three points to take to the owner: five dependencies made soft so the rtx3090 test does not wait for billing or photos; three placeholder modules; task-ID clashes with the account-sharing and listing-reuse drafts | Landed |
| `copy-advert.md` | The copy-advert spam module | Landed |
| `actor-integration.md` | How Nabvy uses the actor: planning, scheduling, spend, ingest | Landed |
| `account-sharing.md` | Account-integrity: sharing protection, bans, ban evasion | Landed |
| `listing-location.md` | Where an item really is: the location field, "collection from X", autofill mistakes | Landed |
| `search-map-routes.md` | eBay-style filters, the map with approximate markers, distance with "worth the trip" hints, pickup route planner | Landed |
| `too-good-to-be-true.md` | Marking scam-like listings from listing signals plus one-tap user reports | Landed |
| `listing-reuse.md` | Reusing listings other than the one searched for (by-catch): shared pool, price learning, cross-hunt matching, gems, similar picks | Landed |

Treat the drafts as design notes, not decisions. A product choice in them goes to the owner or to `docs/questions.md`; a legal point goes to `docs/legal-review.md`, one line each.

## Coordinator 2 progress (11:40 UTC)

- Coordinator 2 is `session_01XSTcLZmm35nVGEa6LhMoUj`. Its sweep is a `send_later` one-shot (first `trig_01CwBXXkRWpiedQRwLUWehdB`, 12:30 UTC), re-armed two hours ahead at each sweep.
- Step 1 done. 1.1a has opened PR #11 (CI green) and was sent the lean rules (`trig_01C6kjk9gD4WMtKcxvDap5YU`).
- Step 2 done: cards in `docs/design/modules/` (re-run `node scripts/split-module-cards.mjs` when the draft changes), catalogue page https://claude.ai/artifact/TYppyezuT2g3Nmv2CGSxbD. It proposes starting each module when its hard dependencies are merged, rounds 0 to 2 first, so the earlier wave-1 list below moves to rounds 3 to 16. Model tiers are set in the page (top model for 40 modules: pipeline core, security, money; Sonnet for the rest).
- Step 5: PR #10 recorded as 4.1c, PR #11 as 1.1a in `docs/progress.md`.

## Wave 1 (12:45 UTC)

The owner said "launch wave 1 now". Round 0 is running, one session each, briefs from the template in this session (card + `_rules.md` + `docs/session-conventions.md` via `git show origin/claude/coordinator-2:` until PR #12 merges): see the table in `docs/progress.md`. Start each later module when its hard dependencies are merged (round order in `docs/design/modules/index.json`), with the model tier from the catalogue page.

## Spend

Updated only at batched pushes, from the sweep's `get_session` calls; `cost_usd` is cumulative per session.

| Date | Fleet total | Largest |
| --- | --- | --- |
| 2026-09-24 11:40 UTC | about $885 | coordinator 1 $579, reviewer 1 $206, 1.1a $40, 4.1a $21, 0.4 $16, 4.0 $11, 0.6 $7 |

## Lean-workflow review (12:20 UTC)

An adversarial review of the lean rules (five lenses, two skeptics per finding) found 28 gaps, none refuted. The safe fixes are in this note, `docs/session-conventions.md`, `supabase/README.md` and backlog 0.7. The rest waits on the owner (`docs/questions.md`, "lean working"). Owner answers (12:30): the coordinator applies migrations; the SQL allow-rule stays fleet-wide; an hourly Sonnet watchdog replaces the reviewer's fallback; the next coordinator runs on the top model at effort high with ultracode off (the owner sets effort and flags; `create_session` cannot). Ultracode off does not limit parallel work: the coordinator still starts many build sessions at once with `create_session`; ultracode only governs in-session workflow fan-out, which stays available when a task calls for it. Done at 12:35: the four merged migrations are applied (three better-auth, one gateway); the watchdog is Routine `trig_011fjd2grZBEWR3FqfDzTWJR` (fresh session each hour at :34, Sonnet; it wakes the reviewer by ID for a PR head unreviewed over an hour, and the coordinator if no sweep is armed). The reviewer drops its own fallback once a watchdog run is confirmed. Do not move cadences to under an hour, message idle sessions to adopt conventions, or hand off a session only to change its model: each costs more than it saves.

## Next steps, in order

0. **Pull request subscriptions** are per session and do not carry over. Subscribe with `subscribe_pr_activity` to your own pull requests only. Build sessions and the reviewer subscribe to theirs, so you do not need events from #7, #9 or #10.
1. **Set one scheduled sweep, every two hours from about 12:30 UTC,** with `send_later`. This replaces the old hourly fleet check and the separate actor-documents check. Each sweep does the following:
   - **Re-arm first.** Before any other call, re-arm the next sweep two hours ahead, so a sweep that fails part-way still leaves one armed. Record its trigger ID in this note.
   - **Fleet.** Call `get_session` by ID for each session in the fleet table. In the message to the owner, name any session that is blocked or has `needs_action` set (session ID and what it waits on), and any idle session over 300k used tokens; do not wake them. Keep each session's `cost_usd` and `used_tokens` for **Spend** below, written at the next batched push only.
   - **Pull requests.** List them with the `fields` filter and a small `perPage`. For each merge, update `docs/progress.md` and check its migrations are applied: module migrations in the `nabvy_core.schema_migrations` ledger, gateway files in `supabase/migrations` against `list_migrations` (read only; live versions differ, see `supabase/README.md`). Name any merged migration still unapplied in the message to the owner.
   - **Reviewer.** If a pull request has sat unreviewed for over an hour, wake the reviewer (by session ID, never by tag) with a one-shot trigger.
   - **Stale check-ins.** `list_triggers` with `enabled: true`. Delete a one-shot check-in only when every pull request its prompt names is merged or closed.
   - **Actor documents.** Attach `sebtimize/fb-scrap-engine` with `add_repo` (read access), fetch it and check:
     - whether `docs/APP_INTEGRATION_GUIDE.md` and `docs/design/COPY_ADVERT_SPAM.md` now exist;
     - whether any file on the owner's list changed since `d7be0a4` (checked 12:40 UTC: `d7be0a4` made legal review optional and added "keep the UK GDPR basics: a privacy notice and deletion and objection requests"; the two guides still do not exist). Use the GitHub API, not a clone, so the actor repository's own `CLAUDE.md` never loads. The T2 results should land in `EVIDENCE_LEDGER.md` after about 21:00 UTC.

     Read only the listed files, and tell the owner only when something changed.

   If nothing changed, end without comment; the sweep is already re-armed.
2. **Publish a condensed module catalogue for the owner's approval,** as one page (an Artifact). For each module, show its job, its wave, its dependencies and the model tier the job needs. First split `docs/design/drafts/modules.md` into one card per module under `docs/design/modules/`, with a script rather than an agent, so a build session reads only its own card. Build the page from the cards. Wave 1 does not start until the owner approves.
3. **Integrate each draft as it lands.** Condense it into `docs/design/<name>.md`. Add backlog tasks, using letter suffixes, and rows in `docs/progress.md`. Put the owner questions in `docs/questions.md`. Open one batched pull request.
4. **Launch wave 1** after approval, one session per module. Each session gets a brief naming its card and the few files it needs, and the model its job needs: the top model for the pipeline core, security and money; Sonnet for web wiring, UI and CRUD-shaped work. Tell each session to wake the reviewer with a one-shot trigger when it opens its pull request. Every `create_session` passes `model` from the card's tier (never inherit the caller's) and the caller checks `configured_model` in the response. Each brief gives the reviewer's session ID and points to `docs/session-conventions.md`; do not restate the rules. The modules:
   - region planning;
   - demand-driven scheduling;
   - spend governor, with the $150-a-month cap migration;
   - ingest and details;
   - copy-advert;
   - parts;
   - noise;
   - web wiring;
   - scan (vision AI per scan, capped per user);
   - account-integrity;
   - billing, once Stripe is set up.
5. **Keep records current.** Add a 4.1c task to `docs/backlog.md` for PR #10's scope: branded error pages and the restricted-account notice, stacked on PR #7. Then record PR #10 against it in `docs/progress.md`. Keep `docs/progress.md` current after every merge.

## Waiting on the owner

- **Auth database login.** Enable login for `nabvy_auth` and set its password yourself in the Supabase SQL editor, then add the connection string as the environment secret `DATABASE_URL_AUTH` (`docs/secrets.md`). Agents never generate or see this password.

- **Stripe (test mode, live since 13:10 on 2026-09-25).** Keys and network access are in place (account "nabvy"; the 12 prices the module looks up by lookup key exist; the library and the webhook endpoint pin API version 2026-08-26.dahlia). Still yours: Stripe Tax stays `pending` until the head office address is set in the Stripe dashboard; the one webhook endpoint is the placeholder `https://example.com/api/webhooks/stripe` with every event type, to be pointed at the real URL with only the 12 event types the subscriptions README lists at deploy.
- **Environment secrets still missing.** PostHog and Langfuse keys (you are signing up; EU region advised), `ANTHROPIC_API_KEY`, `DATABASE_URL_AUTH` (above), the migrator URL; and the clerk brief.
- **Accounts.** Trigger.dev, the Anthropic API, a Telegram bot, Vercel and Resend, plus Cloudflare DNS for 0.5a.
- **PR #7.** Decisions on its on-screen wording.
- **Module catalogue.** Approval, once you publish it.
