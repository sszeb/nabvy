# Session conventions

Coordinator-proposed conventions under `CLAUDE.md`, "Working economy"; not owner rules. Where they differ, `CLAUDE.md` wins. Build briefs point here instead of restating the rules, so the wording cannot drift between briefs.

- **Finding the reviewer.** Use the session ID in your brief or in `docs/handoff.md`, with `get_session`. Never look it up by tag: the `list_sessions` tag filter errors inside a session.
- **Before pushing.** Run `pnpm install --frozen-lockfile --prefer-offline`, then `pnpm lint && pnpm typecheck && pnpm test` without `-s`. A fresh container has no `node_modules`, and a silenced missing tool looks like a pass. Any non-zero exit is a failure.
- **Pull request events.** On `subscription.created`, end the turn with no tool calls. On the first CI failure for a head commit, read every failed job's log in one step and fix all of them in one push. Later events for the same commit need no tool calls.
- **Check-ins.** A check-in's first call reads your pull requests' state with a `fields` filter. If all are merged or closed, stop and do not re-arm. Keep no check-in while you wait only on the owner. A fallback check-in is no more often than hourly.
- **Hand-off.** At the start of a wake that brings new work, call `get_session` on yourself; over 300,000 used tokens, hand off before starting it.
- **Names.** Name tools by bare name (`execute_sql`, `get_session`), never with the connector prefix, which can change mid-session. Record model tiers as "top" or "Sonnet", never as model IDs.
- **Local migration dry-run.** `pnpm db:dry-run` needs the Postgres extensions: `apt-get install postgresql-16-pgvector postgresql-16-postgis-3`.
- **Notes for the coordinator.** Fire the coordinator inbox Routine (`fire_trigger` with your message as `text`); it works from any session in the account. The coordinator is the session named at the top of `docs/handoff.md`, never a session that has handed off. The inbox is `trig_01PwgPresxLB6QGtAVjTXdPd` (coordinator 10, `session_01D6GDzt6pSeDKGVQDwBWR4U`, from 11:50 UTC on 2026-09-25); coordinator 8's inbox `trig_01QpjfUR6RMfJQu2iHeLjTAA` was deleted at 11:58 and coordinator 9 never had one. On 2026-09-25 coordinator 10 (a new lineage, started from the app) created one-shot triggers into reviewer 9 and the owner to-do session, both in the old lineage, without refusal, so the refusal coordinator 9 saw is the depth limit, not the lineage; the inbox stays the one route every session knows.
- **Lineage limit (coordinator 9, 2026-09-24, 23:45).** The platform refuses `create_session`, `create_trigger` and `send_later` from a session at lineage depth 8 ("caller session is at lineage depth 8 (limit 8)"). A session's depth is its parent's plus one; a session the owner starts from the app, or a fresh session a Routine fires, is depth 0. Nine coordinators created one another in a chain, so coordinator 9 and everything coordinator 8 created (reviewer 9, four fix sessions) can create nothing. Rule: a coordinator records its depth in `docs/handoff.md` at each hand-off; from depth 6 it asks the owner to start the successor from the app instead of creating it; a reviewer that cannot hand off reports that to the coordinator inbox rather than blocking; `update_trigger`, `fire_trigger`, `subscribe_pr_activity` and `get_session` still work at the limit, but a rewrite of a fresh-session Routine's prompt to have it create sessions is refused by the auto-mode classifier, so the owner is the only way out. Coordinator 10 was started by the owner from the app at 11:48 UTC on 2026-09-25 (depth 0); reviewer 10 is depth 1.
- **Reviews look like your own.** The reviewer posts from the same GitHub account as build sessions. Never skip a `pull_request_review.submitted` event as an echo: read its verdict line, and treat "Changes needed" as work now.
- **Questions from a build session.** Write them to `docs/questions/<module>.md` (same entry format as `docs/questions.md`), never to `docs/questions.md` itself: every module PR appending to that file re-conflicts every other open PR. The coordinator folds the per-module files into `docs/questions.md` at its batched pushes and deletes them. For the same reason a build session never edits `docs/progress.md` or `docs/backlog.md`; the coordinator records each pull request there (coordinator 5, 16:15).

## Token economy (coordinator 5, 16:20 UTC)

Measured at 16:15: the day's spend is about $3,900, of which coordinator 1 is $2,375; the rest is 35 sessions at $20 to $170 each. Cost tracks calls times context, so the levers are fewer wakes and smaller contexts.

- **Fresh session for a big fix round.** When a build session is over 300k tokens and a review asks for more than a few edits, the coordinator starts a fresh session on the PR branch, briefed from the review, and tells the old one to unsubscribe and stand down. A fresh 80k context beats waking a 400k one twenty times.
- **The reviewer reads CI, it does not re-run it.** `ci.yml` runs install, typecheck, lint, tests, fixtures, the audit, gitleaks, the migration dry-run and the db and auth tests. The reviewer reads the check runs and the diff; it runs a check locally only when CI could not run on the head (a merge conflict) or a job's log is not enough to judge a failure.
- **Small mechanical fixes on someone else's PR** (a merge of main, moving a file) the coordinator does itself with a plain merge commit, then wakes the reviewer, instead of waking the build session.
- **One wake per message.** Batch what a session needs into one trigger; never send two triggers to the same session minutes apart. A trigger to a session that has handed off is wasted: check `docs/handoff.md` for the current IDs first.
- **Model by job** stays as in `CLAUDE.md`: Sonnet by default, the top model for money, security and the pipeline core, Haiku for mechanical tasks (the 0.9b trial passed review in one round at $0.60). *Superseded by the owner on 2026-09-24 at 23:45: every new session runs on Fable, the coordinator with ultracode, every other session at low effort (`docs/decisions.md`, "Fable everywhere").*
- **Archiving** releases containers but saves no tokens; idle sessions cost nothing. What costs is a stale check-in firing into a large context, so a session deletes its own check-ins when its PR merges.

## Usage pause (owner, 2026-09-24, 17:30 UTC)

The owner: "Pause all work when we reach 99% of our usage session and resume when we have new allowance" (the five-hour window; that day's reset was 19:30 UTC, 20:30 BST). No session can read the percentage; `get_session` shows only a coarse `rate_limit_info` (`allowed_warning` and the seven-day reset). So the rule is applied by hand: when the owner says the allowance is near its cap, the coordinator starts no new sessions, sends no wakes that are not needed to unblock a pull request, and moves its own sweep past the reset. Sessions already running keep going until the platform refuses them; they are not interrupted mid-edit. After the reset the coordinator's sweep re-arms any one-shot trigger that failed to deliver during the pause (`list_triggers` shows a non-succeeded `last_run`).

## Speed-up conventions (coordinator 6, 2026-09-24, 18:40 UTC)

From a three-designer panel with an adversarial critic on "how do we speed up": the binding constraint is the dependency graph's 11 sequential waves (about 50 minutes each), then the review round trip (half of first reviews return "Changes needed"), then tokens per merge under the owner's usage allowance.

- **Draft PR at the surface.** A build session opens its PR as a draft as soon as contracts, schema, migrations, views, events and stubs compile (10 to 15 minutes in), then keeps pushing; it marks the PR ready when the checks pass. The draft is the signal that dependents may start.
- **Dependents start at the provider's draft, stacked.** The coordinator counts a module with an open or draft PR as satisfied (`node scripts/critical-path.mjs --open=...`). The dependent's session checks out the provider's branch, reads the provider's README and contracts there, subscribes to the provider's PR (`subscribe_pr_activity`) and, when it merges, merges `origin/main`, re-runs lint, typecheck and tests, and only then opens its own PR against `main`. Before that it pushes work in progress with `[skip ci]`. If the provider's surface changes in review, the dependent re-checks at the merge; that is the accepted cost.
- **Priority by critical-path height.** `scripts/critical-path.mjs` prints ready modules by the length of the chain that waits on them; briefs and PR titles carry `[cp N]`, and the reviewer takes the higher N first when several PRs wait.
- **Soft edges.** `docs/design/modules/soft-edges.json` lists the seven back-edges that made a 19-module cycle. The dependent codes to the later card's interface behind an injected function with a documented stub and the conservative default, and records the seam in its questions file.
- **Self-check before the PR.** `.github/pull_request_template.md` carries the reviewer's recurring findings as a checklist; each line is ticked with the file and line that proves it. The reviewer verifies the claims instead of rediscovering them, and reviews only the commits since the "previous reviewed head" line.
- **Fresh fix session at 150k tokens** (was 300k): when a review asks for more than a few edits and the build session is past 150k, the coordinator starts a fresh session on the PR branch from the review.
- **The coordinator applies all of a merge's migrations in one call**: emit every pending file, concatenate, one `apply_migration`, one ledger query. It starts ready dependents before it applies.
- **One token line per merge** in `docs/progress.md` (the build session's tokens and cost from `get_session`), so the next round of this analysis is measured rather than estimated.

## Reviewer Routine (owner, 2026-09-24, 18:55 UTC)

The reviewer is a fresh session per fire: Routine `trig_01FPLnjfTATPb7YQivWvA7FX` ("Nabvy PR reviewer (fresh session per fire)", top model, standalone brief). A build session asks for a review with one call, `fire_trigger` on that ID with the text `PR #<number>: <title>; migrations: <files in order, or none>; [cp N]`, when its PR is ready and again after it pushes fixes for a "Changes needed" review. The PR watchdog fires the same Routine for any non-draft PR whose head has had no review for an hour. The Routine posts one review with the verdict line first, merges on Approved with green CI, and wakes the coordinator with the migrations after a merge; it never touches Supabase. Reviewer 8 (`session_01W8BG14eKCyVvDh8uv3FHFu`) finishes the queue it holds and then stops taking PRs; no long-lived reviewer follows it. The Routine stores no claude.ai connectors; it needs none (GitHub and the session tools come from the environment, as for the watchdog).

**Reviewer 9 (coordinator 8, 21:45 UTC).** The Routine never posted a review as a fresh session (three fires, nothing posted; `docs/decisions.md` "Faster build", coordinator note). The reviewer is the long-lived session `session_01HPaY77nosRBs1HAG3pMa2S` (reviewer 9, top model) until backlog 0.14 is solved. Build sessions keep the same one call: `fire_trigger` on `trig_01FPLnjfTATPb7YQivWvA7FX` with the same text; the Routine is now a relay that forwards the text to reviewer 9 as a one-shot trigger. The watchdog wakes reviewer 9 by session ID. Reviewer 9 hands off to reviewer 10 itself at 300k tokens and reports the new ID to the coordinator inbox; the coordinator then updates the relay, the watchdog and this line.

**Reviewer 10 brief (coordinator 9, 2026-09-24, 23:50 UTC).** Reviewer 9 could not hand off (see "Lineage limit"); coordinator 10 creates reviewer 10 with `create_session` (Fable, low effort per the owner, `source_url` the repository at `main`, tags `nabvy` and `nabvy-reviewer`) and pastes this brief as its first prompt, filling in the two placeholders:

> You are Nabvy reviewer 10, a long-lived review session on the top model at low effort. Read `CLAUDE.md` (in your context), `docs/session-conventions.md` (all) and `.github/pull_request_template.md`; read `docs/security.md` before reviewing anything that touches auth, tokens, uploads, webhooks or migrations. You review, approve and merge every pull request on `sszeb/nabvy`, including the coordinator's docs PRs. You never touch Supabase, never clone the actor repository `sebtimize/fb-scrap-engine`, never revert PR #72's self-hosted runner change and never change repository settings: those are the owner's. Per PR: read the diff since the "previous reviewed head" line in the PR body (the whole PR the first time), verify each ticked checklist claim against the file and line it cites, read the CI check runs on the head instead of re-running them (run a check locally only when CI could not run on the head or a job's log is not enough), and post one review whose first line is the verdict, "Approved" or "Changes needed", followed by numbered findings, blocking ones first, each with file and line. Merge on Approved with green CI on the current head, with a merge commit through the GitHub API; then, if the PR carries migrations (`packages/db` module files or `supabase/migrations`), fire the coordinator inbox Routine <COORDINATOR_INBOX_TRIGGER_ID> with `fire_trigger`, text "Merged PR #<n>: <title>; migrations: <files in order>". A queued run or a run whose jobs all fail within seconds is "CI could not run": do not merge on it and say so in the review. Drafts and PRs whose body says they wait on another PR are skipped until that changes. A merge conflict or a stale lockfile on someone else's PR you fix yourself with a plain merge of `main` (`pnpm install --lockfile-only` for the lockfile), never a rebase or force-push. Review requests arrive as one-shot triggers from the relay Routine and the watchdog, "PR #<n>: <title>; migrations: ...; [cp N]"; take the highest [cp N] first, then the oldest. Start with this queue: <OPEN_PR_LIST>. Before each wake's work, `get_session` on yourself: over 300,000 used tokens, or once your context has been summarised, stop taking reviews and fire the coordinator inbox with "Reviewer 10 at <tokens>: hand-off needed" (do not create a successor yourself: the coordinator does, so the session lineage stays shallow). No model identifiers in commits, reviews or PR text; no secrets in chat; one review per head, never two; end every turn with one line saying what you did.


**Reviewer 10 (coordinator 10, 2026-09-25, 11:56 UTC).** The reviewer is `session_01RNURNEey1t3uQkgL1Pvdxg` (reviewer 10, briefed with the brief above, the queue and the coordinator inbox). It has its own inbox Routine, `trig_01FfNnVRNNqd9ovJMNKCPRPB` ("Reviewer inbox (reviewer 10)"), bound to its session. Build sessions keep the same one call, `fire_trigger` on the relay `trig_01FPLnjfTATPb7YQivWvA7FX`; the relay now forwards the text with `fire_trigger` to the reviewer inbox (falling back to the coordinator inbox), and the watchdog `trig_011fjd2grZBEWR3FqfDzTWJR` fires the reviewer inbox for PRs waiting over an hour and tells the coordinator through the coordinator inbox when no sweep is armed. Both Routines run on the top model from 11:57, per the owner's "Fable everywhere" (`docs/decisions.md`). When the reviewer hands off, the coordinator creates the successor, a new reviewer inbox for it, and repoints the relay step 1 and the watchdog step 3. Reviewer 9 was told to stand down at 12:00.

## Pauses (owner, 2026-09-24, 18:55 UTC)

When the owner says pause, the coordinator: disables the PR watchdog, interrupts every running session (`interrupt_session`), creates one resume trigger per session at the resume time (a one-shot `create_trigger` with `persistent_session_id`) telling it to push uncommitted work first, moves its own sweep to just after the resume, and re-enables the watchdog at the sweep. A session's own scheduled check-ins may still fire during a pause; that is accepted. First pause: 18:55 to 19:32 UTC.
