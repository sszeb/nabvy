# Backlog

Work in order. One task per session unless a human says otherwise. Every task ends with: types in `packages/contracts`, a fixture-based test, `pnpm typecheck && pnpm lint && pnpm test` clean, and a note in the module README. A task that needs a secret you do not have stops and records the need in `docs/questions.md`.

Check-in points are marked **[CHECK-IN]**: stop and wait for a human before continuing.

## Phase 0 — Foundation

- **0.1 Scaffold the monorepo.** pnpm workspaces + Turborepo; Biome; Vitest; TypeScript strict; folders and package names from `docs/engineering.md`; `@nabvy/config` with Zod-validated env; root scripts from `CLAUDE.md`; `.env.example` with every variable in `docs/secrets.md`; `.nvmrc`. Done: `pnpm install && pnpm typecheck && pnpm lint && pnpm test` pass on an empty project; config fails fast on a missing variable.
- **0.2 Contracts package.** Every schema in `docs/contracts.md` as Zod with inferred types, exported from `packages/contracts`. Done: schemas parse the sample objects in `fixtures/contracts/`; a round-trip test per entity.
- **0.3 Database schema.** `@nabvy/db`: Drizzle schema and migrations for every table in `docs/contracts.md` with owners, `v_` views, PostGIS, pgvector, pg_trgm, `uuidv7()`, monthly partitioning of `price_observations`; Better Auth tables generated with `@better-auth/cli generate` and committed; roles `nabvy_app` and `nabvy_pipeline` without `bypassrls`; the `withUser` helper and the RLS policies from `docs/engineering.md`; PostgREST disabled for `public`; seeds: the founder user (`ADMIN_EMAILS`), the UK `cells` set at H3 resolution 4, an empty `cell_provider_locations`. Done: migration applies cleanly to a fresh Supabase branch; an RLS test proves a query outside `withUser` returns nothing and inside it returns only the user's rows; the pipeline role can read `hunts` and insert `alerts` but cannot read `user_profiles`.
- **0.4 Pack loader.** `packages/packs` with the `CategoryPack` format and `gpu-pc` from `docs/packs/gpu-pc.md` (regexes, dictionary seed, parameters, risk rule references, template). Done: pack validates against the format; dictionary resolves the alias fixtures.
- **0.5 CI pipeline.** GitHub Actions per `docs/operations.md`: typecheck, lint, test, fixtures, gitleaks, audit, migration dry-run, Vercel preview. Done: a pull request shows all checks green.
- **0.5a Waitlist and marketing site skeleton.** nabvy.com with the landing page, `/waitlist` (email, postcode, products) into the `waitlist` table, `/freshness` placeholder, UTM capture, SPF/DKIM/DMARC for both sending subdomains. Done: a waitlist entry lands in the database with its UTM; the domain passes an email authentication check. Catalogue: wave 2; depends on 1.9a.
- **0.6 Fixtures harness.** `fixtures/` layout from `docs/fixtures.md` and a test runner that reports pass rate per stage. Done: runner executes on the placeholder fixture and prints a report.
- **0.7 Module boundary check.** A Node script beside `scripts/split-module-cards.mjs` compares each `services/<m>/package.json` `@nabvy/*` dependency with the module card's "Depends on" line (soft dependencies only through `@nabvy/contracts`), and runs as a non-blocking step in the existing CI job for services that have a card. Until it runs, the reviewer checks this by hand. Done: it flags a planted bad dependency in a fixture. Catalogue: wave 0; depends on 0.2, 0.3 (done).

**[CHECK-IN]**

- **0.8 Config per module.** Give `@nabvy/config` a `./modules/*` export (rule 14) and a module file per module; move `USD_GBP_RATE` out of the `apify` group into its own group; then move cost-meter's settle delay and price table from `services/cost-meter/src/config.ts` into `packages/config/src/modules/cost-meter.ts`. Raised by the cost-meter build and review (PR #16). Catalogue: wave 0; depends on 0.2 (done).
- **0.9 Event transport.** No layer publishes or delivers module events yet (no task wrapper, no Trigger.dev publisher), so modules such as `incidents` return the event envelope instead of emitting it. Build the thin-event publisher and the handler wrapper (idempotency key `source + sourceListingId + contentHash`, T-timestamps per `docs/contracts.md`, failed events to `incidents`), then replace the stubs. Top model; needed before any pipeline module runs end to end. Raised by incidents (PR #19).
- **0.9a Wire incidents into the transport.** After #19 and #20 merge: make incidents' `RecordDeadLetterInput` an alias of the core `DeadLetter` contract, and pass `incidents.record` as the `DeadLetterSink` when the first task is wired (replaces incidents' stub). Also from the #20 review: an admin retry must re-send with a fresh idempotency key or it is dropped; a failed dead-letter write on the last attempt must not lose the event silently; `maxAttempts` defaults to `eventRetry.maxAttempts`; `DeadLetter` and incidents' input are one type. Top model (pipeline core).
- **0.9b Test timeouts under load.** Cost-meter's PGlite `beforeAll` times out at 10 s under the full parallel `pnpm test` on a 4-core container (also on `main`). Raise `hookTimeout` for the PGlite suites or share one instance. Sonnet.
- **0.10 Analytics and observability clients.** Pulled forward from 4.6a and 1.5: a `@nabvy/telemetry` package with consent-gated PostHog capture (server and client), Langfuse tracing through OpenTelemetry with masking and sampling, and a property allow-list; does nothing without keys. Full scope and definition of done in `docs/design/analytics-growth.md` §6. Sonnet.
- **0.11 Replace the switch stubs.** From PR #23 (switches). (1) `quote_redaction.switch_on()` calls `switches.is_on('quote-redaction')` directly (drop the existence check), and callers stop passing a switch state in and use `isOn(q, 'quote-redaction')`; `QuoteRedactionSwitchState` goes. (2) cost-meter: a migration adds `where switches.state('cost-meter') <> 'off'` to `v_costs` (same columns); `CostMeterContext` takes its state from `state(q, 'cost-meter')` in `@nabvy/switches`; `CostMeterSwitchState` becomes `SwitchesState`; `module.json` then depends on `switches`. (3) An admin oRPC procedure for switches in `apps/web` when the admin screen is built (web wiring). Done: both modules read the live switch; fixture pass rates unchanged; the two migrations applied by the coordinator. Depends on: switches (done). Sonnet.
- **4.9a `usage-ledger`: attributed cost per account and the free lifetime cap.** From `docs/decisions.md`, "Free tier: bursts under a lifetime cap". Every check a free account causes writes its attributed cost (lone share) to the account's ledger; `freeCapReached(userId)` reads the policy's lifetime cap; a fixture proves the cap stops a burst mid-window. Depends on: usage-ledger, cost-meter. Sonnet.
- **4.10a `pricing-console`: free-tier policy rows.** Versioned `free_tier_policies` (window count and length, reset hours, burst shape as an ordered list of cadence and minutes, lifetime cap pence, per-user, per-week and per-month caps, pool rules, sign-up limits), edited by an admin with an audit row, effective within a minute through `v_free_policy`; never a constant in code. Done: a policy change alters the next window without a deploy; fixture test. Depends on: pricing-console, audit-log. Sonnet.
- **1.2p `spend-governor`: hard daily and weekly caps with a synchronous gate.** From `docs/decisions.md`, "Hard daily, weekly and monthly spend caps". Budgets gain periods `day` and `week` (Europe/London); `gate(q, provider, estimateMicros)` is called by every paid submit (apify-gateway, model callers) and refuses when reserved plus settled would pass the cap; each Apify run carries a per-run cost cap in its input and reserves it in cost-meter before submit; concurrency per provider is capped from config; a cap reached sets `hold-new` at once. Done: a fixture where the cap is £10 and runs cost up to 50p never books more than £11; the gate refuses without waiting for the scheduled recompute. Depends on: spend-governor, cost-meter, apify-gateway, 1.6a. top.
- **1.2o `spend-governor`: free-burst pool.** A budget per day, week and month for free bursts, revenue-linked with a floor (initially £20 a day or 5% of last month's net, whichever is higher); when spent, new bursts wait or run only where a paid watcher funds the area. Depends on: spend-governor, 4.10a. Sonnet.
- **1.2r `check-scheduler`: burst schedules.** A free want runs the policy's burst shape for its window, at the area's live cadence where that is faster, and stops on the lifetime cap or the pool. Depends on: check-scheduler, 4.9a, 4.10a, 1.6c. top.
- **4.3r `account`: schedule the deletion purge.** From PR #33's review: `purgeDueDeletions` exists but nothing runs it. A pg_cron job or thin Trigger.dev task every 15 minutes, idempotent, batch. Done: a due request is purged within 24 hours in a fixture; no trigger file holds logic. Depends on: account (done). Sonnet.
- **4.3s `account-integrity`: global sign-up throttle and admission queue.** From `docs/decisions.md`, "Sign-up throttle and surge stop". Policy rows for sign-ups per minute, hour and day; beyond the rate a sign-up is queued and admitted in order; free bursts start only on admission at a policy-set rate; admission pauses on a breaker trip. Done: a fixture where 1,000 sign-ups in one minute admits only the policy's number and queues the rest; the queue drains in order. Depends on: 4.3c, 4.10a. Sonnet.
- **1.2q `spend-governor`: circuit breakers.** From the same decision. Rate counters (sign-ups, paid submits, submits from young accounts, cost per minute) with policy thresholds; a trip sets `hold-new` at once for free bursts and young accounts, pauses admission, alerts the founder, and resets only by an audited admin action. Done: a fixture where 1,000 young accounts submit in one minute trips the breaker before the concurrency cap's worth of runs is exceeded. Depends on: 1.6d, 4.3s. top.
- **4.1q Cadence slider.** From `docs/decisions.md`, "Cadence slider and the app's look" and `docs/design/cadence-slider.md`. The want form's interval control on radix Slider and shadcn tokens: seven steps 4 h to 1 min (4.1r), plan-limit lock with an upgrade hint, the credit estimate and live-cadence note from the estimate procedure (never a number in the component), the read-only burst timeline for free accounts, reduced-motion fallback, keyboard and screen-reader support, light and dark. Done: Storybook-free fixture render tests for each state; no new dependency. Depends on: 4.1a (done), want-manager for the live estimate (stub until then). Sonnet.
- **0.12 Enable `pg_cron` and schedule partition rotation.** From PR #42 (`docs/questions.md`, "monthly partition rotation"). A core migration enables `pg_cron` (Supabase ships it) and schedules `product_events.ensure_month_partition(current_date + interval '2 months')` monthly; later modules with partitioned tables register their own jobs the same way. Also the natural home for the schedules in 1.2m and 4.3r if those sessions choose `pg_cron` over Trigger.dev. Sonnet.
- **4.9e `usage-ledger`: follow-ups from the review of PR #47** (reviewer 8, 18:07; fix before pricing-console wires in a real policy). (1) Limit the generic `grant()` to taste and referral credit; allowance and top-up go through `grantByPolicy` only, whose replay requires `policyVersion`. (2) Validate the policy quote with `UsageLedgerPolicyQuote.parse` in the service. (3) Order `dueBuckets` by user so two concurrent `expireBuckets` sweeps cannot deadlock. (4) A reversal after a bucket was swept credits an expired bucket the sweep never closes again; decide and record. (5) Column-level insert grant on `entries`, a future-date check on `expiresAt` for grants, and a SQL test for the `policy_version` check. Top.
- **0.9d Convention checks in CI.** From the speed-up panel (18:40). A `scripts/check-conventions.mjs` step after lint that fails in five minutes on what the reviewer keeps finding: vitest timeouts missing, a view without `security_invoker`, a function without `set search_path` or the `revoke ... from public`, `process.env` outside `packages/config`, a module without a README "Decisions" section, a backlog ID cited in `docs/` that does not exist, a migration folder without `module.json`. Under 200 lines, allowlist comments per rule. Sonnet.
- **0.13 Module generator and interface digest.** `scripts/new-module.mjs <name>` scaffolds a module in the merged shape (from `services/usage-ledger`): package, tsconfig, vitest config with timeouts, README skeleton with the required sections, `src/` shape, PGlite test support, test skeletons, contracts and schema files, `migrations/<name>/module.json`, the SQL test skeleton. `scripts/interface-digest.mjs` writes `docs/design/modules/_interfaces.md`, one paragraph per merged module (exports, views, events) extracted from the READMEs, so a build session reads a digest instead of six READMEs. Sonnet.
- **0.14 Reviewer as a fresh-session Routine.** One Routine (top model, `create_new_session_on_fire`) with the standalone reviewer brief; build sessions and the watchdog fire it with the PR number. Replaces the long-lived reviewer that hands off hourly; each PR gets a small fresh context. Switch at reviewer 8's handoff; record the trigger ID in `docs/session-conventions.md`. Coordinator.
- **0.15 Merge clerk Routine (needs the owner's yes).** A Sonnet fresh-session Routine that does the mechanical post-merge work (apply pending migrations after reading `docs/security.md`, ledger check, progress row, questions fold) so the top-model coordinator wakes only for exceptions and the sweep. Changes "the coordinator applies every merged migration" in `CLAUDE.md`; the owner decides.
- **0.16 Apply-pending script.** `node packages/db/scripts/migrate.mjs apply-pending` that reads the ledger and applies every unapplied file in order; needs a database URL the cloud session can reach (the proxy is HTTPS-only), which the owner must provide. Until then the coordinator concatenates the emitted files into one `apply_migration` call.
- **4.10b `pricing-console`: the paid ladder as policy rows.** From `docs/decisions.md`, "Paid ladder". Per tier: base cadence, floor, bundled credits, monthly and yearly price, top-up rate, areas and wants, each a versioned policy row edited from the admin console, audited, applied within a minute, with the "Always profitable" floor refusing a value below cost plus the minimum margin. `usage-ledger` reads the bundle and top-up rate; `want-manager` and the cadence slider read the base and floor as the plan ceiling; `check-scheduler` caps each want at its plan's base or bought floor. Initial values: the coordinator's table in that decision. Plans are marketed as "up to" their cadence.
- **4.10c** Stripe integration plan and review (docs/design/stripe-integration.md): map Billing, Payments, Tax, Invoicing and Connect to subscriptions, pricing-console and usage-ledger; review PR #54 against it; gaps become 4.10d onward. Owner ask of 19:50, `docs/decisions.md` "Stripe scope". Session `session_01Vmzb8zTcit1ie6z5Y13CNE` (top), started 19:50 by coordinator 6.
- **4.1r Cadence slider: seven steps.** From the same decision. The 30-minute step (Steady) added for Pro's base; the ladder is Ultracheck 1 min, Rapid 5 min, Brisk 15 min, Steady 30 min, Regular 1 h, Relaxed 2 h, Slow Watch 4 h (`docs/design/cadence-slider.md`). Folded into 4.1q if that PR is not yet open.
- **4.3t Abuse and cost-exploit threat model.** From `docs/decisions.md`, "Sign-up throttle and surge stop". `docs/design/abuse-threat-model.md`: every way an attacker or a crowd can make Nabvy spend or leak (sign-up floods, burner accounts, free-burst farming, credit and referral gaming, card-check bypass, Telegram link and webhook replay, Apify cost amplification, account sharing, API keys), with sign-up farming and resource extraction as their own sections (detection signals, automatic response ladder, fixtures; owner, 17:07), each with the guard that stops it, the module that owns the guard, the fixture that proves it, and the gap if none exists; adversarially checked; backlog rows for every gap. Done: the document, the gaps as backlog tasks, `docs/security.md` updated. Depends on: nothing. top.
- **4.3u Edge protection on Cloudflare.** Turnstile on sign-up and magic-link requests (already in `docs/security.md`), rate-limiting rules and Bot Fight Mode on the sign-up and search routes, Waiting Room on the sign-up page if the plan allows; configured as code where Cloudflare supports it, documented otherwise. Needs the owner's Cloudflare account (0.5a). Depends on: 0.5a, 4.3s. Sonnet.
- **4.3q `account-integrity`: free-tier guards.** Sign-up limits per IP, device fingerprint and email domain (disposable domains refused); one card per account, card check (never charged) before windows two and three; a per-area anomaly stop when free sign-ups in one area jump; a switch `free-bursts` an admin can turn off. Depends on: 4.3c, 4.10a. Sonnet.
- **4.6d `lifecycle-messaging`: window messages.** Window end with the user's real numbers, next-window reminder at 72 hours, cap reached, last window used. Depends on: 4.6b, 4.10a. Sonnet.
- **1.2m Spend-governor recompute schedule.** From PR #36's review. A scheduled recompute of `spend_governor.throttle` at most every 15 minutes (pg_cron or a Trigger.dev task, thin), so `v_throttle` never reads `hold-new` from staleness. Done: the job exists, is idempotent, and a fixture test covers a stale row. Depends on: spend-governor (done). Sonnet.
- **1.2n Measure residential proxy use.** From PR #36's review. `apify-gateway` exposes `proxy_gb` per job in `v_jobs` from the run's usage, so the 10 GB proxy budget can throttle. Done: the column is filled from real run usage; spend-governor reads it; fixture test. Depends on: apify-gateway, spend-governor (done). Sonnet.

## Phase 1 — Walking skeleton on Facebook (our actor)

- **1.0 Document the Facebook actor.** Run the deployed actor once by hand; record its real input schema and output fields in `services/source-adapters/README.md`; map every field to the target shape in `docs/providers.md`; fill `cell_provider_locations` for the founder's cell and its neighbours; list any missing required field in `docs/questions.md`. Done: the mapping table exists and one recorded raw response is saved under `fixtures/listings/facebook/`. Split into 1.0a (atomic module tasks): 1.0a records the missing fixture runs.
- **1.1 Apify Facebook adapter.** `apify-facebook-nabvy` implementing `ProviderAdapter.watch` and `detail` with the input mapping in `docs/providers.md`; response snapshots to storage; cost and latency metering; seller hashing. Done: normalisation test from three recorded raw responses; a live smoke test on one cell writes stubs to `listings`. Split into 1.1a to 1.1f (atomic module tasks).
- **1.2 Crawl planner (minimal).** One hardcoded hunt row → one crawl unit (computers, the founder's cell) → scheduled tick at 300 s → `crawl.due`. Done: the unit runs on schedule and `crawl_runs` fills. Split into 1.2a, 1.2b, 1.2c, 1.2e (atomic module tasks; 1.2d unused).
- **1.3 Listing registry.** Upsert, `contentHash`, new/changed detection, high-water mark update, recheck scheduling at 6/24/72 h. Done: second run of the same page produces zero `listing.new`; a changed price produces `listing.changed`. Split into 1.3a to 1.3g (atomic module tasks).
- **1.4 Cheap gate and detail fetch.** Gate from the pack; `detail.requested` in batches of 20–50; `details.fetched` writes `listing_details`, photo fingerprint and embedding. Done: gate precision ≥ 90% on fixtures; details stored for candidates only. Split into 1.4a to 1.4d (atomic module tasks); the title gate is dropped.
- **1.5 Extraction, rules tier + Haiku tier.** `generateObject` with `GpuPcFacts`; per-field confidence; cache by `contentHash`; quarantine on invalid output; product-key resolution by dictionary then pg_trgm; Langfuse tracing through OpenTelemetry with `listingId`, `packId`, tier and cost, trace ID stored on `item_facts`; pack prompts fetched from Langfuse prompt management by label. Done: pass rate on fixtures reported; 20 real listings extracted and spot-checked by a human; traces visible with cost per listing. Split into 0.4b, 1.5d to 1.5h (atomic module tasks); 1.5a (evaluation harness) kept.
- **1.5a Evaluation harness.** Fixtures mirrored as a Langfuse dataset; deterministic scorers (field match, hidden-GPU detection, no invented numbers); an evaluation run per prompt or model change linked from the pull request. Done: a deliberately weakened prompt fails the run.
- **1.6 Price Book v0 and valuation v0.** Observations from asks only; value band with `ask_based` state; CeX cash price via `cex-web` for the top 50 product keys; valuation per `docs/valuation.md` with margin, deal score, confidence, explanation; `unvalued` when data is thin. Done: fixture valuations match hand values within tolerance; explanation contains only input numbers. Split into 1.6d, 1.6e (atomic module tasks); sold-based valuation after the MVP.
- **1.7 Risk screener v0.** Rules `wanted_post`, `empty_box`, `parts_only`, `mining`, `untested`, `deposit_request`, `price_far_below_floor`. Done: rule tests pass; scores stored. Split into 1.7a to 1.7g, 1.7i, 1.7j (atomic module tasks); 1.7h reserved for `multi-quantity-filter` (parked).
- **1.8 Router and Telegram dispatcher.** Match against the hunt; dedupe; deal-card message with freshness stamp and open link; `alerts` and `delivery_log` written; the founder's chat linked through `FOUNDER_TELEGRAM_CHAT_ID` until channel linking exists. Done: a real alert reaches the founder's Telegram within one cadence of a new listing; `freshnessSeconds` recorded. Split into 1.8e to 1.8l (atomic module tasks).
- **1.9 Ops monitor v0.** `track()` and `product_events`; daily rollup of freshness, provider spend, model spend, error rates; per-provider kill switch honoured by the adapter. Done: rollup row appears; flipping the switch stops calls; alert events recorded. Split into 1.9a to 1.9f (atomic module tasks).

**[CHECK-IN]** Run the skeleton for a week. Record: cost per new listing found, median freshness, arrival rate per cell, share of PC listings with hidden GPUs, extraction correction rate. Decide the primary and fallback Facebook actors and the model thresholds; write them into `docs/decisions.md`.

## Phase 2 — eBay and real sold prices

- **2.1 eBay Browse adapter.** Application token; `item_summary/search` newest-first per product key with GB filters; `getItem` for detail; EPN affiliate header; GTIN cache. Done: eBay listings flow through the same pipeline; `listedAtPrecision` exact.
- **2.2 Ended-listing signal.** Recheck eBay items via `getItem`; write `ended` observations. Done: observations accrue; band uses them at half weight.
- **2.3 Marketplace Insights adapter (flagged).** Behind `EBAY_INSIGHTS_ENABLED`. Done: adapter tests against recorded responses; no-op when disabled.
- **2.4 Price Book v1.** Recency weighting, trend, staleness, sell-through, days-to-sell, nightly and hourly refresh. Done: synthetic series tests; bands for the top 50 keys published.
- **2.5 Free-tier eBay alerts.** Router rule: eBay alerts live for free users; other sources digest. Done: digest job produces one message per day.

**[CHECK-IN]**

## Phase 3 — Scan mode

- **3.1 Camera and barcode in the PWA.** Barcode Detection API with ZXing fallback; photo capture; upload to storage with 30-day expiry. Done: EAN read on 20 test items.
- **3.2 Recognition.** EAN → product; vision model with the recognition contract; confirmation UI below 0.8. Done: ≥ 80% correct on 100 test items. Catalogue: wave 3; depends on 0.4b, 1.1d, 4.3a, 1.9a; `cex-adapter` (soft). Kept as `scan-recognition` (atomic module tasks).
- **3.3 On-demand fetch.** Parallel adapter calls with per-scan cap; write observations; compute band; cache. Done: first-time scan under 10 s median; repeat scan under 2 s. Catalogue: wave 10; depends on 3.2, 1.6d, 4.9, 4.3a; eBay, CeX and sold-price modules (soft). Kept as `scan-lookup` (atomic module tasks).
- **3.4 Scan card.** Blocks in `docs/scan-mode.md`; similar-item section from `search_by_image` and `listing_embeddings`. Done: card renders from fixtures; verdict agreement ≥ 70%. Superseded: after the MVP as `similar-items` (atomic module tasks).
- **3.5 Inventory and eBay drafts.** eBay OAuth; encrypted refresh tokens; Inventory API draft; listing pack; outcome capture. Done: draft validates in the eBay Sandbox; outcome writes a `sold` observation. Catalogue: wave 7; depends on 4.0, 3.2, 1.8h, 4.3a. Kept as `inventory` (atomic module tasks); `ebay-drafts` after the MVP; `sold-reports` later.

**[CHECK-IN]**

## Phase 4 — Product surface and billing

- **4.0 Auth service.** Better Auth instance with Drizzle adapter, magic link, Google, admin and captcha plugins; `/api/auth/*` route handler; session helpers for server actions. Done: sign-up by magic link and Google in staging; admin role check test. Catalogue: wave 0; depends on 0.3 (done). Kept as `auth` (atomic module tasks; PR #9 in review).
- **4.0a Auth follow-ups.** Make the per-email magic-link counter atomic with `insert … on conflict`; add a line to `services/auth/README.md` that the x-forwarded-for rate-limit key assumes a trusted proxy (Vercel). Done: a concurrency test shows no lost increments.
- **4.0b Auth: audit admin actions.** Now that `audit-log` exists, make the refusal of unaudited admin actions call its `record()` in the same transaction. Done when every admin action writes an audit row and a test proves the action rolls back if the audit write fails.
- **4.1 Web app core.** oRPC router and typed client; onboarding with default hunt, hunts CRUD, deal feed, deal card, feedback (`real_deal`, `not_a_deal`, `bought`), all through procedures inside `withUser`. Done: Playwright flow passes; an OpenAPI document is generated from the router. Kept as web wiring, not a module (atomic module tasks); `listing-feedback` is 4.1d, `pickup-routes` 4.1e.
- **4.1b User dashboard.** Home dashboard per `docs/dashboards.md`: today block, deals-near-you map (MapLibre, OpenFreeMap), hunts performance, watchlist sparklines, inventory profit, scans, usage; `mv_user_dashboard` refresh. Done: dashboard renders from views only; Lighthouse performance above 90 on mobile. Kept as a web task (atomic module tasks); no pinned map.
- **4.1d Coverage wording.** Replace "UK and Ireland" with UK-only wording on the landing badge and footer (`docs/decisions.md`, "Beta coverage and Apify budget"). Done: no user-facing text mentions Ireland. Catalogue: wave 6; depends on 4.0, 4.3a, 1.3e.
- **4.2 Web push and email.** Serwist service worker; VAPID; Resend digest template. Done: push received on iOS and Android test devices.
- **4.3 Billing.** Per `docs/billing.md`: Better Auth Stripe plugin with the three plans and the 7-day Standard trial, lifecycle hooks → entitlements, billing portal, usage ledger with monthly grants and `chargeUsage`, top-up Checkout and webhook, boosts and exports as usage items, referral credit, automatic tax. Done: tier change alters cadence within one tick; webhook replay test on the top-up handler; entitlement matrix test; trial offered at the cap and refused on a second attempt; a scan at zero balance is refused with the top-up prompt. Catalogue: wave 4; depends on 4.0, 4.3a, 1.9b, 4.9. Kept as `subscriptions` (atomic module tasks); `usage-ledger` is 4.9; boosts parked.
- **4.3a Account and channels.** Telegram one-time-code linking, push subscriptions, email preferences, data export, account deletion with 24-hour purge. Done: Playwright covers link, export and delete. Catalogue: wave 2; depends on 4.0, 1.9a, 1.9b. Kept as `account`, with the standing record (atomic module tasks).
- **4.3b Security hardening.** CSP, rate limits on scan and hunt endpoints, captcha plugin verified on sign-up, RLS policy tests for every user table with and without `withUser`, encrypted token round-trip, upload validation, no database or service credentials in client bundles. Done: `docs/security.md` pre-launch checks all pass in staging.
- **4.4 Crawl planner v1.** Cells activated by hunts; cadence rule with spend; hourly sweep. Done: cadence changes with subscribers and spend in tests. Superseded by 1.2b and 1.2e (atomic module tasks).
- **4.5 Review console.** Quarantine queue, corrections, fixture export. Done: a correction updates the band and adds a fixture. Catalogue: wave 14; depends on 1.5e, 1.5d, 1.5f, 1.5g, 1.7g, 1.7a, 1.8l, 1.9b, 4.3c, 4.1d. Kept as `review-console` (atomic module tasks).
- **4.6 Public freshness page.** Median freshness per source from `metrics_daily`. Done: page renders live numbers. Kept as a web page over `app.v_ops_metrics_freshness` (atomic module tasks; 1.9e).
- **4.7 Compliance surface.** Terms, privacy and cookie pages (human-written text), cookie consent gating PostHog, affiliate disclosure, retention jobs (pg_cron), sub-processor list. Done: consent blocks analytics until accepted; retention jobs verified on staging data. Kept (atomic module tasks); `output-guard` (4.11) and `seller-rights` (4.12) added beside it.
- **4.5a SEO price pages.** `/prices/[productKey]` rendered nightly from `value_bands` for every product key with a band, with sitemap and structured data. Done: pages indexable, under 1 s server render, sign-up prompt tracked. After the MVP as `seo-price-pages` (atomic module tasks).
- **4.6a Analytics and feedback loop.** PostHog after consent (client and server), person properties, the funnels and surveys in `docs/analytics.md`, session replay with masking on three screens, feature flags read server-side (including the Facebook gate flag), user feedback and corrections written back as Langfuse scores. Done: the sign-up-to-paid funnel renders in PostHog with test users; a `not_a_deal` verdict appears as a score on the originating trace.
- **4.6b Lifecycle messaging.** Per `docs/marketing.md`: consent capture and preference centre, `canMarket`, suppression sync with Resend and PostHog webhooks, the eleven programmes configured in PostHog Workflows with goal events and the daily cap, React Email templates with one-click unsubscribe, transactional templates on `mail.nabvy.com`. Done: abandoned-checkout and cap-reached programmes fire in staging from test events; a suppressed address is skipped; unsubscribe works in one click. Catalogue: wave 4; depends on 1.9d, 4.10, 4.3a. Kept as `lifecycle-messaging` (atomic module tasks); `marketing-consent` is 4.10.
- **4.6c Nabvy Daily.** Per `docs/marketing.md`: `mv_market_daily`, the recap model call with the no-invented-numbers check, per-user rendering, Resend batch send, `/daily/[date]` public page with newsletter sign-up, channel-bot post, skip-when-empty. Done: a full brief renders for a test user with two hunts; a user with nothing new receives nothing; the public page for today is indexable and under 1 s. Catalogue: wave 13; depends on 1.8g, 4.10, 1.8l, 1.8h, 1.6e; `sold-price-book`, `cex-adapter` (soft). Kept as `daily-brief`, private brief only (atomic module tasks; question 28).
- **4.7a Affiliate programme.** Per `docs/affiliates.md`: Dub click cookie read at sign-up, lead tracking, sale and chargeback tracking from the Stripe handlers (idempotent by invoice ID), the £5 referred-user credit, `/partners` page with terms and application link. Done: an end-to-end test in Dub's test mode shows click → lead → sale → commission; a chargeback reverses it. Catalogue: wave 5; depends on 4.0, 4.3a, 4.3, 4.9. Kept as `attribution` (atomic module tasks).
- **4.8 Admin dashboard, monitoring and runbooks.** Four-tab admin dashboard per `docs/dashboards.md` with audited actions; Sentry alerts; founder Telegram alerts; runbooks linked from admin. Done: a forced provider pause triggers the alert and shows on the operations tab; an audited action writes `audit_log`.

**[CHECK-IN]** Private beta with design partners.

## Phase 5 — Breadth and launch

- **5.1 Gumtree adapter** (Apify Store actor). Done: same acceptance as 1.1. Parked as `gumtree-adapter` (atomic module tasks).
- **5.2 Facebook fallback adapter** (Store actor) and fail-over. Done: forced primary failure routes to fallback.
- **5.3 Discord and Telegram channel bots** for "deals near [city]" with watermark. Done: a server owner can install and receive a feed. Back in the plan with no module owner yet (atomic module tasks; question 39).
- **5.4 Second pack** (consoles and controllers) as data only. Done: no pipeline code changes required.
- **5.3a MCP server (read-only).** `services/mcp` exposing deals, hunts, trends and metrics tools with the same authorisation as the calling user, for the founder's analysis agents first. Done: an agent lists yesterday's top deals through the server.
- **5.4a Public API (Business tier).** Versioned OpenAPI endpoints from the oRPC router for deals, hunts and scans; API keys hashed and scoped; rate limits; docs page. Done: a Business test account pulls its deal feed with an API key. Back in the plan with no module owner yet (atomic module tasks; question 39).
- **5.5 Legal gate checklist** for Facebook alerts to paying users, per `docs/decisions.md`. Done: checklist signed off by a human. *(The gate was lifted by the owner on 2026-09-24: `docs/decisions.md`, "Legal gates lifted".)*

- **5.6 Load check and launch checklist.** Staging load run per `docs/operations.md`; every launch-checklist item ticked with evidence in `docs/questions.md` or the admin area. Done: p95 pipeline delay under 30 s; checklist complete.

**[CHECK-IN]** Paid beta.

## Atomic module tasks (catalogue order, 2026-09-24)

Converted from the ordered table in `docs/design/drafts/catalogue-audit-changes.md` (lines 47–122), which sequences the atomic-module catalogue (`docs/design/drafts/modules.md`) into waves by dependency round. An ID that already existed in this file before this push (for example 0.7, 0.8, 4.0, 0.5a, 4.3a, 3.2, 3.3, 3.5, 4.5, 4.6b, 4.6c, 4.7a, 4.1d) is not duplicated here; its catalogue wave and dependencies are appended to its existing Phase 0–5 bullet above instead. The rows below are the ordered table's remaining tasks.

- **1.1a `apify-gateway`.** actor input contract and run shapes. Done: As `actor-integration.md:431`. Wave 0. Depends on: 0.2 (done). Source: `actor-integration.md:431`.
- **1.1d `cost-meter`.** Done: As `actor-integration.md:430`. Wave 0. Depends on: 0.3 (done). Source: `actor-integration.md:430`.
- **1.9b `audit-log`.** Done: As `actor-integration.md:427`. Wave 0. Depends on: 0.3 (done). Source: `actor-integration.md:427`.
- **1.9c `incidents`.** Done: As `actor-integration.md:429`. Wave 0. Depends on: 0.3 (done). Source: `actor-integration.md:429`.
- **1.1b `apify-gateway`.** gateway migration and Edge Function. Done: As `actor-integration.md:432`. Wave 1. Depends on: 1.1a. Source: `actor-integration.md:432`.
- **1.9a `switches`.** Done: As `actor-integration.md:428`, plus `switches.state()` and `switches.is_on()` as SECURITY DEFINER functions with `EXECUTE` for `nabvy_app`. Wave 1. Depends on: 1.9b. Source: `actor-integration.md:428`; `copy-advert.md` 5.1.
- **0.4b `product-catalogue`.** Done: Every `part-patterns.json` regex compiles with the `i` flag; "OptiPlex 3090" never resolves to an RTX 3090; desktop and mobile 5080 differ; the pack's alias fixtures; seed extended from the pack dictionary. Wave 2. Depends on: 1.9a, 1.9b. Source: `modules.md:366`.
- **1.1c `apify-gateway`.** module package. Done: As `actor-integration.md:433`. Wave 2. Depends on: 1.1b, 1.1d, 1.9a. Source: `actor-integration.md:433`.
- **1.0a Record fixture runs (owner-approved one-offs).** Done: As `actor-integration.md:446`. Wave 3. Depends on: 1.1c. Source: `actor-integration.md:446`.
- **1.1e `route-health`.** module. Done: As `actor-integration.md:437`. Wave 3. Depends on: 1.1c. Source: `actor-integration.md:437`.
- **1.2c `spend-governor`.** Done: As `actor-integration.md:439`. Wave 3. Depends on: 1.1d, 1.9a, 1.1c. Source: `actor-integration.md:439`.
- **1.3a `listing-ingest`.** Done: As `actor-integration.md:434`, plus the primary photo ID and `money.kind` in `v_listings`. Wave 3. Depends on: 1.1c. Source: `actor-integration.md:434`; `copy-advert.md:20`. listing-reuse draft: record the finding term and centre on each sighting (`centre_id` added to `sightings`); a listing found by two terms carries both origins; existing sighting tests still pass.
- **1.9d `product-events`.** Done: The consent gate; the property allowlist. Wave 3. Depends on: 1.9a, 4.3a. Source: `modules.md:1066`.
- **4.9 `usage-ledger`.** (split from 4.3). Done: Bucket order; refusal at zero; reversal on failure; a repeated `grant()` with the same `refId` writes once. Wave 3. Depends on: 4.0, 4.3a, 1.9a. Source: `modules.md:1372`.
- **4.10 `marketing-consent`.** (split from 4.6b). Done: A suppressed address is never sent to. Wave 3. Depends on: 4.0, 4.3a, 1.9a. Source: `modules.md:1432`.
- **1.3b `run-coverage`.** Done: As `actor-integration.md:435`, plus bounded baselines. Wave 4. Depends on: 1.1c, 1.3a. Source: `actor-integration.md:435`.
- **1.3c `detail-evidence`.** Done: As `actor-integration.md:436`. Wave 4. Depends on: 1.1c, 1.3a. Source: `actor-integration.md:436`.
- **1.3f `seller-key`.** (gated: question 7). Done: The key is stable for a numeric ID; links for any other purpose refused; only the allowlist reads `restricted_listing_keys`. Wave 4. Depends on: 1.1c, 1.3a. Source: `modules.md:593`.
- **1.4a `details-queue`.** Done: As `actor-integration.md:442`. Wave 4. Depends on: 1.1c, 1.1e, 1.2c. Source: `actor-integration.md:442`.
- **1.7j `seller-reply-reports`.** Done: A second identical report writes nothing; no reporter ID in any view; purge on `account.deleted`; the rate limit. Wave 4. Depends on: 1.9a, 4.0, 4.3a, 1.3a. Source: `modules.md:1004`. too-good-to-be-true design §6.2: scaffold with `pnpm new:module seller-reply-reports`; the tables, views, events and functions of §6.2; the gate on `notifier`'s open record, weights, levels, second answers, per-member location distance, burst and counter-report holds, report-then-buy, carried relist reports, bans and withdrawal. Done also: stages `gate`, `aggregate`, `spread` and `privacy` pass; the `v_reporter_signals` grants are tested; ships `shadow`. Depends also on: soft `copy-advert`, `relist-merge`, `listing-feedback`, `account-integrity`.
- **1.1f `source-health`.** Done: As `actor-integration.md:440`. Wave 5. Depends on: 1.1c, 1.1e, 1.3b. Source: `actor-integration.md:440`.
- **1.2a `city-pages`.** Done: As `actor-integration.md:438`. Wave 5. Depends on: 1.3a, 1.3b, 1.9b. Source: `actor-integration.md:438`.
- **1.3d `listing-lifecycle`.** Done: As `actor-integration.md:445`. Wave 5. Depends on: 1.3a, 1.3c, 1.4a. Source: `actor-integration.md:445`.
- **1.3e `listing-suppression`.** Done: A suppressed listing never reaches an `app` view; look-alike entries expire after 90 days; only hashes stored; `is_suppressed()` granted to `nabvy_app`; with the module off, `app.v_listing_card` returns no rows. Wave 5. Depends on: 1.3a, 1.3c; `seller-key` (soft). Source: `modules.md:1019`.
- **1.3g `relist-merge`.** Done: Every merged group checked by hand in a backtest; the PC 4070 median case as a synthetic index case. Wave 5. Depends on: 1.3a, 1.3c; `photo-review`, `seller-key` (soft). Source: `modules.md:730`.
- **1.5d `parts-rules`.** Done: The labelled set of about 100 PC descriptions scored per field; "GDDR7" is not system RAM; the Legion desktop; `+`-encoded text. Wave 5. Depends on: 1.3a, 1.3c, 0.4b. Source: `modules.md:625`.
- **1.2b `search-planner`.** Done: As `actor-integration.md:441`. Wave 6. Depends on: 1.2a, 1.9b; `want-manager` (soft). Source: `actor-integration.md:441`.
- **1.4b `details-selector`.** Done: As `actor-integration.md:444`. Wave 6. Depends on: 1.3a, 1.2a, 1.4a; `want-manager` (soft). Source: `actor-integration.md:444`.
- **1.5e `parts-ai`.** (ships off: question 11). Done: Schema validation; a quote not in the text is rejected; a listing whose text instructs the model; a cache hit on the same evidence hash; a weakened prompt fails the evaluation run. Wave 6. Depends on: 1.5d, 1.3c, 0.4b, 1.2c, 1.1d, 1.4a, 0.8. Source: `modules.md:640`.
- **1.5f `parts-record`.** Done: The two worked examples as synthetic fixtures; the recorded inclusion cases. Wave 6. Depends on: 1.5d, 0.4b; `parts-ai`, `photo-review` (soft). Source: `modules.md:670`.
- **1.7a `copy-advert`.** core, in shadow. Done: As `copy-advert.md:650`. Wave 6. Depends on: 1.9a, 1.9b, 1.3a, 1.3c, 1.4a, 1.2a, 1.3e, 4.3a; `seller-key`, `photo-review` (soft). Source: `copy-advert.md:650`.
- **1.8f `location`.** Done: The recorded run's distances from Facebook's reported centre; the city-page fallback; rounding. Wave 6. Depends on: 1.2a. Source: `modules.md:443`.
- **1.8h `listing-card`.** Done: The exact column list; a suppressed listing never appears; titles masked; the stale-fallback flag; no coordinates; no image column while `listing-photos` is off. Wave 6. Depends on: 1.3a, 1.3c, 1.3d, 1.3e, 0.8. Source: `modules.md:882`.
- **1.8i `price-drop-watch`.** Done: The history view never joins across listing IDs; an unchanged price gives no alert; the displayed previous price is the seller's figure only. Wave 6. Depends on: 1.3a, 1.3d, 1.3g, 1.3e, 4.0, 4.3a. Source: `modules.md:897`.
- **1.2e `check-scheduler`.** Done: As `actor-integration.md:443`. Wave 7. Depends on: 1.1c, 1.2b, 1.2c, 1.1f, 1.3a, 1.3b. Source: `actor-integration.md:443`.
- **1.4c `pasted-link-lookup`.** Done: As `actor-integration.md:447`. Wave 7. Depends on: 1.4a, 1.8h, 1.3e, 4.0, 4.3a. Source: `actor-integration.md:447`.
- **1.5g `listing-assessment`.** Done: "RTX 4090 not included" demoted; a complete system titled "OFFERS" is a container; a container with no GPU is "not stated"; a photo-only positive marked so. Wave 7. Depends on: 1.5f, 1.3c, 1.3a. Source: `modules.md:685`.
- **1.5h `pickup-location`.** (AI step off: question 11). Done: "Collection from Bognor" under a Chichester field resolves with a conflict; no postcode or street in any view; the town fallback; replay writes nothing. Wave 7. Depends on: 1.3a, 1.3c, 1.2a, 1.8f, 0.8, 1.2c, 1.1d, 1.3e. Source: `modules.md:835`.
- **1.8e `want-manager`.** Done: A user cannot read another's wants; the aggregate views, `v_want_areas` included, carry no user IDs; postcode resolution. Wave 7. Depends on: 1.8f, 0.4b, 4.0, 4.3a; `subscriptions` (soft). Source: `modules.md:852`.
- **4.1e `pickup-routes`.** Done: A user cannot read another's pickups; no view over `pickups`; purge on deletion. Wave 7. Depends on: 4.0, 4.3a, 1.8f. Source: `modules.md:987`. search-map-routes draft: folds its own `4.1k` (`pickups`) and `4.1l` (`route-planner`) tasks in here — `pickup_reminders`, `pickup_days`, `route_plans`, `planner_defaults`; the in-house exact solver; secret `PICKUPS_DATA_KEY`; soft-depends also on `router-gateway`, `travel-cost`. Done also: solver order matches a brute-force optimum up to 8 stops; hard windows honoured; encrypted fields round-trip; reminder text carries no address.
- **1.4d `photo-review`.** (gated: actor photo capture, question 11). Done: The photo gold set once the owner approves actor test T4; bytes deleted after the verdict; the expired-link path. Wave 8. Depends on: 1.5d, 1.5e, 1.8e, 1.3c, 1.4a, 1.2c, 1.1d, 1.1c. Source: `modules.md:655`.
- **1.7b `copy-advert`.** backtest and calibration. Done: As `copy-advert.md:651`. Wave 8. Depends on: 1.7a; 1.2e (the test hunt's live checks); question 29 for the control run. Source: `copy-advert.md:651`.
- **1.7e `noise-filter`.** Done: The 14 "5080" mentions; recorded rows 11, 12, 14 and 20; buyer adverts added on purpose; no false wanted or empty-box hits. Wave 8. Depends on: 1.5g, 1.5f, 1.5d, 1.3a, 1.3c, 1.3e. Source: `modules.md:700`.
- **1.7i `demand-signals`.** (built once first-party wants exist: question 17). Done: Suppression below the threshold; no user ID in any output; a copied wanted advert counts once. Wave 8. Depends on: 1.8e, 1.5g, 1.2a; `copy-advert` (soft). Source: `modules.md:805`.
- **1.8k `prepared-message`.** Done: An unstated GPU becomes a question; no seller data; no send path. Wave 8. Depends on: 1.5g, 0.8. Source: `modules.md:927`.
- **1.6d `asking-price-index`.** Done: Split-half stability at n≥10; the worked example's groups; relist and copy collapse; EUR kept apart; "copy collapse unavailable" while `copy-advert` is off. Wave 9. Depends on: 1.5f, 1.5g, 1.3c, 1.3a, 1.3g, 1.7a, 1.7e, 1.2a, 1.3e; `seller-key`, `seller-boosts` (soft). Source: `modules.md:745`.
- **1.7d `copy-advert`.** optional evidence (gated). Done: As `copy-advert.md:653`. Wave 9. Depends on: 1.7a, 1.3f, 1.4d. Source: `copy-advert.md:653`.
- **1.8g `spec-match`.** Done: The RTX 5080 and "Pc" worked examples; ten real wants over stored data; an unstated GPU never `no_match`; the owner's filters and sorts; hints in shadow only. Wave 9. Depends on: 1.8e, 1.5f, 1.5g, 1.7e, 1.3a, 1.8f, 1.7a, 1.3e, 0.8, 4.3a; `multi-quantity-filter`, `pickup-location`, `asking-price-position` (soft). Source: `modules.md:867`.
- **1.6e `asking-price-position`.** Done: The worked example at n=12 and the hidden n=6 case; no row below n=10. Wave 10. Depends on: 1.6d, 1.5g, 1.5f, 1.3c, 1.3a, 1.3e. Source: `modules.md:760`.
- **1.7f `warning-signs`.** Done: Each rule on positive and negative cases, including postage-only on a collection listing; a cheap ask explained by fault wording not flagged. Wave 10. Depends on: 1.3c, 1.5g, 1.6d, 1.3a, 1.3e, 0.8. Source: `modules.md:775`.
- **1.8j `alert-router`.** Done: One alert per copy cluster and per relisted item; photo-only positives to the digest; the rate limit; silence at the baseline, bounded baselines included. Wave 10. Depends on: 1.8g, 1.8i, 1.7a, 1.3g, 1.5g, 1.8e, 1.3e, 1.3d, 1.3b, 4.3a; `subscriptions` (soft). Source: `modules.md:912`.
- **1.7g `suspected-labels`.** Done: No label without evidence; every label starts "Suspected"; the trade-seller case from recorded row 11 in shadow; "too good to be true" candidates from each listing signal, in shadow. Wave 11. Depends on: 1.7f, 1.7a, 1.7e, 1.3c, 1.3e, 1.9b; `pickup-location`, `seller-reply-reports` (soft). Source: `modules.md:790`.
- **1.8l `notifier`.** Done: The claim written before the send; `unknown` never retried; no seller field or location finer than town; freshness from the exact `listedAt`. Wave 12. Depends on: 1.8j, 1.8h, 4.3a, 1.8g, 1.8i, 1.6e, 1.7f, 1.7g, 1.7a, 1.8k, 0.8, 1.3e, 1.8f; `pickup-location` (soft). Source: `modules.md:942`.
- **1.10 rtx3090 end-to-end acceptance.** Done: As `actor-integration.md:448`. Wave 13. Depends on: 1.2e, 1.4b, 1.3d, 1.7a, 1.5d, 1.5f, 1.5g, 1.7e, 1.6d, 1.6e, 1.8g, 1.8j, 1.8l, 1.9a, 1.9b, 1.8e, 0.4b, 1.8f, 0.8, 1.8h, 1.3e, 1.3g, 1.8i, 1.7f, 1.7g, 1.8k, 4.3a. Source: `actor-integration.md:448`.
- **1.9e `ops-metrics`.** Done: Rollup arithmetic on synthetic stamps; copy-advert shadow metrics carry no account data. Wave 13. Depends on: 1.3a, 1.4b, 1.5g, 1.6e, 1.8g, 1.8l, 1.1d, 1.9d, 1.9c, 1.3b, 4.1d, 1.7a. Source: `modules.md:1081`.
- **1.9f `ops-alerts`.** Done: Each rule fires once per key; no repeat within its window. Wave 13. Depends on: 1.2c, 1.1f, 1.1e, 1.9c, 1.3a, 1.1c, 1.8l, 4.3. Source: `modules.md:1096`.
- **4.3c–4.3l `account-integrity`.** (the account-sharing draft's tasks). Done: The catalogue card's tests, merged with the draft's; ships in shadow. Wave 13. Depends on: 4.0, 4.3a, 4.3, 4.9, 1.9d, 1.8e, 1.8j, 1.4c, 1.8l, 1.9b. Source: `modules.md:1402`; `nabvy/docs/design/drafts/account-sharing.md:801-810`.
- **4.12 `seller-rights`.** Done: After erasure only hashes remain; every `erase()` idempotent; suppression applies before erasure finishes. Wave 13. Depends on: 1.3e, 1.9b, 1.1c and every listing-holding module above. Source: `modules.md:1034`.
- **4.11 `output-guard`.** Done: Deliberately bad views and cards fail it, map markers and postcodes included. Wave 14. Depends on: Every module whose `app.` view it checks (`modules.md:1057`). Source: `modules.md:1049`.
- **1.7c `copy-advert`.** user-facing flag. Done: As `copy-advert.md:652`. Wave 15. Depends on: 1.7b, 4.11, 1.3e, 0.7; 4.5 or an admin procedure. Source: `copy-advert.md:652`.

**[CHECK-IN]** after 1.10 (`actor-integration.md:450`), and after 1.7b before 1.7c (`copy-advert.md:651-652`).

New tasks from `docs/design/actor-app-guide.md` (2026-09-24, the `fb-scrap-engine` integration guide and `COPY_ADVERT_SPAM.md`). No task is written for the notified `excludeListingIds` saving (belongs to `check-scheduler`/`details-queue`): the term does not appear in the guide as read; see `docs/questions/actor-app-guide.md` question 1 before adding one.

- **1.3h `listing-ingest`.** Fallback for `listingIds`-only rows (pasted links, rechecks) that carry no city page at all: nearest seeded town within 40 km by coordinates. Done: a synthetic ID-only row with no `locationDetails.reverse_geocode` resolves to its nearest seeded town, never null. Depends on: 1.3a, 1.2a. Source: `actor-app-guide.md`, "What changes" 4; `fb-scrap-engine/docs/APP_INTEGRATION_GUIDE.md:186`.
- **1.7k `copy-advert`.** Reconcile the cluster/label thresholds in `copy-advert.md` with `COPY_ADVERT_SPAM.md`'s hand-labelled S1–S4 tiers, and decide whether the trade-seller and scam label mechanics sit in `copy-advert` or in `suspected-labels`/`warning-signs` as the catalogue currently splits them. Done: owner decision recorded in `docs/decisions.md`; `copy-advert.md`'s threshold section and module-ownership updated to match. Depends on: 1.7a, 1.7g; owner decision (open question 2). Source: `actor-app-guide.md`, "What changes" 10–12; `fb-scrap-engine/docs/design/COPY_ADVERT_SPAM.md`.

Not scheduled in this push: `ebay-adapter`, `ebay-sold`, `cex-adapter`, `gumtree-adapter`, `cross-post-links`, `sold-price-book`, `valuation`, `similar-items`, `ebay-drafts`, `seo-price-pages` (after the MVP); `side-discovery` and the nine "Later" modules; `boosts`, `multi-quantity-filter` (1.7h reserved) and `fake-door` (parked) (`modules.md:171-177`). Their stubs come from 0.7 where an MVP module reads them.

## Draft integrations (2026-09-24)

Backlog rows from the five design-draft integration notes, reconciled against `docs/backlog.md` as it now stands (including the atomic module tasks above). An ID already claimed — by an existing bullet, by the atomic-module-tasks section, or reserved for PR #10's error pages (4.1c) — is not reused for a different module; the row keeps the next free letter in the same series instead. `1.3a` (listing-ingest) and `1.7j` (seller-reply-reports) name the same module as the existing atomic-module-tasks bullet at that ID, so their tests are merged into those bullets above instead of repeated here.

### search-map-routes

- **1.2g `location`: place search, per-place uncertainty and `withinKm`.** Depends on 1.2f. Add `searchPlaces(q, near?)` (town autocomplete, pg_trgm on `v_aliases`); `uncertainty_km` per place on `v_display_places` (bounding-box source, 1-mile floor); `withinKm(origin, displayPlaceId, km)` as the one exported distance predicate; the City/Town display level. Done: all 18 recorded city-page towns found by `searchPlaces` (test); no external calls in tests. After the MVP (marked after the MVP; `router-gateway`'s host is not approved, and this module's own scheduling is open — see `docs/questions.md`).
- **4.1f `listing-search`: filters, sorts and saved filters.** Renumbered from the draft's own 4.1e (that ID now names `pickup-routes`). Depends on 1.2g, 1.5m, `listing-suppression`, `listing-card`, 4.1 (parts/noise/copy-advert/asking-price-position/price-drop/too-good-to-be-true modules optional, their chips hide until they exist). `FeedFilter`/`FeedSort`/`FeedItem`/`MapFeature` contracts; `search_rows` built by batch handlers; `feed_state`, `hunt_views`, `listing_visits` (RLS); one SQL builder anti-joining suppression, card text from `app.v_listing_card`; `erase()` and the account-deleted purge; `search.feed`/`facets`/`saveState`/`markVisited` procedures; chip row, sheets, desktop panel, URL state; "Save as hunt" in `want-manager`. Done: count identical across all sorts; network log shows no coordinates beyond display points; Lighthouse ≥90 on the feed.
- **4.1g Map view (web feature).** Renumbered from the draft's own 4.1f. Depends on 4.1f (`listing-search`), 4.1a. `maplibre-gl` + `@vis.gl/react-maplibre` (+ optional `supercluster`), each with a one-line licence justification; worker copy script and CSP; the Protomaps UK extract on R2 behind a Nabvy-subdomain Worker, OpenFreeMap as the fallback; `search.map` with server-side aggregation above 300 towns at display points; split view, in-house bottom sheet, one-card carousel on phones, radius ring, card/marker linking; accessibility checklist. Done: the dashboard "Deals near you" block reuses the component.
- **4.1h `travel-cost`.** Renumbered from the draft's own 4.1g. Depends on 0.2, 0.3. Dated HMRC-style advisory-fuel/mileage rate rows with source URLs, checked against gov.uk before merge; travel settings UI; `tripCost` and `params`; the "Lowest price + trip" sort switched on. Done: reproduces the worked numbers (£1.31/extra mile, £6.54 at 5 miles) as tests; a quarterly review reminder is in place.
- **4.1i Router VM and `router-gateway`.** Renumbered from the draft's own 4.1h. After the MVP: the self-hosted router host is not approved (owner question). Depends on owner decisions on self-host vs. hosted and the host, the hosting account, secrets `ROUTER_BASE_URL`/`ROUTER_TOKEN`. `infra/router/`: Docker Compose (OSRM + Caddy with a bearer token), the GB build script, the monthly rebuild with a health route and logs off; `router-gateway` module (`table`/`route`/`health`) with `router_calls` holding no coordinates and a switch-off. Done: measured first-build RAM/disk/time recorded in the README; a runbook exists.
- **4.1j `travel-time`.** Renumbered from the draft's own 4.1i. After the MVP (follows `router-gateway`). Depends on 1.2g, 4.1i (`router-gateway`). `road_times` keyed by origin cell, place and OSM build; batch `roadTimes`; ferry detection from `location.landmassFor()` plus OSRM; invalidation on `router.build-changed`; straight-line×1.3 fallback labelled "estimate". Done: the Ryde (Isle of Wight) ferry case is tested.
- **4.1k `deal-hints`.** Renumbered from the draft's own 4.1j. Depends on 4.1f (`listing-search`), 4.1h (`travel-cost`), `asking-price-position`, the too-good-to-be-true modules, `gem-finder`; 4.1j (`travel-time`) optional. Versioned `hint_rules`; shadow mode first (logging through `product-events`), live only on owner approval; the strip after the 6th card and dashed map pills; at most 3 per search; per-hunt alert toggle off by default. Done: Stage A hint rows pass.
- **4.1m Acceptance: the rtx3090 Chichester hunt, end to end.** Depends on 4.1e (`pickup-routes`, folds `pickups`/`route-planner`), 4.1f–4.1k. Stage A in CI, Stage B on staging; calibration of c, v, M and `scale_factor` recorded in the PR and the modules' READMEs. Done: the owner signs off on hints going live.
- **4.1n *(optional, later)* Hints near a planned pickup day.** Depends on 4.1k (`deal-hints`), 4.1e (`pickup-routes`, folds `route-planner`). Detour cost from `pickup-routes`' route planner, computed inside the user's own context; wording "about N miles off {day}'s route"; no pickup data ever leaves `pickup-routes`.
- The draft's own `4.1k` (`pickups`) and `4.1l` (`route-planner`) are not given rows here: coordinator decision 2 folds both into the existing `4.1e` `pickup-routes` bullet above (already updated to carry `pickups`, `pickup_reminders`, `pickup_days`, `route_plans`, `planner_defaults` and the router-gateway/travel-cost soft dependencies).

### listing-reuse

- **0.4a Pack similarity section (packs, contracts).** Add a `similarity` section to the category pack format (variant, tier, containment relations; curator, date, rationale; no numeric performance field) and `product-catalogue.v_similar` to resolve it. Done: pack schema and CI (`output-guard`) reject a performance field; `v_similar` resolves the gpu-pc pack's first curated list.
- **1.3a Sighting origin (listing-ingest).** Merged into the atomic-module-tasks `1.3a` bullet above (same module): "Record the finding term and centre on each sighting. a listing found by two terms carries both origins; existing sighting tests still pass."
- **1.4e Pool-wide detail selection (details-selector).** Renumbered from the draft's own 1.4a (that ID names `details-queue` in the atomic-module-tasks list). Select details candidates against all active wants (`v_want_parts`), not only the triggering hunt, within the existing budget. Done: a by-catch listing matching another hunt's want is selected for details within budget.
- **1.4f Gem-confirm lane (details-queue).** Renumbered from the draft's own 1.4b (that ID names `details-selector`). Add a capped `gem-confirm` lane to the details queue. Done: enqueuing a candidate twice gives one item (key `source + sourceListingId + contentHash`); the lane never exceeds its cap.
- **1.4g Gem-confirm budget share (spend-governor).** Renumbered from the draft's own 1.4c (that ID names `pasted-link-lookup`). Starting cap 2% of the $150 monthly cap ($3.00), priority below pasted links, above routine selection. Done: shadow measures candidate rate, fetches per day and share confirmed; spend never exceeds the cap.
- **1.5b By-catch extraction, rules tier (parts-rules).** Drop hunt/term context from extraction; normalise `+` encoding in titles and descriptions. Done: the MSI AlphaSync fixture (title and description both `+`-encoded) resolves to GTX 1660.
- **1.5c By-catch extraction, AI tier (parts-ai).** Run on by-catch only in pack categories; headsets and other off-pack items get rules only. Done: a by-catch headset never reaches a model call; a by-catch PC in a pack category does.
- **1.6a Sample origin and group health (asking-price-index).** `sample_origin` per member via `product-catalogue.resolve()`; median, MAD, IQR in `stats`; internal `v_group_health` (n, by-catch share, newest ask, centres). Done: a group built entirely from by-catch is flagged in `v_group_health`; median/MAD match a hand-computed fixture.
- **1.6b Robust positions (asking-price-position).** Internal robust z-score in `v_positions`, never in an `app.` view. Done: the worked-example group's z-scores match a hand-computed fixture; no `app.` view gains a new column.
- **1.6c `gem-finder`, in shadow.** New module (card in `docs/design/drafts/modules.md`). Verdicts recorded on the rtx3090 hunt. Done: the gem-maths and veto-boundary fixtures pass; the module ships with its switch in `shadow`.
- **1.7t Noise vetoes (noise-filter).** Renumbered from the draft's own 1.7a (that ID names `copy-advert`) and again past the too-good-to-be-true block (`1.7j`–`1.7s`), to keep that block contiguous. Headset and repair-service fixtures. Done: the recorded run's headsets and "Builder and repair" listing classify as noise, not PC.
- **1.7u Warning-signs threshold view (warning-signs).** Renumbered from the draft's own 1.7b (that ID names `copy-advert`), past the too-good-to-be-true block. Publish its far-below threshold in a view for `gem-finder` to read. Done: the view's cut is calibrated strictly more extreme (≤0.6×median) than `gem-finder`'s 0.75×median cutoff, checked at the end of the rtx3090 shadow run.
- **1.8a Cross-hunt matching and backfill (spec-match).** Backfill on `want-manager.changed`; `match_origin` (`own_search`/`other_search`) on every match. Done: a listing found by term A matches user B's hunt with the same `eligible_at` as the triggering user; backfill is in-app and digest only.
- **1.8b Fair release batching (alert-router).** One release batch per listing version; send order shuffled per batch, no tier-based delay. Done: a test proves the triggering hunt gets no priority within a batch.
- **1.8c `similar-picks`.** New module (card in `docs/design/drafts/modules.md`). Curated alternatives for each hunt from the pool. Done: only curated relations appear; the similarity and wording fixtures pass.
- **1.8d Hunt alternative controls (want-manager).** Alternatives off, variants only, or variants plus one tier either side; PC containment off by default; max price; instant alerts off (alternatives) and on (top picks) by default. Done: a hunt with alternatives off shows no "also found" section; a hunt with PC containment off never gets a PC pick. Needs the `preferences`-shape decision recorded on the `want-manager` card (per-hunt vs. per-user) before this task starts.
- **1.9g Pool metrics (ops-metrics).** Renumbered from the draft's own 1.9a (that ID names `switches`). By-catch share, on-target versus by-catch median gap, `gem-confirm` spend. Done: a gap over 15% in many groups is surfaced for the owner.
- **4.1l Top picks and "also found" UX.** Renumbered from the draft's own 4.1c (reserved for PR #10's error pages). Playwright covers the top-picks strip, the "Top pick" badge, the hunt-page "also found" section and its controls. Done: Playwright passes; no "worth"/"fair"/performance wording appears (`output-guard`).

**[CHECK-IN]** after 1.6c: review shadow verdicts and cost with the owner before 4.1l goes live.

### too-good-to-be-true

- **1.7j `seller-reply-reports`: core in shadow.** Merged into the atomic-module-tasks `1.7j` bullet above (same module): "Scaffold with `pnpm new:module seller-reply-reports`; the tables, views, events and functions of §6.2; the gate on `notifier`'s open record, weights, levels, second answers, per-member location distance, burst and counter-report holds, report-then-buy, carried relist reports, bans and withdrawal. Done: stages `gate`, `aggregate`, `spread` and `privacy` pass; the `v_reporter_signals` grants are tested; ships `shadow`."
- **1.7k `warning-signs`: too-good-to-be-true text facts, in shadow.** Depends on 1.7f (the `warning-signs` core task), 1.3c, 1.5l. Done: the codes of §6.5 with rule IDs, versions and clause-level exclusions; `deposit_request` replaced by `pay_first_text`; the `low_ask_explained` reasons; ships `shadow`.
- **1.7l `suspected-labels`: the `too_good_to_be_true` type in shadow.** Depends on 1.7g (the `suspected-labels` core task), 1.7k, 1.7j, 1.5l, 1.7a; `asking-price-position`; `listing-assessment`/`parts-record` (soft). Done: the rules and modes of §6.3 including B-P as review-only; `evaluations`, `candidates`, `approvals`, `correction_requests` and `reviews`; every §8.1–8.3 case passes, hard negatives at 100%; ships every path in `shadow`.
- **1.7m Shadow calibration on the rtx3090 hunt.** Depends on 1.7l, 1.10 (the hunt running), 4.5b (labelling UI). Done: a calibration report in the `suspected-labels` README (per-path precision with Wilson intervals); labelled cases added as fixtures; ends with the owner's go-live decision per path (1.7n).
- **1.7n Go-live: user-facing mark, report a mistake, reviewed mode.** Depends on 1.7m; owner decisions recorded in `docs/decisions.md`; `output-guard`; `listing-suppression` on. Done: `app.v_suspected_labels` rows under the §6.3 conditions; `approve()`; `requestCorrection()` in-app and from the public form; the output-guard checks of §8.5 added and passing.
- **1.7o `notifier`: mark, report and correction buttons.** Depends on 1.8, 4.3g, 1.7l. Done: `/go/<token>` extended to card and pasted-link opens; renders only `app.v_suspected_labels` rows; Telegram callbacks call the report procedure; one pull request in `services/notifier`.
- **1.7p `want-manager`: "Hide suspected" and alert opt-out preference keys.** Depends on 1.7l. Done: one pull request in `services/want-manager`.
- **1.7q `spec-match`: apply "Hide suspected".** Depends on 1.7p. Done: applies the preference from `app.v_suspected_labels`; one pull request in `services/spec-match`.
- **1.7r `alert-router`: alert opt-out and the gem-candidate wait.** Depends on 1.7l, 1.7p. Done: reads the alert opt-out and the §2.1 gem-candidate wait (up to 2 minutes); one pull request in `services/alert-router`.
- **1.7s `listing-feedback`: bought verdicts for report-then-buy.** Depends on the `listing-feedback` core task. Done: `v_bought_for_reports` granted only to `seller-reply-reports`; a db test on the grant.
- **4.1o Web UI for the mark and reports.** Renumbered from the draft's own 4.1m (that ID now names the search-map-routes rtx3090 acceptance task). Depends on 4.1, 4.1c, 1.7j, 1.7l, 1.7o. Done: the report bar and the one-tap sheet with its second questions; "Your reports" with withdraw; the chip and panel; "Report a mistake" in-app and at `nabvy.com/report-a-mistake` with Turnstile; the Playwright checks of §8.5 pass.
- **4.3p `account-integrity`: report-abuse rules and linked groups.** Depends on 4.3c, 4.3d, 4.3i, 4.3j, 1.7j. Done: `linkedGroupOf(userIds)` returning opaque group IDs; the report-abuse rules of §7, versioned, in `shadow`; the steps weight-0, reporting disabled, then fair-use suspension; nothing acts until the owner approves thresholds (question 37).
- **4.5b `review-console`: queues for "too good to be true".** Depends on 4.5, 1.7j, 1.7l. Done: queues for approvals, correction requests, sub-threshold reports, `other` reports, burst and counter-report holds; outcomes call `applyCorrection()`, `approve()` and `resolve()`, are audited and export fixtures.

### account-sharing

Detailed breakdown of the single `4.3c–4.3l` bullet in the atomic-module-tasks section above, which already reserves that ID range for "the account-sharing draft's tasks"; these rows are that detail, not a renumbering, and their IDs are unchanged.

- **4.2a Name the MIT push sender before 4.2 starts.** Needs D14 (open). Docs only, merged before 4.2 starts. `docs/architecture.md`, `docs/secrets.md` and `docs/backlog.md` 4.2 name `@pushforge/builder` 2.0.5 (MIT) as the push sender. Done: `web-push` appears in no `package.json` and not in `pnpm-lock.yaml`.
- **4.3c `account-integrity` foundation.** Needs 0.2, 0.3, 4.0. Module scaffolded; `account_status`, `sessions`, `devices`, `enforcement_actions` tables; `v_account_status`, `v_session_state`; the oRPC middleware checks status and session state on every request; the N1–N4 notice enum. Done: a revoked session is refused on its next request; `off` mode allows everything.
- **4.3d Sessions, devices and the Devices page.** Needs 4.3c, 4.3a. Device cookie; auth's hooks call the module; session mirror plus activity buckets; device cap and picker; "Sign out everywhere"; `/app/account/devices` with sign-out/sign-out-others. Done: Playwright covers the picker, "Not me", "Sign out everywhere" and sign-out.
- **4.3e Live-screen lease ("Use here").** Needs 4.3c, 4.1. `live_leases`/`lease_events`; heartbeat and lapse; silent takeover when the holder is hidden/idle/lapsed; feed/map/search gated, deal cards and alerts not. Done: Playwright runs two browser contexts and the laptop is never signed out.
- **4.3f Telegram binding hardening.** Owning module: `account`. Needs 4.3a, D6 (open). Unique constraints on user and chat; single-use 10-minute link codes; `channel.linked`/`channel.unlinked` events. Done: grammY tests for group-add-then-leave, private-only handling, reused-code refusal.
- **4.3g Protected delivery and signed links.** Owning module: `notifier`. Needs 1.8, 4.3c, D7, D8. `chatType('private')` only; `protect_content: true`; `/go/<token>` that never creates a session; `alert_opens` plus `v_alert_open_context`. Done: Playwright — the recipient opens a link while signed in, 3 other contexts open it, a fifth gets the sign-in page.
- **4.3h Push bound to devices.** Owning module: `account`. Needs 4.2, 4.3d. Each push subscription carries `device_id`/`session_id`; subscriptions deleted or paused on `devices.revoked`. Done: signing a device out stops its pushes.
- **4.3i Signals and risk score (shadow only).** Needs 4.3d, 4.3e, 4.3g. The signal catalogue as versioned `rules`; 5-minute and nightly Trigger.dev batch tasks; decay and bands. Done: an account that only receives Telegram alerts is still scored.
- **4.3j Enforcement ladder, notices and admin tools.** Needs 4.3i, 4.8. L0–L4 state machine with pacing; `enforcement_actions` and audit rows; `/admin/integrity`; everything ships in `mode = shadow`. Done: each consumer module's own fixture test passes.
- **4.3k Approval from an existing device.** Needs 4.3d, 4.3i. Spike on Better Auth's Device Authorization plugin, or the in-house `device_approvals` table as fallback. Done: Playwright covers approve, deny and both fallbacks.
- **4.3l Rollout gates and metrics.** Needs 4.3j. Per-rule mode switching; the honest-cohort list; weekly label sampling; automatic demotion. Done: a rule with an honest-cohort hit drops to shadow automatically.
- **4.3m Paid extra seat.** Needs 4.3, D5 (open). Blocked on the owner's decision: whether a paid extra seat exists, its name and its price.
- **4.3n Invisible per-recipient variation (optional).** Needs 4.3g, D10 (default: off). HMAC-selected equivalent layouts with no zero-width characters and no changed facts. Done: ships disabled; unit tests on the decoder and the accessibility check.
- **4.3o Trial eligibility.** Owning module: `subscriptions`. Needs 4.3j, D22 (open). `accountIntegrity.checkTrialKeys()` called from the subscription-created hook. Done: a second account with the same card gets no trial.

### listing-location

- **1.1g Sighting redaction of postcodes.** Renumbered from the draft's own 1.1f (that ID names `source-health`). Depends on 1.1b. `redact_text_v2` (or a v3) masks the inward half of full postcodes in any case, with or without the space, and with O/0 or I/1 in the outward code. Done: SQL tests cover "po21 1ab", "PO211AB" and "P021 1AB"; over-masking spec strings such as "M2 1TB" is accepted; `redaction_leaks` is extended to the same patterns.
- **1.2f `location`: UK reference data and point-to-point distance.** Depends on 1.2a, 1.9a, 1.9b. A quarterly, non-migration loader loads GB postcode districts and sectors from ONSPD, populated places from OS Open Names, OSNI place names, island/county regions, NI Local Government District boundaries, aliases and a stop-list. Provides `v_districts`, `v_sectors`, `v_places`, `v_display_places`, `v_aliases`, `v_stoplist`, `v_attributions`, `distanceKm(a,b)`, `pointForCityPage()`, `nearestDisplayPlace()`, `landmassFor(point)` and a sector-based `pointForPostcode()`. Done: E1, BH16 and PO21 resolve; no BT rows loaded; attribution strings checked against each release's own notice.
- **1.5l `pickup-location`: rules pass in shadow.** Depends on 1.2f, 1.3a, 1.3c, 1.2a, 1.9a, 1.9b. Build the card and detail resolution passes (normalise, postcode extraction, gazetteer matching, phrase-role classifier, decision table, conflict detection, confidence levels, display-area rule); the `resolutions`, `current`, `candidates`, `mentions`, `handover`, `page_stats` tables; the `pickup-location.resolved`/`.changed` events. Done: all 20 recorded rows match their expected resolution; the noise suite gives 0 false places; ships `shadow`.
- **1.5m `pickup-location`: user-facing view and consumer contract.** Depends on 1.5l, `listing-suppression`. `app.v_pickup_location` with the §4.3 column list, grants and the suppression anti-join; the `output-guard` location checks and the `quote-redaction` postcode/street-number masking change land alongside it; `listing-card` reads `area_label`. Done: the §5.8 off/shadow fallback passes through `spec-match`'s fixtures with this module off.
- **1.5n `pickup-location`: AI fallback.** Depends on 1.5l, 1.1d, 1.2c. Contracts for the AI input/output; the post-call checks of §3.9; the queue's real-time and batch lanes; `spend-governor` and `cost-meter` wired in; a cache on `(locationHash, promptVersion)`. Done: recorded-output replay fixtures pass; a weakened prompt fails the evaluation run.
- **1.5o `pickup-location`: labelled set, calibration and go-live.** Depends on 1.5l, 1.5n, 1.1g, 1.9d (the rtx3090 hunt running). At least 150 hunt listings exported redacted and hand-labelled, 200+ cases in total. Done: a report gives the owner's failure modes as rates with Wilson intervals; the switch moves to `on` only once targets hold.
- **4.1p Consumers: filters, map, hints, alerts and the planner's read-only area hint on resolved locations.** Renumbered from the draft's own 4.1c (reserved for PR #10's error pages). Depends on 1.5m, 4.1a, `spec-match`, `alert-router`. Distance filters and nearest-first sorting measure from the display point, never from coordinates; map markers group by area with approximate/conflict styles; worth-the-trip hints are limited to non-approximate areas and never cross a ferry crossing; the pickup planner shows a read-only area hint and never fills the address field. Done: a Playwright check shows the network log carries no coordinates other than display points and no inward postcode.

### ID mapping

| Draft ID | Final ID | Module |
| --- | --- | --- |
| search-map-routes 1.2g | 1.2g | `location` |
| search-map-routes 4.1e | 4.1f | `listing-search` |
| search-map-routes 4.1f | 4.1g | Map view (web feature) |
| search-map-routes 4.1g | 4.1h | `travel-cost` |
| search-map-routes 4.1h | 4.1i | `router-gateway` |
| search-map-routes 4.1i | 4.1j | `travel-time` |
| search-map-routes 4.1j | 4.1k | `deal-hints` |
| search-map-routes 4.1k | (merged into 4.1e) | `pickup-routes` |
| search-map-routes 4.1l | (merged into 4.1e) | `pickup-routes` |
| search-map-routes 4.1m | 4.1m | acceptance (cross-cutting) |
| search-map-routes 4.1n | 4.1n | `deal-hints`/`pickup-routes` (optional) |
| listing-reuse 0.4a | 0.4a | `product-catalogue` (pack format) |
| listing-reuse 1.3a | 1.3a (merged) | `listing-ingest` |
| listing-reuse 1.4a | 1.4e | `details-selector` |
| listing-reuse 1.4b | 1.4f | `details-queue` |
| listing-reuse 1.4c | 1.4g | `spend-governor` |
| listing-reuse 1.5b | 1.5b | `parts-rules` |
| listing-reuse 1.5c | 1.5c | `parts-ai` |
| listing-reuse 1.6a | 1.6a | `asking-price-index` |
| listing-reuse 1.6b | 1.6b | `asking-price-position` |
| listing-reuse 1.6c | 1.6c | `gem-finder` |
| listing-reuse 1.7a | 1.7t | `noise-filter` |
| listing-reuse 1.7b | 1.7u | `warning-signs` |
| listing-reuse 1.8a | 1.8a | `spec-match` |
| listing-reuse 1.8b | 1.8b | `alert-router` |
| listing-reuse 1.8c | 1.8c | `similar-picks` |
| listing-reuse 1.8d | 1.8d | `want-manager` |
| listing-reuse 1.9a | 1.9g | `ops-metrics` |
| listing-reuse 4.1c | 4.1l | web UI (`similar-picks`/`gem-finder`) |
| too-good-to-be-true 1.7j | 1.7j (merged) | `seller-reply-reports` |
| too-good-to-be-true 1.7k | 1.7k | `warning-signs` |
| too-good-to-be-true 1.7l | 1.7l | `suspected-labels` |
| too-good-to-be-true 1.7m | 1.7m | calibration (cross-cutting) |
| too-good-to-be-true 1.7n | 1.7n | `suspected-labels` go-live |
| too-good-to-be-true 1.7o | 1.7o | `notifier` |
| too-good-to-be-true 1.7p | 1.7p | `want-manager` |
| too-good-to-be-true 1.7q | 1.7q | `spec-match` |
| too-good-to-be-true 1.7r | 1.7r | `alert-router` |
| too-good-to-be-true 1.7s | 1.7s | `listing-feedback` |
| too-good-to-be-true 4.1m | 4.1o | web UI (cross-cutting) |
| too-good-to-be-true 4.3p | 4.3p | `account-integrity` |
| too-good-to-be-true 4.5b | 4.5b | `review-console` |
| account-sharing 4.2a | 4.2a | docs |
| account-sharing 4.3c–4.3o | 4.3c–4.3o (unchanged) | `account-integrity` / `account` / `notifier` / `subscriptions` |
| listing-location 1.1f | 1.1g | `apify-gateway` |
| listing-location 1.2f | 1.2f | `location` |
| listing-location 1.5l | 1.5l | `pickup-location` |
| listing-location 1.5m | 1.5m | `pickup-location` |
| listing-location 1.5n | 1.5n | `pickup-location` |
| listing-location 1.5o | 1.5o | `pickup-location` |
| listing-location 4.1c | 4.1p | web feature (`spec-match`/`alert-router` consumers) |

## Abuse threat model gaps (4.3t, 2026-09-24)

From `docs/design/abuse-threat-model.md` ("Gaps"); each row names its gap and threat rows.

- **4.3v `account-integrity`: free-tier guards hard from day one.** G1 (A1, A7, F3): sign-up limits, admission and the card check act in every mode, never shadow; with the module off or in shadow no new free window is admitted, paid users unaffected. The admission queue takes only email-verified, Turnstile-passed sign-ups, with a maximum depth and expiring slots. Done: `integrity-off-holds-free`, `queue-flood-starves-genuine`. Depends on: 4.3q, 4.3s. Sonnet.
- **4.3w `account-integrity`: canonical email and trial keys that survive deletion.** G2 (A2, A3): plus-tags, Gmail dots and case canonicalised before hashing; `trial_*` evasion keys kept after `account.deleted`; trial keys include the Telegram chat; lifetime cap and used windows follow the linked group. Done: `email-canonical`, `rejoin-after-delete`. Depends on: 4.3q, 4.9a. top.
- **4.3x `account-integrity`: card-check hardening.** G3 (B1, B2): prepaid funding refused; attempts capped per account, IP and device; Stripe Radar block rule; SetupIntent only after a verified email. Done: `card-check`, `card-testing`. Depends on: 4.3q, subscriptions. Sonnet.
- **4.3y `account`: Telegram hardening.** G4 (C1, C2, C4): wrong-code attempts capped per `chat_id`; private chats only; secret-token header compared in constant time; `update_id` dedupe. Done: `link-code-guess`, `telegram-webhook`, `telegram-private-only`. Depends on: account (done). Sonnet.
- **4.3z `auth`: magic-link send cap and admin step-up.** G5 (A6, F1): a global magic-link send cap per hour; admin actions need Google 2-step verification or a passkey, re-authenticated within 15 minutes, the elevated session bound to its device. Done: `magic-link-flood`, `admin-step-up`. Depends on: auth. top.
- **1.2s `spend-governor`: reservation sizing and an atomic gate.** G6 (D4–D6): a reservation derived from the run's own cap (`maxRequests`, `maxRunSeconds`), at most 3 times measured cost with a 45% uplift; gate and reservation in one locked transaction. Done: `reservation-sized`, `reservation-covers-cost`, `gate-race`. Depends on: 1.2p. top.
- **1.2t `spend-governor`: free pool inside the monthly cap.** G7 (A4, D7, F2): a monthly free-pool ceiling as a share of the provider cap; a lone-area sub-pool; risk-weighted admission; config ceilings on every policy row; a same-area surge of free accounts held. Paid sign-ups, upgrades and funded watchers are never queued or held by free-tier guards (owner, 2026-09-24). Done: `pool-starvation`, `pool-vs-cap`, `policy-ceiling`, `funded-area-piggyback`, `day-boundary-double-spend`. Depends on: 1.2o, 4.10a. top.
- **1.2w `check-scheduler`: free burst cost fixed per window.** G8 (A5): a free want's window costs the same whatever its radius; cadence scales down with areas. Done: `free-wide-radius`. Depends on: 1.2r. Sonnet.
- **4.9b `usage-ledger`: prepaid watching and a user spending limit.** G9 (B7, D1): credits reserved per check before submit; a want pauses at zero; a monthly spending limit each user sets, on by default; the reserve is locked per user. Done: `prepaid-watch`, `wide-paid-want`. Depends on: 4.9a, check-scheduler. top.
- **3.3a Scan and pasted-link spend.** G10 (E3–E5): every scan and paste charged or counted to the free cap and gated; pasted URLs parsed to (source, listing ID), never fetched; 24-hour cache per listing; a pixel cap before decoding. Done: `paste-cost`, `scan-quota`, `upload-pixels`. Depends on: pasted-link-lookup, scan-recognition, 1.2p. Sonnet.
- **1.3i `listing-ingest`: first-seen written once.** G11 (C5, D3): `first_seen_at` set once per (source, listing ID); alerts deduped per (user, listing, kind); a replay with a fresh key re-announces nothing. Done: `replay-first-seen`, `alert-dedupe`. Depends on: listing-ingest (done), alert-router, 0.9a. top.
- **4.9c `usage-ledger`: dispute clawback and reversal rules.** G12 (B4, B5): a disputed top-up freezes its unspent credits and holds paid watching; a reversal needs a failed job ID. Done: `dispute-clawback`, `reversal-only-provider-failure`. Depends on: usage-ledger, subscriptions. Sonnet.
- **4.9d `attribution`: referral credit rules.** G13 (B3): credit only after the referee's paid purchase clears its dispute window, never within a linked group, capped per referrer per month. Done: `referral-self`. Depends on: attribution, account-integrity. Sonnet.
- **1.2u `spend-governor`: failure-rate breaker and retry budget.** G14 (D2): a run shape failing repeatedly holds and alerts; resubmits per area and shape capped an hour. Done: `retry-storm`. Depends on: 1.2q. Sonnet.
- **5.4b API key spend limits.** G15 (E2): calls with a key charge the ledger, pass the gate and stop at the key's daily limit; the key is shown once with a scanner-friendly prefix. Done: `api-key-spend`. Depends on: 5.4a. Sonnet.
- **4.3aa `account-integrity`: bulk-read rule.** G16 (E6): reading more than the policy's listings a day per account or linked group slows, then holds; hard for free and signed-out traffic from launch, shadow first for paid and API. Done: `bulk-read`. Depends on: account-integrity. Sonnet.
- **4.3ab `account-integrity`: farm signals, score and ladder.** From the owner's addition (17:07): velocity per IP, /24 and ASN; device reuse; email pattern and domain age; disposable and catch-all domains; the same want and area across new accounts; time-of-day clustering; card reuse. These are scored per account and linked group into the ladder (queue, re-verify, early card check, hold, ban with evasion links), in hard mode from day one. Done: the `farm-*` fixtures and the honest cohort never passing step 2. Depends on: 4.3q, 4.3s, 4.3v. top.
- **4.3ac Web layer: per-route quotas and extraction anomalies.** Daily quotas per account, route and API key in the oRPC middleware counter, besides the per-minute limits; anomaly rules on volume and diversity (areas, products, IPs); every refusal is a product event for `account-integrity`. Done: a scripted-read fixture is slowed, then held; a normal user's week passes. Depends on: product-events, account-integrity. Sonnet.
- **4.3ad `account`: export variation.** Exports are capped a day and carry per-account ordering and a keyed row marker, so a leaked export traces to its account. Done: two accounts' exports of the same rows differ and each traces back. Depends on: account (done). Sonnet.
- **1.2v `spend-governor`: £2 a day per free account and pool slow-down.** Owner (`docs/decisions.md`, "Free-tier limits, paid users and the daily cap"; free tier only): every free account costs at most £2 a day (a policy row edited in admin; paid accounts have no daily cap), counting bursts, scans, pastes, details runs and model calls at attributed cost; checked atomically in the gate before submit. As the daily free pool drains, the remaining accounts slow down (cadence steps as policy rows) rather than stop at once. Done: `scan-quota` (43,200 scans stop at £2), `pool-share` (a £20 pool lets at most 10 accounts run dry; the 11th slows). Depends on: 1.2p, 1.2s, 4.10a. top.
- **4.3ae Admin panel hardening and adversarial audit.** G18, `docs/design/admin-hardening.md`: step-up, device-bound elevated sessions, per-action confirmation for caps and switches, config ceilings, audit rows that cannot be skipped, rate limits and founder alerts on admin writes; an adversarial audit of the built panel before the free tier opens. Done: the fixtures in that file; the audit's findings fixed. Depends on: auth, audit-log, pricing-console, switches. top.
- **4.3af Web: admin gate before any deploy.** Audit A1, A2, A11, A12 (`docs/design/admin-hardening.md`): `requireAdmin` in every admin page, procedure and action; the kill switch read-only until it calls an audited procedure; admin fixtures `server-only` and refused in production; no client-session gating. Done: e2e shows 401/403 on `/admin` and `/admin/review` for anonymous and user sessions. Blocks 0.5a going live. Depends on: auth (done). top.
- **4.3ag Database: switch, role and audit coupling.** Audit A3, A4, A8: `switches.set_switch` as `security definer` with an admin check and an audit row in one statement, and pipeline write grants revoked; a trigger that audits every role and ban change on `better_auth."user"`; audit inserts by column with `at = now()` and an allow-list of actions per role. Done: SQL tests where a direct update without the audited path is refused. Depends on: switches, auth, audit-log (done). top.
- **4.3ah `auth`: admin account guards.** Audit A5–A7, A9, A10: bootstrap only when no admin exists; the session list without tokens, and audited; `account_active` in the admin check; ASCII-only founder emails; a last-admin guard, a second approver for actions on admins, and a required reason. Done: a fixture for each. Depends on: auth (done). top.
