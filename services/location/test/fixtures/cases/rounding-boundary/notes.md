Synthetic: the same point twice (distance exactly 0, rounds to 0) and two London points about
3.8 km apart east-west at that latitude, which rounds up to the nearest 5 km. Exercises
`roundToNearest` at its low boundary; `test/domain.test.ts` covers the .5-of-a-step tie directly.
