# pin-first-and-last

Synthetic. Four stops with no agreed times; the user pinned Littlehampton first ("Go here first")
and Portsmouth last ("Go here last", §6.4). The solver keeps both pins and orders the middle two
(Bognor, then Emsworth) by least driving. Without the pins the cheapest tour would start at the
nearer Bognor.
