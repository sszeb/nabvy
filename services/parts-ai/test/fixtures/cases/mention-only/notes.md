# mention-only

Synthetic, from recorded row `1639721697586478`. "Upgraded to an RTX 4070 so this has to go" and
"Graphics card not included": the rules leave the GPU as `mention_only`, the model is asked and
reads the 4070 as a `mention`. It is stored with its catalogue ID and `mention`, never `offered`:
a part that is only mentioned is never included (`PARTS_INTELLIGENCE.md:374-376`).
