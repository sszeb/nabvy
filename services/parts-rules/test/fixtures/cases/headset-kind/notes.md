# headset-kind

Recorded rows `1154042653756525` and `2126837844711748`: gaming headsets "for PC". The title
matches `pcTitle` ("PC") and `notAPcTitle` ("Headset", "Xbox"); the rules do not pick one, so
the kind is a gap (`conflict`) for the model pass, and the core parts are listed as not stated.
