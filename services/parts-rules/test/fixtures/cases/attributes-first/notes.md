# attributes-first

Recorded row `1352645884594841`: the seller filled the structured attribute "Processor type:
AMD Ryzen 5 3600" (in both `attributes` and `detailSections`, read once). It is read before the
title and description, so it is hit 0, source `attribute`. The "(e.g. a hard drive, I had one in
there myself)" storage-type hit is kept: storage type is not a core part and the model pass sees
the quote.
