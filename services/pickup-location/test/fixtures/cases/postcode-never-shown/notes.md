# A full postcode in the text

The card's second case: the postcode's point (given to the test as the lookup's answer; in production `location.pointForPostcode()`) decides agreement, its district is the only form shown, and the display point is the nearest gazetteer place. The suite checks every view for the postcode and its inward code.
