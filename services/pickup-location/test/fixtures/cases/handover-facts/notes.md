# Handover facts: the field first, the text adds

The field says in person (collection yes) and the text says postage only: the text never overrides the field, so collection stays yes, and the postage-only and courier-only flags are recorded for "too good to be true" (docs/decisions.md).
