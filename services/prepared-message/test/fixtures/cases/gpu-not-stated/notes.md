# gpu-not-stated

The card's test: an unstated GPU becomes a question. listing-assessment's case of the same name
(recorded row 28242423458759790 with its GPU line removed): a container by parts, GPU
`not_stated`, `gpu` its one unknown. The message asks for the GPU; the checklist also lists the
CPU, RAM and storage the listing states, in part order. `ram_generation` has no question in the
template, so no check.
