# nothing-to-ask

Synthetic. No message when nothing is unknown: a placed part that is not a container (its stored
unknowns never reach `v_unknowns`), a container that states every core part, and a GPU a reviewer
has since stated (listing-assessment's correction; `v_unknowns` leaves it out).
