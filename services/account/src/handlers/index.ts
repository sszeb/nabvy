// Event handlers. account.README.md, "Inputs": web forms, the Telegram bot's link callback and
// setStanding() calls from account-integrity — no event this module consumes yet, so there is
// nothing to register here. Scaffolded per the module shape (CLAUDE.md).
export {}
