# free-ebay-call

Synthetic. eBay and CeX calls cost nothing but are counted against their daily limits
(`docs/engineering.md`, "Cost metering"), so each is one row, settled at zero when recorded.
A failed call is counted too. The module names are placeholders for modules not yet built.
