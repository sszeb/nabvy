# worked-pc-5060

The brief's second worked example, listing `29056633657273875`, titled "Pc", with an RTX 5060 in
the description only (`fb-scrap-engine/docs/design/CONTAINER_LISTINGS.md:6-9`;
`docs/fb-actor-reference.md`, section 8.3). Synthetic over the recorded row `2264485254402498`
(the run's RTX 5060 PC, whose specs sit in the title): the title is cut to "Pc" and the specs
moved to the description, with the brief's listing ID. Expected: kind `pc`; the GPU, CPU, RAM
and storage recorded from the description, the card resolved to the catalogue; no AI call, as
the rules leave no gap.
