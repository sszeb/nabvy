The recorded run `VkryjpwS6U2GBDh3k`'s own `run-summary.json.detailRoute`: 19 of 19 replays
succeeded, below the 50-attempt minimum, so the region stays on graphql as `insufficient-data`
rather than `healthy` (card: "the recorded run (`insufficient-data`, 19 of 19)").
