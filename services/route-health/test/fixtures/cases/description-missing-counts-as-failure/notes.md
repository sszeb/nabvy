Pinned caveat, decision pending (`docs/questions.md`, "route-health and listings without a
description"): a listing with no description counts as a failed replay, unchanged from the actor.
50 replays, 3 counted as failed (47 ok, 94%), below the 95% bar, so the region switches to `page`
with an alert — the same outcome the owner's `docs/questions.md` entry describes for the actor's
own live check (47 of 50).
