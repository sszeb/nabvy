A user who changes their mind (real_deal, then not_a_deal on the same listing, no alert) replaces
the stored verdict rather than adding a second row: one identity, `(user_id, listing_id, no
alert)`, one row.
