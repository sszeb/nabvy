A verdict given directly on the listing (no `alertId`) and a verdict given from an alert are
different identities for the same listing, so both rows are kept (card: "verdict ... alert_id").
The `bought` verdict also appears in `v_bought_for_reports` (the report-then-buy abuse exemption).
