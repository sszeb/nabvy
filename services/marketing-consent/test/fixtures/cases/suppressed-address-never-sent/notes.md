The module's core acceptance test (module card, "Tests and fixtures"): even with full consent and
an active account, a bounced or complained-about address is never mailed. `isSuppressed()` gates
`canMarket()` regardless of consent.
