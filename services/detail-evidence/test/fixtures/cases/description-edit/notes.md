# description-edit

Synthetic, from the recorded run: row 1 (`1756692548940192`, no attributes, no condition) is
collected again a day later with its description rewritten. The description is in the evidence
hash, so it is a new version (21 in all), the listing is announced as `changed`, and `v_current`
shows the new version and its text. The first version is kept (every version is kept).
