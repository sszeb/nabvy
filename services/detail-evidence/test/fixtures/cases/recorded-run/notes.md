# recorded-run

The recorded run `VkryjpwS6U2GBDh3k` has 20 `listing` rows, all with details fetched on the
graphql route, and one `sourceOutcome` row (skipped). Expected (the card): 20 versions, all
`full_verified` (`run-summary.json`, `descriptionStatuses`), 20 fetches, 20 listings announced as
changed (each is new), none unresolved.

Row 0 (`1816901372840238`) is the session page, the only row with a gallery: 4 photos,
`photoGalleryTotal` 4, `photoGalleryComplete` true. Its Condition attribute gives the machine value
`used_good` (label "Used – good"); `categoryPath` is null in the row, stored as empty; coarse
coordinates from `locationCoordinates`. The fixture's photo links are redacted and carry no `oe`
expiry, so the links expire 104 hours after `collectedAt` (2026-09-24T01:40:43.415Z).
