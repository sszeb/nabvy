# status-two-quick-misses (synthetic)

Two misses within five hours: the missed-sweeps count reaches 2, but the listing was observed less
than 24 hours ago (`LISTING_LIFECYCLE_NOT_SEEN_MIN_HOURS`), so it stays `live`.
