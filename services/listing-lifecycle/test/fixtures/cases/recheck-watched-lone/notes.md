# recheck-watched-lone (synthetic)

A lone watched listing waits for its batch, then goes on its own once it has waited a day past its
due time (`LISTING_LIFECYCLE_WATCHED_MAX_WAIT_HOURS`), so it is still refreshed.
