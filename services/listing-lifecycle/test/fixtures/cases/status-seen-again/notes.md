# status-seen-again (synthetic)

After `status-missed-two-sweeps`, a later sweep returns row 0 again: it is `live` again, with no
missed sweep since, and announced a second time. `not-seen-recently` was never a sale.
