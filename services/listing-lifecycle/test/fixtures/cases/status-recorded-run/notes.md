# status-recorded-run

The recorded run `VkryjpwS6U2GBDh3k` (20 listing rows, every `availability.live` true,
`dataset.json`) as one search job, ingested and recorded, then one tick an hour later. Every listing
is `live` on its search card, and each is announced once as a new status. No recheck is sent.
