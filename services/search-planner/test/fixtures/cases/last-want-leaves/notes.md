# No base terms without a hunt

Centre 2002 had wants before; its last want went, so want-manager's view no longer lists it. It has
no pair and no plan, so nothing runs for it (`docs/decisions.md`, "Nothing runs unless a user
asks": areas and products nobody hunts cost nothing).
