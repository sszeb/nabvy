# The owner's test hunt before want-manager is on

The owner's team tests on a real hunt for "rtx3090" at Chichester, a verified centre
(`docs/decisions.md`, "Beta coverage and Apify budget"). Until want-manager is on it enters as one
audited admin pair with origin `admin-test` (card; `actor-integration.md` task 1.2b). The admin
enters the term; this case uses the owner's own spelling. It runs first and alone.
