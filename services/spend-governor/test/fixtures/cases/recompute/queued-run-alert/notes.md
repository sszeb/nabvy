# queued-run-alert

Synthetic: one run queued at the gateway with a $70 reservation, not yet in the ledger (the watcher meters a run after it starts). It counts at its reservation: 82% of the $85 plan ceiling raises `slow-free` and one alert; 47% of the $150 budget stays `none`. The replay publishes nothing.
