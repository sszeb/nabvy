# recorded-run-settled

The recorded run (fixtures/listings/facebook/runs/2026-09-24-VkryjpwS6U2GBDh3k/run.json) settled at $0.0177: it counts at its settlement. It is a settled run of the month whose proxy GB `v_jobs` does not publish, so the proxy budget is unmeasured (level none, reason unmeasured).
