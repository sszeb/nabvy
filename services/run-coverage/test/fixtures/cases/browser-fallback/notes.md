# browser-fallback

Synthetic summary: the recorded search with `route: browser-fallback`, which reads about one page yet still reports `sourceBinding: verified` (README.md:103-105; EVIDENCE_LEDGER.md:98-106 as cited by the card). Degraded whatever the binding; no baseline.
