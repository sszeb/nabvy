# route-failed

Synthetic summary: `route: failed` (the HTTP route failed, or the run deadline came first; README.md:103-105), with stop `time-limit`. The route makes it degraded; `time-limit` alone would be capped.
