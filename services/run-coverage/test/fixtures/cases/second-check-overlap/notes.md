# second-check-overlap

The recorded run stored twice (synthetic repeat). The second check's page 1 shares all 20 listings with the first: the gap check passes, still `capped`. The bounded baseline stays with the first read (the first read of a basis wins).
