A barcode the catalogue knows (seeded as an `ean` code) identifies the scan with no model call, even though a photo came too (docs/scan-mode.md: "No scan is sent to a model when a barcode resolves").
