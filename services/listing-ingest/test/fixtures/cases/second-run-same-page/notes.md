# second-run-same-page

The same page collected twice (two jobs with the recorded rows): the second run adds 20 sightings
and gives zero `first-seen` (`docs/backlog.md:24`) and no `card-changed`, since no card changed.
