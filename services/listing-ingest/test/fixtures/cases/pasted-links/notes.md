# pasted-links (synthetic)

Built from the recorded run: the 20 listings arrive first through a details run (pasted links),
never seen in a feed. Expected: 20 listing identities, 20 `detail` observations, no feed sighting,
and `first-seen` for all 20. Listing 28242423458759790 has 17 digits and is kept as text.
