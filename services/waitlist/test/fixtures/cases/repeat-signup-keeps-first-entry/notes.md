A repeat sign-up with the same address (any case) writes one row (task 0.5a, "Done"). The second
submission's different postcode and UTM are discarded: `docs/questions.md`, "waitlist: repeat
sign-ups" takes the conservative option of never overwriting a stored entry's attribution.
