Every field the module card lists: email, postcode, wanted products and all five UTM parameters.
The postcode is stored upper-cased with one space; wanted products are free text (module card:
"may later feed demand counts, with consent").
