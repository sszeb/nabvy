Task 0.5a's definition of done: "a waitlist entry lands in the database with its UTM." One
submission with `utm.source`/`medium`/`campaign` set; the stored entry carries all three, and no
other UTM field.
