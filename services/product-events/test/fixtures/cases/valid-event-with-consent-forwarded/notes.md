Consent gate, positive case: a user with `analytics_consent = true` (module card, "Inputs":
`v_profiles`) gets their event both recorded and forwarded. `docs/analytics.md`'s `alert_delivered`
properties, matching `fixtures/contracts/product-events/ProductEventsEvent.alert-delivered.json`.
