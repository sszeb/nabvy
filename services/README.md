# services

One package per module in `docs/modules.md`, named `@nabvy/<module>`, each with the shape in
`CLAUDE.md`: `README.md`, `src/index.ts`, `src/handlers/`, `src/domain/`, `src/repo/`, `test/`.
Each module's package is created by the first backlog task that needs it.
