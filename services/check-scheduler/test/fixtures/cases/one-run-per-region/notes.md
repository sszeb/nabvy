# One run per region per tick

Card: "All of a region's due terms go in one run" (31% cheaper per term). Both regions are new; each gets exactly one newest-first run holding every term, narrow first. The paid region comes first. Sweeps wait for a later tick: one run per region per tick.
