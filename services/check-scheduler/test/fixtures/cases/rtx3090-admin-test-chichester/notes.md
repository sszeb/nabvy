# The owner's test hunt at Chichester

The admin-test pair `rtx3090` at Chichester (search-planner case `rtx3090-admin-test-chichester`) is new, so the first tick in active hours sends one newest-first check for it. Chichester's ID is the recorded run's `cityId` (`fixtures/listings/facebook/runs/2026-09-24-VkryjpwS6U2GBDh3k/input.json`).
