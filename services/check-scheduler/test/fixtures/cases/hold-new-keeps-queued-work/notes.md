# hold-new starts nothing new and drops nothing

At `hold-new` Chichester's running pair continues at the slowed cadence (paid: 2 h; last ran 2 h ago), a queued rerun still goes, but Belfast's pair has never run and waits, and so does the approved gap-fill one-off. Nothing is dropped: both are reported as waiting (`actor-integration.md` 2.12).
