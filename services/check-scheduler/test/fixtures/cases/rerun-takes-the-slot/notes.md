# A degraded search's rerun takes its region's slot

A pending rerun of a degraded broad sweep goes in the region's one run this tick, in its original shape; the newest check waits for the next tick. A verification one-off at an unverified centre runs with the `verification` shape.
