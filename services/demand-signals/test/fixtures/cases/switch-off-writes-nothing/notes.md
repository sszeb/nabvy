# switch-off-writes-nothing

The module off (its default): the publish acknowledges and writes nothing (rule 11), so even with
the switch turned to shadow afterwards `v_cells` has no rows for the week.
