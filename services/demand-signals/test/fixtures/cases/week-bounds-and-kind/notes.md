# week-bounds-and-kind

The week is [Monday 00:00 UTC, next Monday 00:00 UTC). Counted: the first instant, the last second,
and 8 adverts with no listing time, weeked by T1 (`first_fetched_at`): 10. Not counted: the next
Monday's first instant, the previous Sunday's last second, and 4 listings whose kind is `pc` or
`laptop` rather than parts-record's `wanted_or_swap`.
