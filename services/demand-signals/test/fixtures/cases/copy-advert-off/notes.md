# copy-advert-off

The soft dependency off: `copy_advert.v_members` is empty, so each advert counts once on its own
(rule 11: a missing row is "no data"). 7 + 3 = 10. With copy-advert on the same seed would be
7 + 1 = 8 and suppressed.
