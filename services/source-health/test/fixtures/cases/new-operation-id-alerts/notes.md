A single healthy search (route `http`), but route-health's decision for the job's region reports
one operation ID this module has not recorded before today (`newQueryIds`). `evaluateAlert` fires
`new-operation-id` regardless of the degraded share.
