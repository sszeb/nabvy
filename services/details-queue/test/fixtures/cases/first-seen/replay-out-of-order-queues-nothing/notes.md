Synthetic, from the known risk in PR #35's review. Three search listings are first seen (a details-origin listing is not queued: its own run fetched it). The same event is replayed before submit (kept once), again while the batch is leased (kept once, no second fetch), and again after the batch closed with full descriptions (skipped). Across all replays each ID is sent to Apify exactly once; only the genuinely new ID 4 gets its own batch.
