A city page with no town label (a details-only sighting; listing-ingest's `town_label` can be
null) gets named after its own ID and an empty `towns` array: the module has no other source for
a name and never guesses one (CLAUDE.md, "No invented numbers").
