# seller-key-only

A request carrying one seller key (SHA-256 of `seller-a`, derived when the test runs,
standing in for a `seller-key` HMAC key). It is recorded but hides nothing: seller-key does not publish
`restricted_listing_keys` yet (docs/questions/listing-suppression.md).
