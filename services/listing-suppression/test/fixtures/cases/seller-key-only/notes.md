# seller-key-only

A request carrying one seller key (a synthetic 64-hex value standing in for a
`seller-key` HMAC key). It is recorded but hides nothing: seller-key does not publish
`restricted_listing_keys` yet (docs/questions/listing-suppression.md).
