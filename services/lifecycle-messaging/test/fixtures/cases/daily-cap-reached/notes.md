The user already has a marketing message recorded earlier today (`win-back.30d`, a different
programme -- the cap is global, not per programme). `cap-reached`'s `immediate` step is due, would
otherwise send (consented, unsuppressed), but is held back by the daily cap; `3d` is separately not
yet due. Neither writes a row, so `immediate` sends on the next day's run.
