The module's core "fire from a test event" case (module card, "Tests and fixtures"): a user
created a hunt 3 hours ago, never linked a channel, has consented to `tips` and is not suppressed.
The `2h` step is due and sends.
