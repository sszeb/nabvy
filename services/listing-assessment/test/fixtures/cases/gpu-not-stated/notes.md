# gpu-not-stated

The card's test: a container with no GPU anywhere is "not stated"
(`fb-scrap-engine/docs/design/CONTAINER_LISTINGS.md:43-45`). Synthetic, built from recorded row
`28242423458759790` with the GPU line removed from its description. A container by parts, GPU
`not_stated` (never `none`), and `gpu` is the one unknown in `v_unknowns`.
