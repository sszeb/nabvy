# cautions

Two synthetic listings built from recorded rows:

- `1716871313386322` fetched from a stale cache (`detailCacheStatus: "stale-fallback"`): the
  caution `stale_text`, and nothing is confirmed (R5 needs fresh detail).
- `1154042653756525` retitled "RTX 3080 box only" ("Empty box for an RTX 3080 with the
  manuals."): form `box_only`, the caution `box_only`, not a container, nothing confirmed, and
  the GPU state `none` from the title's not-included card (parts-rules' box-only reading).
