# offers-title

The card's test: a complete system titled "OFFERS" is a container
(`fb-scrap-engine/docs/design/CONTAINER_LISTINGS.md:153-154`). Synthetic, built from recorded row
`1816901372840238` (PCSpecialist tower, full spec list) with its title replaced by "OFFERS".
The title gives no kind (parts-rules leaves it open), and the description names CPU, RAM and
storage, so it is a container by parts (R1), form `system`, GPU `named`.
