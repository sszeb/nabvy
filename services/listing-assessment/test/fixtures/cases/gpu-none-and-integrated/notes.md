# gpu-none-and-integrated

Two synthetic listings built from recorded rows:

- `28242423458759790`, description "… no GPU, add your own." with no card named: positive
  evidence (R6, `fb-scrap-engine/docs/design/CONTAINER_LISTINGS.md:183-185`), so the GPU state
  is `none` with the phrase as its exclusion.
- `2005582460147017`, description "Ryzen 7 5700G with integrated graphics …": GPU state
  `integrated`; "No issues." is not read as "no GPU".
