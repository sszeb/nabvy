# trigger

Trigger.dev task definitions, thin wrappers around service functions (`docs/engineering.md`,
"Events and tasks"). The first task arrives with 1.2. Which runtime runs the pipeline is an open
question in `docs/questions.md`.
